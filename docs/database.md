# Datenbank-Referenz (Supabase)

Vollständige Referenz aller Tabellen, Spalten, RPCs und der Realtime-Konfiguration.
Die Spalten-Doku enthält bewusst auch die Verhaltens-Contracts der App (wie eine
Spalte gelesen, geschrieben und aufgelöst wird). Vor Arbeit an Migrationen,
`store.js`-Sync oder Features mit DB-Berührung den passenden Abschnitt lesen.

**Pflege-Regel:** Bei jeder DB-Änderung diese Datei mitziehen (Workflow: CLAUDE.md,
Abschnitt „Datenbank (Supabase)"). `supabase/schema.sql` ist der vollständige
Schema-Snapshot (Tabellen, RLS, Funktionen, Trigger, Realtime) und muss immer mit
dem Live-Schema übereinstimmen.

**Drift-Checks (automatisiert):** `tools/check-db-docs.cjs` (CI, bei jedem Push)
replayt alle Migrationen und schlägt fehl, wenn Tabellen/Spalten/Funktionen nicht
in `schema.sql` und dieser Datei nachgezogen wurden. `tools/check-db-live.cjs`
(`.github/workflows/db-drift.yml`, wöchentlich + manuell) vergleicht zusätzlich
die echte Datenbank. Mit Service-Key (GitHub-Secret `SUPABASE_SERVICE_ROLE_KEY`):
volles Inventar via `admin_schema_inventory()` (Spalten in beide Richtungen,
anon- und authenticated-EXECUTE-Grants, Realtime-Publikation). Ohne Key: Existenz-Probe aller
`schema.sql`-Spalten per anonymer Read-only-Selects. (Die PostgREST-OpenAPI-Spec
unter `/rest/v1/` wäre die schlankere Quelle, ist bei Supabase aber nur noch mit
dem service_role-Key abrufbar.)

**Playbook: db-drift-Workflow schlägt fehl** (der Nutzer postet typischerweise
das Actions-Log). Fehlschläge sofort bereinigen, nicht liegen lassen; jede
Bereinigung endet damit, dass `node tools/check-db-docs.cjs` lokal grün ist und
der Nutzer den Workflow re-runnt (Actions → „DB drift check" → Run workflow).

- **Exit-Code 2 (operational):** kein Drift, sondern Netz/Auth/Setup. Erste
  Log-Zeile prüfen: `service key: not set` heißt Secret-Name falsch (muss exakt
  `SUPABASE_SERVICE_ROLE_KEY` heißen). „Has Migration 0142 been applied?" heißt:
  Nutzer bitten, Migration 0142 auszuführen. Sonst: Supabase-Erreichbarkeit.
- **Exit-Code 1 (echter Drift), je nach Meldung:**
  - *Spalte/Tabelle existiert live, fehlt im Repo:* Es wurde an den Migrationen
    vorbei live geändert (SQL-Editor/Dashboard). Mit dem Nutzer klären, ob die
    Änderung bleiben soll. Ja: als idempotente Migration nachziehen
    (`IF NOT EXISTS`) und `schema.sql` + diese Datei nachdokumentieren.
    Nein: Nutzer baut sie live zurück.
  - *Spalte/Tabelle im Repo, fehlt live:* Entweder wurde eine Migration nie
    ausgeführt (Nutzer bitten, sie auszuführen) oder live wurde etwas
    entfernt. Erst klären, dann Repo oder DB angleichen; nie stillschweigend
    aus der Doku löschen.
  - *`anon_exec = true`:* zuerst `EXPECTED_ANON_EXEC` in `tools/check-db-live.cjs`
    prüfen, dort stehen bewusste Ausnahmen (aktuell `get_public_feature_map()` und
    `get_founding_seats()`, siehe „Grant-Fallen" unten; die Menge ist auf **volle
    Signaturen** gekeyt, nicht auf nackte Namen). Ist die Funktion dort nicht gelistet, ist es eine
    echte Grant-Falle: REVOKE-Migration nach dem Muster von 0141 nachschieben.
    Fehlt umgekehrt der Grant für eine gelistete Ausnahme, ist der Live-Grant
    kaputt (Migration erneut prüfen), nicht die Doku.
  - *`has_function_privilege('authenticated', ...) = true` auf einer
    service-role-only Funktion:* eine echte Grant-Falle, und zwar die Hälfte,
    die noch offen ist (siehe „Grant-Fallen" unten): die
    `ALTER DEFAULT PRIVILEGES`-Regel vergibt `EXECUTE` bei jedem
    `CREATE FUNCTION` weiter direkt an `authenticated`. Fix ist ein explizites
    `REVOKE EXECUTE ... FROM authenticated` als Migration, Muster: 0208. Soll die
    Funktion in Wahrheit für eingeloggte User da sein, stattdessen die Signatur
    aus `EXPECTED_NO_AUTHENTICATED_EXEC` in `tools/check-db-live.cjs` entfernen.
  - *„service-role-only function ... is absent":* die Funktion wurde umbenannt,
    gelöscht oder ihre Signatur hat sich geändert. Erst klären, dann die
    Signatur in `EXPECTED_NO_AUTHENTICATED_EXEC` nachziehen.
  - *„authenticated_exec not reported" (nur ein Hinweis, kein Fehler):*
    Migration 0258 ist live noch nicht eingespielt, der authenticated-Teil des
    Grant-Checks läuft bis dahin nicht.
  - *Unerwartete App-Tabelle in der Realtime-Publikation:* Wenn gewollt, die
    Realtime-Doku (hier + CLAUDE.md) **und** `EXPECTED_REALTIME` in
    `tools/check-db-live.cjs` nachziehen; sonst Publikation bereinigen lassen.

## Tabellen & Spalten

### `zane_exercises`

Übungs-Library des Users.

- `id` (text), `user_id` (uuid), `name`, `note`, `category` (text), `tags` (text[]), `equipment` (text)
- `unilateral` (boolean), `movement_type` (text: 'bilateral'|'unilateral'|'assisted'|'mobility'; 'cardio' on the built-in cardio exercise). `'assisted'` (assisted dip/pull-up/chin-up) stores the machine/band assistance as a **negative** kg load, resolved via `LB.isAssisted(ex)`; no schema change (free-text column).
- `no_weight_reps` (boolean, default false): **legacy** binary "no weight" flag; superseded by `log_mode` but kept written in sync (`no_weight_reps = log_mode <> 'weight'`) so older cached clients still hide the weight field. Read via the `LB.exerciseLogMode` fallback, never trust it alone once `log_mode` exists.
- `log_mode` (text, nullable): how the exercise is logged. `'checkbox'` (tick each set, no numbers, 0 volume) | `'reps'` (reps only, no weight, 0 volume) | `'weight'` (weight + reps); null ≈ `'weight'`. Resolved by `LB.exerciseLogMode(ex)` (log_mode wins, else legacy `no_weight_reps ? 'reps' : 'weight'`). The exercise editor shows a **Logging** picker whenever `equipment ∈ {no_equipment, bodyweight}` OR `movement_type === 'mobility'`; loaded equipment is always weight+reps, and `movement_type === 'assisted'` always forces weight+reps (it logs the negative assistance load). Migration 0139.
- `bodyweight_mode` (text, nullable, CHECK in `'pull' | 'plus_load'`): dreiwertiger Nachfolger von `pull_bodyweight` (Migration 0243), nur sinnvoll bei `equipment='bodyweight'` + `log_mode='weight'`. `null` = Gewicht selbst eintippen, `'pull'` = letztes geloggtes Körpergewicht vorbefüllen (identisch zum alten Boolean), `'plus_load'` = **nur die Zusatzlast eintippen** (gewichtete Klimmzüge, Dips mit Gürtel), die App addiert das Körpergewicht. Aufgelöst über `LB.bodyweightMode(ex)`, das auf das alte Boolean zurückfällt, wenn die Spalte leer ist. **`pull_bodyweight` wird weiterhin im Gleichschritt geschrieben** (`mode === 'pull'`), damit ein Client, der noch einen älteren SW-Cache fährt, das Vorbefüllen behält; die Spalte ist damit abgeleitet, nicht mehr die Quelle. `LB.shouldPullBodyweight` liest jetzt ebenfalls den Modus und ist für `'plus_load'` bewusst **false**: dort hält das Feld die Gürtel-Last, ein Vorbefüllen mit dem Körpergewicht würde die doppelte Last loggen. Angezeigt wird ein `'plus_load'`-Set über `LB.setLoadLabel(set)`; geschrieben wird im Training ausschließlich über den `weightPatch`-Helper in `screens-train.jsx` (einziger Schreibpfad: Tippen, Backspace und Stepper laufen alle über `kbApply`).
- `pull_bodyweight` (boolean, default false): opt-in, pulls the user's latest logged bodyweight (`LB.latestBodyweight`) as the set's starting weight. Only meaningful for `equipment='bodyweight'` + `log_mode='weight'`; the editor toggle is only enableable once a weight has been logged. Replaces the old implicit auto-pull that fired for every bodyweight exercise. Resolved by `LB.shouldPullBodyweight(ex)`. Backfilled `true` for existing bodyweight rows to preserve behaviour. Migration 0139.
- `progression_reps` (int, **deprecated/legacy**): used to be a global cross-plan "Rep Target" editable in the exercise screen that silently overrode every plan's per-item reps on save; removed from the UI once per-plan-item Range mode plus the per-exercise Smart Progression override (`repsMax`/`progressionOffset`, Migrations 0134/0135) made it redundant and conflicting. Column kept for old data / backward-compat reads only (e.g. `addExercise`'s new-item seed default in `screens-schedule.jsx`); no longer written to by any UI.
- `youtube_url` (text, nullable): optional form-reference video; shown as a play button in the exercise editor/detail and during training. Migration 0106.
- `note_pinned` (boolean, default false): when true and the exercise has a non-empty `note`, that note pops up in a must-acknowledge sheet the first time the exercise becomes active in a session (once per session, every workout), so a setup note can't be forgotten. A per-exercise toggle next to the note field (`ExerciseCreator` / `ExerciseDetailScreen`) sets it; the training screen fires the sheet on exercise-start (dismissed exIds tracked per session). Store field `note_pinned` (exercises are a raw DB-row passthrough, so it flows through load/sync/backup with the other columns). In the user backup. Migration 0167.
- `progression_increment` (numeric, nullable, `CHECK (progression_increment IS NULL OR progression_increment > 0)`): per-exercise override for the weight-bump step used by Smart Progression and the Meso/Autoregulation weight-boost engine. Both normally derive the increment from `zane_user_settings.equipment_config[ex.equipment].increment` (see there), falling back to a per-call-site hardcoded default when that's unset too; a non-null value here wins over both, ahead of the equipment-category config. Resolved by the shared `LB.incrementForExercise(store, ex, fallback, catCfg?)` (`store.js`), used by `progressionSuggestion`, the live "Progression unlocked" check and outlier guard in `screens-train.jsx`, `computeMesoGains`, and the post-hoc feedback-edit re-earn path (`fbEarnInputs`, `screens-lib.jsx`). Must be strictly positive: 0 or negative is treated as unset and falls through the chain rather than applied literally, since the Meso earn/cut logic reads the increment's sign to distinguish an earned bump from a rep-miss cut, and the outlier "possible mistype" tolerance band is sized off it too, so a 0/negative value would invert or collapse both. `NumInput`'s `positiveOnly` prop (`ui.jsx`) enforces this in the editor UI; the same rule applies to `equipment_config[key].increment` (see below), both funneled through `incrementForExercise`. Editable in the exercise editor (`ExerciseDetailScreenInner`); intentionally not part of the exercise-creation wizard (an advanced, rarely-touched field, same treatment as the YouTube link and note). Migration 0184.
- `horn_labels` (jsonb, nullable, Migration 0254): geordnete Namen der Aufnahmedorne einer Multi-Dorn-Maschine (PRIME und Ähnliche), z.B. `["Standard","Mid","High"]`. Nicht-leer heißt: die Übung wird **pro Dorn** geloggt (`zane_sets.horn_loads`), die Satzzeile zeigt die Summe. Bewusst **keine** zweite Modus-Spalte, `no_weight_reps` gegen `log_mode` zeigt im selben Schema, was eine abgeleitete Doppelung kostet. Aufgelöst über `LB.exerciseHornLabels(ex)` / `LB.isMultiHorn(ex)`. Nur sinnvoll bei `log_mode='weight'`. **Es wird bewusst keine effektive Last gerechnet:** bei einer Hebelmaschine ist der Widerstand ein Drehmoment, die Hebelverhältnisse je Dorn sind modellspezifisch und unveröffentlicht, eine berechnete Zahl wäre erfunden und flösse in e1RM, PRs, Volumen und die Autoregulation.

### `zane_workout_templates`

- `id` (text), `user_id` (uuid), `name` (text), `created_at` (timestamptz)
- `exercises` (jsonb): `[{ exId, name, sets, reps, repsPerSet, repsMax, progressionOffset, supersetGroup }]`, structure only, no logged sets. `repsMax` optional: top of a Range reps target, `reps` holds the floor, mutually exclusive with `repsPerSet`. `progressionOffset` optional: per-exercise Smart Progression override, `0` = off, `N` = on with base+N ceiling, unset = inherits the global setting.
- Store field: `store.workoutTemplates`. Synced via `syncStore` diff (like `cardioPlans`). Saved from a finished freestyle session, used to start a freestyle session ("From template") or imported into a plan day (Plans|Templates sub-tab in the day import picker). Migration 0107.

### `zane_checkin_schema_templates`

- `id` (text), `user_id` (uuid, the coach), `name` (text), `schema` (jsonb, same shape as `zane_coaching.checkin_schema`), `created_at` (timestamptz)
- RLS: owner-only (`auth.uid() = user_id`), identical pattern to `zane_workout_templates`. Not loaded on a coach-load of a client's store (`isCoachLoad`): these are the acting coach's own saved schema snapshots, never the viewed client's.
- Store field: `store.checkinSchemaTemplates`. Synced via `syncStore` diff, no dedicated RPC. Soft-capped at 5 per user, enforced client-side only (UI hides "Save as template" once 5 exist). Used from both `CheckInSchemaBuilder` instances: the coach's per-client builder (`screens-coaching-detail.jsx`, `ClientCheckInsTab`) and the self-coaching builder (`screens-coaching-tabs.jsx`, `ClientCheckInTab` with `isSelf`); real (non-self) clients never open the builder. Each template row supports apply (load into the draft), update-in-place (overwrite name/schema, keeps the same `id`), and delete. Exists so a coach can snapshot the outgoing default before overwriting it for "All clients" (previously instant and irreversible), and reuse a saved form across clients or, for self-coaching, across time. Migration 0152.

### `zane_schedule_backups`

- `id` (text), `user_id` (uuid), `schedule_id` (text), `schedule_name` (text), `days` (jsonb: same format as `zane_schedules.days`, always a non-empty array), `created_at` (timestamptz)
- Automatic snapshots of a schedule's `days`, written fire-and-forget from `syncStore` whenever `days` changes to a valid non-empty array. Never written if `days` is empty or malformed (guards against backing up broken state). Used by the "Backups" button in the plan viewer to restore a previous day layout. Initial snapshot of all valid plans inserted via Migration 0114.

### `zane_plan_drafts`

- `user_id` (uuid), `schedule_id` (text), `draft` (jsonb: the in-progress editor draft, same shape as the schedule object being edited), `updated_at` (timestamptz); PRIMARY KEY `(user_id, schedule_id)`. Migration 0162.
- Multi-device autosave for the plan editor. `ScheduleEditScreen` holds edits in a local `draft` that only reaches `zane_schedules` on an explicit Save, so until then an app kill / closed tab / device switch loses the work. This table holds that draft, synced across devices, **deliberately decoupled** from the committed plan: a debounced autosave writes only this small row (never the large `zane_schedules` row), so it can never touch `days`/`versions`, and it stays out of the `zane_schedules` boot-merge entirely. Owner-only RLS (`to authenticated`), last-write-wins over `updated_at`. One draft per (user, plan); the row is **deleted** the moment the editor Saves (commits to `zane_schedules`) or Discards. Transient in-progress state, **not** in user backups (`EXCLUDED` in `check-backup-coverage.cjs`). No FK to `zane_schedules` (the two stay independent); the app deletes a plan's draft row when the plan itself is deleted.

### `zane_meso_states`

One row per (user, plan). Store field: `store.mesoStates`. Migration 0120.

- `id` (text): `userId + '_' + scheduleId`, deterministisch
- `user_id` (uuid), `schedule_id` (text), `weeks` (int, nullable: null = autoregulate-only, no fixed block length, see `zane_schedules.mesocycle_autoregulate`. Migration 0160), `start_date` (text, YYYY-MM-DD), `start_cycle_index` (int, default 0)
- `deltas` (jsonb, default `{}`): `{ exId_dayId: ±N }` set count adjustments
- `joint_flags` (jsonb, default `{}`): `{ exId: true }` flagged exercises
- `pump_low_counts` (jsonb, default `{}`): `{ exId: N }` low-pump counter
- `weight_boosts` (jsonb, default `{}`): `{ exId_dayId: increment }` weight adjustment applied to next session's seed. Usually a positive earned increase; can also be a **negative** cut from the `rep_miss_counts` streak below (see there). Re-earned every session either way: on the exercise's next training day the key is wiped first and only re-set if that session's outcome (earn or cut) writes it again, so neither a boost nor a cut lingers past the one session it seeds. **Range exercises earn by double progression** (`LB.mesoEarnTarget` / `LB.mesoRepOutcome`): the boost is granted only once the TOP of the range is reached, on a staggered per-set ladder, first (freshest) set must hit `plannedRepsMax`, the last only the floor `plannedReps`, sets in between linearly interpolated (a single working set uses the range midpoint). This makes the weight HOLD while the lifter climbs the range instead of creeping every session, then jump one increment. When a boost (or a cut) does move the weight, the seed resets the reps back to the range floor (`repFloor` arg to `LB.resolveMesoSeedSuggestion`) so double progression restarts at the new load. Uniform / Per-Set items are unchanged (the ladder collapses to their plain target). Migration 0166 note: earn-ladder + reps-reset shipped there alongside `meso_recap`, no schema change. **Deleting a finished meso session restores the `weight_boosts` / `rep_miss_counts` keys it owned** (`LB.revertMesoSessionBoosts`, when it was the most recent non-deload session of its day for that key; a later same-day session that retrained the exercise keeps its own value untouched) **to whatever the most recent EARLIER same-day session for that exercise had itself left behind, instead of just clearing them**: every session's finish re-earns wholesale (see above), so the deleted session's own finish had already overwritten an earlier session's still-live boost the moment it ran, not just added to it. A plain clear would drop that earlier boost too, seeding the re-log light (delete a session that consumed an earlier +2.5 kg bump and the re-log would seed 2.5 kg under what it should). `weight_boosts` restores from the earlier session's own `meso_recap.gains` entry for that key (`gains` rows only started carrying a `key` field from the 2026-07-19 decline-toggle change; an earlier session's recap is matched by the exercise name its own entry used instead), falling back to a plain clear only when no such earlier session exists or it predates `meso_recap` entirely. `rep_miss_counts` restores from the deleted session's own `meso_recap.raw.repMissBase` snapshot (its pre-finish streak, captured before that session's own re-earn), no history scan needed. Both the store copy and the `logbook-meso-state-<planId>` cache are re-stamped so `getMesoState` prefers the rollback. Set-count `deltas` are intentionally not rolled back.
- `weight_boost_declines` (jsonb, default `{}`): `{ exId_dayId: true }` a user-declined earned bump, set by tapping "Decline" on the given exercise's item on the post-session "Next session" gains sheet (positive `weight_boosts` deltas only, never a rep-miss cut). While set, `LB.resolveMesoSeedSuggestion` sees that key's `weight_boosts` value as withheld for seeding purposes (same effect as a joint/pump/volume gate failing: the weight holds instead of climbing, Smart Progression's own suggestion is vetoed too since the plan is meso-active). `weight_boosts` itself is left untouched (not deleted), so undoing a decline (clearing the key) restores the original earned amount with nothing to recompute. Cleared automatically for a key the moment that key is re-earned/re-evaluated the next session (`LB.clearMesoWeightBoostDeclines`, called from the same `computeMesoGains` step that re-earns `weight_boosts`), so a decline never persists past the session it was set in: the user is asked again every time a bump is earned, never a standing cooldown. Cleared (never restored, unlike `weight_boosts`/`rep_miss_counts` above) on session delete (`LB.revertMesoSessionBoosts`) so a restored or freshly-cleared boost is always offered fresh next time, not silently suppressed by a decline that belonged to the deleted session's own evaluation; also reconciled on an exercise-swap re-key (`LB.remapMesoStateExId`). Store field `weightBoostDeclines`. Migration 0178.
- `growth_counts` (jsonb, default `{}`): `{ exId_dayId: N }` how many set-grants that exercise has received this meso block, from either a "Volume: Not enough" answer **or** a low-soreness recovery signal ("Never sore" / "Healed a while ago"). Both share this pool so the two feedback questions stay fair to each other; kept separate from `deltas` so an unrelated shrink event never distorts turn fairness. Used to rotate which exercise of a muscle group grows next (fewest grants among those still below their own per-exercise `deltas` ceiling wins, ties toward the muscle group's main/first lift; a newly swapped-in exercise is seeded at the group's current max instead of 0 so it can't cut ahead) instead of always growing only the main lift. Store field `growthCounts`. Migration 0130.
- `affinity` (jsonb, default `{}`): `{ exId: { v: 'love'|'ok'|'dislike', streak: N } }` per-exercise affinity ("keeper, or would you swap it?"), a slow-moving preference signal asked alongside the per-exercise joint/weight/pump feedback. `v` is the sticky current value (pre-filled every session, so it costs no taps in steady state); `streak` counts consecutive `dislike` confirms (any `love`/`ok` resets it to 0) and drives the adherence swap suggestion at `>= 2`. It **gates nothing** (a disliked-but-effective lift still earns its weight); it only feeds the swap hint. Store field `affinity`, exId-keyed (remapped on restore like `jointFlags`/`pumpLowCounts`). In the user backup. Migration 0169.
- `rep_miss_counts` (jsonb, default `{}`): `{ exId_dayId: N }` consecutive-session counter, objective counterpart to the subjective soreness/joint/volume signals above. A working set "misses" when it doesn't reach its own rep target (per-set target if the item uses Per-Set, else the uniform/Range-floor `plannedReps`; a Range exercise's `plannedRepsMax` only marks the ceiling that EARNS extra progression, not what counts as hit). The exercise's own LAST working set is exempt from counting as a miss (an all-out final set failing from accumulated fatigue doesn't mean the weight itself is too heavy; an earlier set failing does), except when there's only one working set, which has no earlier set to lean on and counts directly. Any qualifying hit resets the counter to 0. Two qualifying misses in a row cut that exercise's weight by one increment for the next session (a negative entry in `weight_boosts`, reusing the same re-earned-every-session mechanism) and reset the counter. Runs independently of the joint/pump/volume gates and of Load-only mode (where it's the *only* rep-driven lever, since set deltas are frozen there). Store field `repMissCounts`. Migration 0165.
- `completions` (int, default 0): how many meso blocks completed on this plan
- `pending_meso2` (boolean, default false): set when the last meso week finishes and the user chose to start a deload first; cleared when the user responds to the Meso 2 offer on the home screen after deload ends. Store field `pendingMeso2`. Migration 0121.
- `started_at` (timestamptz, nullable): exact block-start timestamp; `mesoCurrentWeek` prefers it over the date-only `start_date` so a previous block's sessions logged the same calendar day the new block starts can't fast-forward the flex week/RIR counter. Store field `startedAt`; was client-only until it round-trips through the DB in Migration 0138.
- `autoreg_state` (jsonb, **nullable**, no default): versioned Autoreg v2 blob (spec 9). In P2 it carries the anti-nag deload-nudge cooldown: `{ version, deloadNudge: { block: { declinedAt, cooldownUntil, escalation } } }`. `declinedAt` is the ISO time of the last decline, `cooldownUntil` is the block-session count at which the full deload offer may fire again (spec 10: cooldown is measured in **sessions**, N=3), `escalation` counts prior declines this block (drives the escalated-evidence re-ask, spec 5.3). Written only in the Auto-mode emergent-deload path (`offerEmergentDeload` decline) and cleared on auto stand-down (`LB.clearDeloadNudge`) when the detector no longer flags a ceiling; later phases (P3) extend the same blob with `landmarks` / `block` snapshots (`overreach` is never persisted here, it's a stateless recompute from session history each call, spec 2.2). v3 adds `landmarks[muscle].mevFloor` (a per-muscle MEV floor that mirrors the learned MRV's growth/shrink across blocks instead of pinning to the plan's authored set count) and `block.startMrvByMuscle` (the per-muscle MRV snapshot the next reset diffs against). Nullable so an unset plan reads null and (like `started_at`) is COALESCE-preserved on update, an older client that omits it can't wipe live nag state. Store field `autoregState`; **in the user backup** (real per-user governance: a restore must not reset an active cooldown into a re-prompt loop), copied verbatim on restore (scope/muscle-keyed, not exId-keyed, so no remap). Migration 0172.
- `created_at`, `updated_at` (timestamptz)
- Synced via `syncStore` diff. Ersetzt den per-Device localStorage-Key `logbook-meso-state` (Meso-Fortschritt ist cross-device). localStorage dient während einer Training-Session als schneller Write-Through-Cache; am Session-Ende via `flushMesoStateToStore()` in den Store (→ DB) geflusht.

### `zane_feature_grants`

- `feature` (text), `email` (text)

### `zane_profiles`

- `id` (uuid), `name` (text)
- `x_handle` (text, nullable): optional canonical X handle stored as `@name`; the client accepts `name`, `@name` and `x.com/name` and normalizes them without API verification. Migration 0262.
- `x_handle_public` (boolean, default `true`): allows future public/social features to display the handle. Admin support views can still see it when this is disabled. Migration 0262.
- `x_handle_prompt_opted_out` (boolean, default `false`): permanent opt-out from the optional X-handle prompt until the user adds a handle again. Migration 0262.
- `tier` (text, NOT NULL, default `'free'`, CHECK in `'free' | 'lifetime' | 'premium'`): Account-Tier, **server-authored**. `'lifetime'` vergibt ausschließlich der Trigger `zane_sessions_grant_lifetime` → `grant_lifetime_if_qualified()` (Migration 0240), sobald ein Account **alle drei** Hürden nimmt (Stand Migration 0242): ≥ 5 beendete Workouts, ≥ 150 Trainingsminuten (dabei zählen **maximal 120 pro Session**) und Abschlüsse an ≥ **3 verschiedenen Server-Tagen**, solange freie Plätze existieren (`zane_app_config.lifetime_seats_total`, default 75). Die Zeit- und Tages-Hürden sind bewusst **nicht** öffentlich dokumentiert: die Workout-Zahl steht auf `welcome.html`. Die ursprüngliche Fassung (nur Workouts + Minutensumme) war in einem einzigen Request fälschbar, weil `duration_minutes` ein Client-Wert ist; der Per-Session-Deckel entwertet die eine lange Fake-Session, und die Tages-Spreizung liest `zane_sessions.completed_server_at` (siehe dort), kostet einen Fälscher also drei echte Kalendertage. `'premium'` ist für spätere Bezahl-Accounts reserviert, wird derzeit nirgends gesetzt und hat bewusst **keinen** Chip in der UI (`TIER_CHIPS` in `screens-home.jsx` kennt nur `free` und `lifetime`): ein Badge für einen Zustand, den die App nicht erreichen kann, wäre erfunden.
- `tier_granted_at` (timestamptz, nullable): Zeitpunkt der Vergabe.
- **Client kann `tier` nicht setzen:** Die Policy `own profile` ist `FOR ALL`, ein Client könnte sein Profil also direkt per REST patchen. Der BEFORE-UPDATE-Trigger `zane_profiles_protect_tier` setzt `tier`/`tier_granted_at` für `anon`/`authenticated` still auf den alten Wert zurück (kein `RAISE`, damit normale Profil-Writes wie eine Namensänderung weiter durchgehen). `store.js` liest `tier` nur und schreibt es nie zurück. Die Spalte ist außerdem bewusst **nicht** im User-Backup: ein editiertes Backup-JSON wäre sonst ein Gratis-Upgrade. Gerendert wird der Tier als `TierChip` unter der Wordmark im Home-Screen.
- Die frühere Spalte `approved` samt Signup-Approval-Workflow ist mit **Migration 0241** entfernt: die Registrierung ist offen und wird nicht mehr freigeschaltet.

### `zane_app_config`

Globale Admin-Config (RLS an, nur SECURITY-DEFINER-Funktionen greifen zu).

- `id` (int, Singleton = 1)
- `lifetime_seats_total` (int, NOT NULL, default 75): Größe der Founding-Member-Kohorte. Liegt in der Config statt im Trigger, damit sie ohne Deploy erhöht werden kann. Migration 0240.
- `force_update_nonce` (text, nullable): gesetzt von `admin_force_update()`, pusht das "New version available"-Banner an alle Clients ohne `sw.js`-Cache-Bump. Migration 0131.

### `zane_feature_map`

**DRAFT-Ebene** der Feature-Map (Option A + Live-Publish). Der **Master-Inhalt** liegt versioniert im Code (`src/feature-map-db.js`, `window.FEATURE_MAP`, Kategorien + Karten mit stabiler `id`); den rendern alle User und die Public-Seite als Basis. Diese Tabelle ist der **private Entwurf des Admins**: hier landen alle In-App-Änderungen (ausblenden/editieren/hinzufügen/sortieren), gekeyt auf die Katalog-Karten-`id`, als Live-Vorschau im `FeatureMapScreen` über den Katalog gelegt. Beim **Publish** (`publish_feature_map()`) wird dieser Stand in `zane_feature_map_published` gespiegelt; „Discard all" (`discard_feature_map()`) setzt den Entwurf zurück auf den veröffentlichten Stand. **Nicht** im User-Backup (in `check-backup-coverage.cjs` `EXCLUDED`). Migration 0154 (Content) → 0155 (Overrides) → 0156 (Publish-Flow).

- `card_id` (text, PK): Katalog-Karten-`id`, oder ein `custom-…`-Slug für admin-hinzugefügte Karten
- `hidden` (boolean, default false): Karte in der Vorschau/Publikation ausblenden (Soft-Delete)
- `is_custom` (boolean, default false): true = admin-hinzugefügte Karte (nicht im Katalog)
- `cat`, `name`, `role` (CHECK null/`user`/`coach`/`both`), `summary`, `actions` (jsonb), `sort` (int): alle **nullable**. Bei Katalog-Karten überschreiben nicht-null Werte den Default (null = erben); bei Custom-Karten der volle Inhalt. `sort` = Reihenfolge innerhalb der Kategorie (null = Katalog-Reihenfolge).
- `created_at`, `updated_at` (timestamptz, default `now()`)
- **RLS:** eine Policy `feature_map_admin_all` (`FOR ALL TO authenticated`), nur Admin via `auth.email() = 'office@btc-prime.biz'` liest **und** schreibt.

### `zane_feature_map_published`

**PUBLISHED-Ebene** der Feature-Map: die Ebene, die tatsächlich alle sehen (Public-Seite + alle eingeloggten User rendern Katalog + diese Overrides). Spaltenidentisch zu `zane_feature_map`. Wird nur durch `publish_feature_map()` (Draft → Published) geschrieben und von `discard_feature_map()` (Published → Draft) sowie dem Bake-Workflow gelesen/geleert. **Nicht** im User-Backup (`EXCLUDED`). Migration 0156.

- `card_id` (text, PK), `hidden` (boolean, default false), `is_custom` (boolean, default false)
- `cat`, `name`, `role` (CHECK null/`user`/`coach`/`both`), `summary`, `actions` (jsonb), `sort` (int): wie bei `zane_feature_map`
- `created_at`, `updated_at` (timestamptz, default `now()`)
- **RLS:** eine Policy `feature_map_published_admin_all` (`FOR ALL TO authenticated`), nur Admin liest direkt (für den Unpublished-Diff). Alle anderen lesen über `get_public_feature_map()`.
- **RPCs:** `publish_feature_map()` (admin, SECURITY DEFINER, atomar Draft → Published) · `discard_feature_map()` (admin, SECURITY DEFINER, Published → Draft) · `get_public_feature_map()` (SECURITY DEFINER, an `anon` **und** `authenticated` gegrantet: Login-freie/Alle-User-Leseansicht; hidden **Custom**-Karten werden zurückgehalten, hidden-Flags auf Katalog-Karten kommen mit). `anon`-Execute ist hier **gewollt** (einzige Feature-Map-Funktion mit anon-Zugriff); `publish_feature_map`/`discard_feature_map` müssen für `anon` `false` sein.

### `zane_push_subscriptions`

- `id` (text: endpoint URL), `user_id` (uuid), `endpoint` (text), `p256dh` (text: client EC public key, base64url), `auth` (text: auth secret, base64url), `created_at` (timestamptz)
- One row per device per user; managed by `subscribeWebPush`/`unsubscribeWebPush` in `store.js`; 410/404 responses auto-prune stale rows via the `web-push` Edge Function. Migration 0080.

### `zane_pushover_active`

- `id` (text), `nonce` (text)

### `zane_schedules`

- `id` (text), `user_id` (uuid), `name` (text), `days` (jsonb), `archived` (boolean, default false)
- `versions` (jsonb, default `[]`): array of `{ validFrom: 'YYYY-MM-DD', days: [...] }` sorted newest first; used for plan-change-from-date versioning
- `is_flex` (boolean, default false): **Flexible plan**, a cycle variant whose position advances only on a logged session/skip, never by calendar date; rest days can't push the plan forward. Migration 0090.
- `sessions_per_week` (int, nullable): weekly training-frequency goal, the adherence denominator for flex plans. Migration 0090.
- `mesocycle_weeks` (int, nullable): when set, this plan runs as a **bounded** mesocycle of that many weeks (RIR taper + auto-regulation feedback during training, ending in a deload/next-block offer). Truthy = this plan is a bounded block, which auto-starts (or re-starts) a `zane_meso_states` row with `weeks` set and suppresses the generic 8-week auto-deload nudge (that nudge is `mesocycle_weeks`-specific: it must NOT fire mid-block, but an unbounded autoregulate-only plan below has no block-end of its own to reach otherwise, so it still needs that generic nudge). Set/cleared in the plan editor's Auto-regulate section's nested "Fixed-length block" toggle, or the wizard's Meso step.
- `mesocycle_start_rir` (int, nullable, app-fallback 3): RIR target for meso week 1, range 0 bis 3.
- `mesocycle_end_rir` (int, nullable, app-fallback 0): RIR target for the final/peak meso week, range -3 bis 0. A **negative** end drives auto-prescribed lengthened partials during training: `|RIR|` partials per working set. `LB.mesoRirForWeek(week, weeks, startRir, endRir)` tapers linearly between the two. Migration 0133.
- `mesocycle_rir_enabled` (boolean, default true): **RIR taper on/off**. When false the meso still runs on volume (delta) auto-regulation, load progression and deload, but the weekly RIR target watermark and the negative-RIR lengthened-partials prescription are both suppressed. Resolved by `LB.mesoRirEnabled(sch)` (`!== false`, so only an explicit false disables; a bare null can't mean "off" because the app falls back to 3/0). Toggled by a **RIR taper** switch in the plan wizard's Meso step and the editor Options sheet (below the weeks stepper; it hides the Start/End RIR steppers + taper preview when off). `mesoRirVal` in the training screen becomes null when off, which gates both the watermark and `mesoPartials`. Migration 0140.
- `mesocycle_autoregulate` (boolean, default false): runs the **same** autoregulation engine as a mesocycle (volume/load feedback, `zane_meso_states`) with no bounded week count at all: no RIR taper, no completion/deload-offer flow, deltas/growth rotation/weight boosts accumulate indefinitely. Independent of `mesocycle_weeks`: `LB.mesoActive(sch)` (`!!(sch?.mesocycle_weeks || sch?.mesocycle_autoregulate)`) is the "is the engine on at all" check; `mesocycle_weeks` alone still means "and it's ALSO a bounded block" for RIR/completion/badge logic, which stays keyed on it directly. A mesoState created for an autoregulate-only plan has `weeks: null`. Toggled by the plan wizard's Meso step ("Autoregulate volume and load" option) or the editor Options sheet's Auto-regulate master toggle (the nested "Fixed-length block" toggle separately controls `mesocycle_weeks` on top). Migration 0160.
- `mesocycle_autoregulate_mode` (text, nullable): for an unbounded `mesocycle_autoregulate` plan, WHICH halves of the engine run. null/`'both'` = tune sets AND weight (default); `'load'` = tune weight only, set counts stay at the plan's authored value. Resolved by `LB.autoregLoadOnly(sch)` (true only for an unbounded autoregulate plan whose mode is `'load'`; a bounded mesocycle always regulates both, so a stray value there is ignored). In `'load'` mode all set deltas are frozen at the `commitContrib` choke point (same mechanism as the final-week freeze) and seeding never applies deltas (`LB.autoregLoadOnly` guard in `buildSessionEntries`), so leftover deltas from a prior "Volume + Load" run go inert without wiping the mesoState. The three feedback questions all still fire, but soreness is **repurposed**: instead of tuning set counts it acts as a recovery brake on weight, a muscle answered "still sore" is added to `mesoSoreBlockRef` and its exercises skip the weight boost that session (an extra load-only gate in `computeMesoGains`, alongside the usual joint/pump/volume + all-reps-hit gates). Only meaningful with `mesocycle_autoregulate` true; the bounded mesocycle ignores it. Set by the wizard's Autoregulate step and the editor Options sheet's Volume+Load / Load only picker. Migration 0161.
- `program_type` (text, nullable): when `'531'`, this schedule is a **Wendler 5/3/1** program (NULL = a normal history-based plan). Resolved by `LB.is531Plan(sch)`. Migration 0143.
- `program_data` (jsonb, nullable): 5/3/1 config plus per-lift state: `{ unit, includeDeload, mainLifts: { <exId>: { tm, kind, stall } }, tmHistory: { <exId>: [{ cycle, tm, reason }] }, bumpedCycle }`. `kind` is `squat|bench|deadlift|ohp` and drives the per-cycle TM bump (`LB.tmBump531`: upper +2.5kg/+5lb, lower +5kg/+10lb). During training a 531 main lift's working sets are `round(pct * tm)` off the current 4-week wave (`LB.fiveThreeOneSets`); the TM rises one step each cycle when the AMRAP top set hits its required minimum reps, holds on a miss, and resets to 90% after two missed cycles in a row (`stall`). `tmHistory` (reason `start|bump|reset`) feeds the progress chart; `bumpedCycle` gates the once-per-cycle prompt. The full shape is opaque to the DB (jsonb): the app owns it; see `docs/internals.md` (5/3/1). Assistance exercises on a 531 plan are ordinary items and keep normal Smart Progression. Migration 0143.
- `is_template` (boolean, default false): coach-only Plan-tab bucket flag, splits a coach's own schedules into "My Plans" (false) vs "Client Templates" (true) sub-tabs (`PlanScreen`). Pure client-side grouping, no behavior change to the plan itself; flipped by a toggle in `PlanViewerScreen`'s plan actions. Always false for non-coach users' plans. Migration 0164.

Verhalten:

- **Schedule-Objekte sind ein DB-Column-Passthrough:** die snake_case-Spalten `is_flex`/`sessions_per_week`/`mesocycle_weeks`/`mesocycle_start_rir`/`mesocycle_end_rir`/`mesocycle_rir_enabled`/`mesocycle_autoregulate`/`mesocycle_autoregulate_mode`/`program_type`/`program_data`/`days`/`versions`/`archived`/`is_template` liegen unverändert auf dem Store-Objekt; nur das local-only `mode`-Feld wird vor dem Upsert entfernt.
- `LB.isFlexPlan(sch)` = `sch.is_flex === true`; ein Flex-Plan ist nie ein Weekday-Plan.
- **Flex-Position = `cycleIndex`** (action-advanced; `todaysDay`/`nextDay` lesen ihn direkt und ignorieren `cycleStartDate`, das für Flex null bleibt). Streak-/Missed-Workout-Karten und die datumsbasierten Home-Strip-Marker sind für Flex ausgeblendet; der Home-Strip zeigt die Rotation (`D1…Dn`) mit hervorgehobenem Next-up-Tag.

### `zane_sessions`

- `id` (text), `user_id` (uuid), `schedule_id`, `day_id`, `day_name` (text), `date`, `started_at`, `ended` (timestamptz), `duration_minutes` (int), `feel` (text: easy|good|hard|very_hard|max)
- `completed_server_at` (timestamptz, nullable): **Server-Zeit** beim ersten Beenden der Session, gesetzt vom BEFORE-Trigger `zane_sessions_stamp_completion` (Migration 0242). Der Client kann sie **nicht** schreiben (der Trigger verwirft jeden mitgelieferten Wert) und sie ist nach dem Setzen unveränderlich, ein Wieder-Öffnen und erneutes Beenden erzeugt also keinen zweiten Qualifikationstag. Grund: `duration_minutes`, `ended` und `date` sind allesamt Client-Werte, die Founding-Member-Bedingung aus Migration 0240 war damit in einem einzigen Request fälschbar (fünf Sessions durchklicken, eine auf 150 Minuten setzen). `grant_lifetime_if_qualified` zählt seit 0242 **Server-Tage** über diese Spalte statt Client-Zeiten. Bestandszeilen wurden aus `ended` gebackfillt (sie entstanden, bevor es etwas zu gewinnen gab). Bewusst **nicht** im Backup: ein Restore würde sonst importierte Werte übernehmen. Nach einem Restore werden die Zeilen neu gestempelt, ein noch nicht qualifizierter Account sammelt seine Tage also neu, was als seltener Randfall gegenüber der Fälschungssicherheit akzeptiert ist.
- `entries` (jsonb): **legacy, wird nicht mehr geschrieben**. Seit Migration 0058 sind `zane_session_entries`/`zane_sets` die alleinige Quelle, alte Zeilen behalten ihren JSONB-Stand. Erst per separater Migration droppen, wenn alle Clients ≥ v2.085 sind (alte SW-Caches laden sonst noch den alten Boot-Code, dessen Select dann 400 würfe).
- `is_bonus` (boolean, default false): Extra-Session, die den Plan-Fortschritt beim Beenden nicht automatisch vorrückt. Gesetzt beim Start von Freestyle-Sessions sowie von Plan-Tagen, die nicht der heutige Tag sind oder wenn der heutige schon absolviert wurde (`screens-home.jsx`). Beim Beenden entscheidet der User (`shouldAdvance = isBonus ? advanceCycle : true` in `screens-train.jsx`); wählt er das Vorrücken, wird das Flag auf der gespeicherten Session wieder gelöscht. Store field `isBonus`, stripped from the row when false. Migration 0089.
- `is_freestyle` (boolean, default false): ad-hoc gestartete Session ohne Plan-Tag (leer oder „From template", startet immer auch mit `is_bonus`). Bei leerer Session öffnet sich direkt das Add-Exercise-Sheet; beim Beenden kann der User einen Namen vergeben (wird `day_name`). Store field `isFreestyle`, stripped from the row when false. Migration 0089.
- `is_deload` (boolean, default false): session logged during a deload week; excluded from progression seeds, regression detection and PR baselines so a light week never skews training. Store field `isDeload`, stripped from the row when false. Migration 0108.
- `is_cleanup` (boolean, default false): session logged during a cleanup week (see `zane_status_periods`). Deliberately **not** a copy of `is_deload`'s effects: a cleanup session stays in the progression seed chain (the following week builds back up from the reduced loads) and keeps `signal_weight='full'` so the overreach detector's own baseline moves down with it. What the flag excludes is the autoregulation grading: earn/cut and the rep-miss streak (`computeMesoGains`), PR/regression baselines (`bestE1rmForExercise`/`bestAssistLoad`/`bestTimeForExercise`, the `screens-lib.jsx` comparisons), stall detection (`detectStall`, which shares `detectOverreach`'s full-signal filter and would otherwise read the reduced e1RM as a stall) and post-hoc feedback editing (`isMesoSessionEditable`). Store field `isCleanup`, stripped from the row when false. Migration 0251.
- `meso_recap` (jsonb, nullable): durable per-session snapshot of the autoregulation / mesocycle feedback given and the weight/set bumps or cuts earned, so the session detail screen (`SessionDetailScreen`, „Feedback recap" button) can show it long after the fact and across devices. Written at session finish in `screens-train.jsx` (`buildMesoRecap`), previously only in device localStorage. Shape: `{ loadOnly, meso, week, weeks, unit, groups: [{ muscle, general: [{title,sub}], joint: [{title,sub}] }], gains: [{ name, weightDelta, setDelta }], raw }` (app-defined, not queried server-side; `week`/`weeks` are the mesocycle position and block length, both `null` outside a mesocycle plan). `raw` (present only when feedback was given) is the durable copy of the per-question answer records that powers post-hoc feedback editing: `{ answers: { soreness: {muscle: rec}, joint: {exId: rec}, volume: {muscle: rec} }, negOwner, frozen, dayId }`. Store field `mesoRecap`, only mapped onto the row when set. Part of the user backup (round-trips via `sessionToRow`). Migration 0166.
- `readiness` (text, nullable): the session-start one-tap self-report (Autoreg v2 P0, `screens-train.jsx` readiness sheet). Values `fresh` | `normal` | `rough` (null = not answered, treated as normal). Store field `readiness`, only mapped onto the row when set. Part of the user backup (round-trips via `sessionToRow`). Migration 0171.
- `signal_weight` (text, nullable): how much the session counts toward autoregulation learning (Autoreg v2 P0 signal-hygiene). Values `full` | `discounted` | `none` (null = full). `none` = deload (no earn, no cut, generalizes `is_deload`), `discounted` = rough day or re-entry ramp (may still EARN a weight boost, but never advances the rep-miss cut and never pulls MRV down), `full` = normal. Derived at session start from `readiness` (rough → discounted) and status mode (deload → none), consumed by `computeMesoGains` (`screens-train.jsx`). Store field `signalWeight`, only mapped onto the row when set. Part of the user backup (round-trips via `sessionToRow`). Migration 0171.
- `cycle_pos` (integer, nullable): a flex plan's absolute rotation position for this session (`store.cycleIndex` at session start), the field `doneSession`/`completedCyclePos` (`screens-home.jsx`) match on to know whether a given rotation slot was completed. Previously local-cache-only (never written to the DB), so it was lost on any full reload from Supabase (cleared cache after sign-out, a new device); the app falls back to a dayId + most-recent heuristic when it's `null`. Existing rows are `null` (no retroactive backfill). Store field `cyclePos`, only mapped onto the row when set. Migration 0176.
  - **Per-exercise feedback gates the weight in every mode (shape contract, read before touching the recap edit path or the meso gates).** Joint pain, weight-feel and pump are all asked **per exercise** and stored on that exercise's `joint[exId]` record: `answer` (joint), `weight` (`not_enough`/`just_right`/`pushed`/`too_much`), `pump` (`low`/`moderate`/`amazing`), plus `pumpLowApplied` (per-exId low-pump swap flag). The optional affinity answer also rides here: `affinity` (`love`/`ok`/`dislike`) and `affinityStreakBase` (the durable streak BEFORE this session, so a post-hoc edit re-derives the streak cleanly); the durable running value lives in the `zane_meso_states.affinity` column and gates nothing (see there). The weight-bump gate is `allHit && jointFine[exId] && pumpOk[exId] && weightOk[exId] && (load-only only) !soreBlock[muscle]`, all keyed by `exId`. The per-muscle `volume[muscle]` record now carries **only** the workload answer (`volume`, plus `exIds`, `contrib`) and drives **set deltas only** (Volume+Load / non-final Meso weeks); it no longer gates the weight, and there is no per-muscle step at all in load-only or the Meso final week (set deltas frozen). Soreness stays per muscle (`soreBlock`, holds the weight in load-only only). `volumeOk` was removed. **Backward-compat:** sessions finished (or backfilled) before pump/weight moved per-exercise carry pump on `volume[muscle].pump` and the weight/workload answer on `volume[muscle].volume`. `LB.mesoGateSetsFromAnswers` (store.js) and its live-capture mirror `mesoBoostSetsInitRef` (screens-train.jsx) resolve each gate **per exId**: use `joint[exId]`'s own `weight`/`pump` when present (an explicit answer is never overridden), otherwise fall back to the exId's muscle `volume[muscle].pump`/`.volume`. Never a union, so fully-new sessions ignore the fallback and old sessions are driven entirely by it. The weight fallback reads `volume[muscle].volume` with the session's own `loadOnly` semantics (an old load-only session stored a weight answer there; an old Volume+Load/Meso session stored the workload answer, which WAS the weight gate then). The **display/edit** builders `mesoRecapGroups` (screens-train.jsx) and `fbEditRows`/`fbGroupsForStore` (screens-lib.jsx) fold each joint row's `weight`/`pump` into its sub string when present, and render the per-muscle `volume[muscle].volume` as a "Workload" row.

### `zane_session_entries`

- `id` (text), `session_id` (text), `user_id` (uuid), `entry_idx` (int), `ex_id` (text), `name` (text), `planned_sets` (int), `planned_reps` (int), `planned_reps_per_set` (integer[]), `note` (text), `superset_group` (text)
- `planned_reps_max` (int, nullable): top of a **Range** reps target set in the plan/template item editor, e.g. "8-12"; `planned_reps` holds the floor. Store field `plannedRepsMax`. For a Range-mode exercise this also replaces Smart Progression's global `progression_range_top` add-on as the weight-increase ceiling; see `progressionSuggestion`/`progressionTargetForSet`. Migration 0134.
- `planned_progression_offset` (int, nullable): **per-exercise Smart Progression override**, settable in the Uniform/Per-Set sets-reps editor independent of Range mode. `null` = inherit the global `smart_progression`/`progression_range_top` setting, `0` = explicitly **off** for this exercise regardless of the global toggle, `N` = explicitly **on** with a ceiling of base reps + `N`. Store field `plannedProgressionOffset`. `0` is a meaningful, distinct value: always use `??`/`!= null` checks against it, never `||`. Resolved together with `planned_reps_max` by the shared `LB.progressionEnabled`/`LB.progressionCeilingFor` helpers in `store.js` (a Range item's `repsMax` always wins over an offset). Migration 0135.
- `planned_techniques` (text[], nullable): **per-set planned intensity techniques**, one slot per planned set (`null` where a set has no technique), chosen in the plan/template item editor so each set auto-arms its technique live during training instead of relying on the client to pick it. Each non-null element is a `zane_sets.technique` value (`'drop'` | `'myorep'` | `'myorep_match'` | `'amrap_variations'` | `'lengthened_partial'` | `'weighted_stretch'`). Same per-set shape as `planned_reps_per_set`; lets a coach prescribe e.g. Drop on set 1, Myo on set 2, or a technique on just the last two sets. Copied from the schedule day item / template at session start (like the other `planned_*` columns) so a later plan edit does not change an in-progress session; carried in the `zane_schedules.days`/`zane_workout_templates.exercises` JSONB as `plannedTechniques` passthrough. Store field `plannedTechniques`. Surfaced by `zane_entries_json`. Migrations 0157 (single-technique + scope, superseded) and 0158 (per-set array).

### `zane_sets`

- `id` (text), `session_id` (text), `entry_id` (text), `user_id` (uuid), `set_idx` (int), `kg` (numeric), `reps` (int), `reps_l` (int), `reps_r` (int), `done` (boolean), `skipped` (boolean), `warmup` (boolean), `updated_at` (timestamptz)
- `time_sec` (int, nullable): logged duration in seconds for a time-based set (`log_mode = 'time'`, e.g. HIIT or a max hold). Adds 0 to volume, counts as done once set. Synced through `sync_sets_batch` and surfaced by `zane_entries_json` (`timeSec`). Migration 0144.
- `added_kg` (numeric, nullable): Zusatzlast auf dem Gürtel bei einer `plus_load`-Übung (siehe `zane_exercises.bodyweight_mode`). **`kg` trägt weiterhin die Gesamtlast** (Körpergewicht + `added_kg`), damit e1RM, PR-Erkennung, Volumen und die komplette Autoregulation unverändert dieselbe Spalte mit derselben Bedeutung lesen; `added_kg` existiert nur, damit die UI die Zahl zeigen und wieder editieren kann, die der Nutzer tatsächlich getippt hat. `null` bei jeder anderen Satzart. Das Körpergewicht ist dadurch konstruktionsbedingt zum Logzeitpunkt eingefroren (`kg - added_kg`), ein späteres Wiegen schreibt also keinen alten PR um. Anzeige zentral über `LB.setLoadLabel(set)` (`'+10'` vs `'90'`), das am **Satz** ansetzt statt an der Übung, damit jeder Renderer formatieren kann ohne Lookup. Migration 0243. **Bug (behoben Migration 0245):** `sync_sets_batch` referenzierte die Spalte anfangs in keiner der drei Listen (unreferenzierte jsonb-Keys werden von PostgREST/der Funktion still ignoriert), jeder Sync über den normalen Pfad (alles außer dem einmaligen Boot-Import) verlor `added_kg` also auf `NULL`. `kg` blieb davon unberührt (Gesamtlast, kein Volume-/e1RM-Schaden), aber die getippte Zusatzlast war nach jedem Sync futsch.
- `technique` (text, nullable): intensity technique the set was logged with, one of `'drop'` | `'myorep'` | `'myorep_match'` | `'amrap_variations'` | `'lengthened_partial'` | `'weighted_stretch'`. Plain text, no DB CHECK. Migration 0115 seeded `'drop'`/`'rest_pause'`/`'myorep'`; the later techniques were added client-side and `'rest_pause'` is no longer written.
- `drops` (jsonb, nullable): for drop sets, `[{kg, reps}, ...]` ordered heaviest→lightest. `drops[0]` mirrors the top-level `kg`/`reps` so progression seeds use the first drop; only the first drop counts toward volume and doneSetCount. Migration 0115.
- `horn_loads` (jsonb, nullable, Migration 0254): Beladung je Dorn für eine Multi-Dorn-Übung (siehe `zane_exercises.horn_labels`), `[{label, kg}, ...]`. **Selbstbeschreibend statt positionell**, damit ein späteres Umbenennen oder Umsortieren der Dornliste die bereits geloggten Sätze nicht still falsch beschriftet. **`kg` trägt die schlichte Summe** und behält damit exakt seine Bedeutung für Volumen, e1RM, PR-Erkennung und die komplette Autoregulation, dieselbe Anordnung wie bei `added_kg`. Anzeige: `LB.setLoadLabel(set)` gibt weiterhin die Summe (die Satzzeile ist schmal), `LB.hornLoadLabel(set)` die Aufschlüsselung `"20 / 10 / 10"` für Detailansichten. `LB.sameHornLoad(a, b)` vergleicht zwei Sätze **nach Verteilungs-Form, nicht nach Kilos, und nach Label, nicht nach Position**: 20/20 und 40/0 summieren beide auf 40, sind aber verschiedene Arbeit (andere Hebel, anderer Kurvenverlauf), deshalb unterdrücken `LB.isImprovement`/`LB.isDecline` (Guard direkt in `store.js`, deckt Session-Detail, Recent-Trend, Home und beide Coach-Ansichten in einem ab) und die PR-Logik ihr Urteil. Verglichen wird bewusst die **Form** (Anteil je Dorn in Promille der Summe): 20/20 zu 30/30 ist dieselbe Maschineneinstellung mit mehr Gewicht und damit ganz normale Progression, ein Vergleich auf rohe Kilos hätte jede Laststeigerung als „nicht vergleichbar" gewertet und Progression an diesen Maschinen komplett unterdrückt. `buildSeedSets` trägt die Verteilung in die nächste Session vor und baut `kg` daraus neu auf. In allen drei Listen von `sync_sets_batch` referenziert (siehe die `added_kg`-Falle bei Migration 0245).

### `zane_coaching`

- `id` (text), `coach_id` (uuid), `client_id` (uuid), `status` (text: pending|active), `created_at` (timestamptz), `checkin_requested_at` (timestamptz, nullable), `checkin_enabled` (boolean, default true)
- `checkin_schema` (jsonb, nullable): coach-definiertes Formular-Schema; null = `CHECKIN_DEFAULT_SCHEMA`. Beim Einladen wird es aus dem `default_checkin_schema` des Coaches vorbefüllt (Migration 0150), damit Coach und Client dasselbe Formular sehen; `saveDefaultCheckinSchema` stempelt es zusätzlich auf bestehende Zeilen. Coach-Review und Client lösen bei null beide auf `CHECKIN_DEFAULT_SCHEMA` auf (nur Self-Coaching nutzt das eigene Default).
- `support_status` (text, nullable: 'open'|'in_progress'|'resolved'), `support_category` (text, nullable: 'feature_request'|'bug'|'question')
- `archived` (boolean, default false), `archived_at` (timestamptz, nullable)

Sonderfälle und RLS:

- **Support-Ticket** (Migrationen 0085/0086): eine Zeile mit id-Präfix `support_` zwischen User (`client_id`) und Admin (`coach_id`), die `support_status`/`support_category` trägt und die bestehende Coaching-Chat-/Realtime-Infrastruktur wiederverwendet (kein neuer Client-Code). Verwaltet über `open_support_chat`/`set_support_status`/`archive_support_ticket`/`delete_support_ticket`/`get_support_chats`/`get_archived_support_chats`/`get_user_support_chats`.
- **Self-Coaching:** eine Zeile mit `coach_id == client_id` (id-Präfix `self_`) bedeutet „be your own coach". Sie wird aus allen Coach-/Client-Listen herausgefiltert (`get_coach_info`, `get_coaching_clients`, `get_coach_clients_status`, `get_coach_checkin_status`) und ermöglicht das volle Coaching-Dashboard für die eigenen Daten.
- **RLS-Härtung (Migration 0125):** Die INSERT-Policy verlangt `status='pending' AND coach_id<>client_id` (ein Coach kann sich keine *aktive* Beziehung zu fremden Accounts mehr selbst anlegen), UPDATE-Policies haben `WITH CHECK`, und der Trigger `zane_coaching_guard_update` macht `coach_id`/`client_id` unveränderlich und erlaubt nur dem Client den Status-Wechsel (pending→active). **Migration 0213** ergänzt ihn um die Coach-Konfiguration: `checkin_enabled`, `checkin_schema`, `support_status`, `support_category` und `created_at` darf nur der Coach ändern (die Client-UPDATE-Policy kann per RLS nicht auf Spalten eingeschränkt werden), und die Trigger-Funktion selbst bekommt endlich ihr `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated` wie ihre beiden Geschwister. `find_user_by_email` ist nicht mehr direkt für `anon`/`authenticated` ausführbar (nur intern via `invite_client`).
- **Migration 0137 (Audit A2):** die INSERT-Policy „coach can invite" ist entfernt und der direkte `INSERT`-Grant auf `zane_coaching` für `anon`/`authenticated` entzogen. Coaching-Zeilen entstehen ausschließlich über die SECURITY-DEFINER-RPCs (`invite_client`/`enable_self_coaching`/`open_support_chat`/`admin_broadcast_message`), sodass niemand mehr per Direkt-POST beliebige Pending-Einladungen (Spam) anlegen kann.

### `zane_coaching_threads`

- `id` (text), `coaching_id` (text), `name` (text), `created_by` (uuid), `created_at` (timestamptz)

### `zane_coaching_notes`

- `id` (text), `coaching_id` (text), `author_id` (uuid), `thread_id` (text, nullable → references `zane_coaching_threads`), `type` (text: session|plan|general|change), `entity_id` (text, nullable), `entity_name` (text, nullable), `body` (text), `created_at` (timestamptz), `read_at` (timestamptz, nullable)
- `attachments` (jsonb, nullable): `[{ url, name, type }]` image attachments; uploaded to the public `chat-attachments` storage bucket; rendered as thumbnails in the ChatThread + support-ticket bubbles. Migration 0104.

### `zane_coaching_macros`

- `id` (text), `coaching_id` (text), `set_by` (uuid), `set_at` (timestamptz), `calories_training` (int), `protein_training` (int), `carbs_training` (int), `fat_training` (int), `calories_rest` (int), `protein_rest` (int), `carbs_rest` (int), `fat_rest` (int)
- **RLS** (Migration 0149): beide Policies (`Coach can manage macros`, `Client can read macros`) verlangen `status='active'` und `id NOT LIKE 'support_%'`. Kein Schreiben/Lesen auf pending Invites (Write-before-consent) oder Support-Threads. Self-Coaching läuft weiter (self-Zeilen sind aktiv, `coach_id = client_id`).

### `zane_checkins`

- `id` (text), `coaching_id` (text), `client_id` (uuid), `week_start` (date), `checked_in_at` (timestamptz)
- **RLS**: `checkins_client` (Client liest/schreibt eigene, `client_id = auth.uid()`), `checkins_coach_read` (Coach liest die seiner aktiven Clients; seit Migration 0149 mit `coach_id <> client_id` + `id NOT LIKE 'support_%'`, analog zu den übrigen Coach-Read-Policies). Self-Coaching liest die eigenen Check-ins über `checkins_client`.
- `responses` (jsonb, nullable): all field values keyed by field key, primary storage since Migration 0065
- `weight_today` (numeric), `weight_avg_last_week` (numeric), `off_plan_notes` (text), `hydration_ml` (int), `days_trained` (int), `performance_vs_last_week` (text: worse|same|improved), `steps` (int), `cardio_minutes` (int), `cardio_distance_m` (int), `cardio_pace_feeling` (int 1-6), `cardio_effort` (int 1-10), `goal_note` (text), `hunger` (int), `sleep_quality` (int), `life_stress` (int), `work_stress` (int), `tiredness` (int), `issues_notes` (text), `general_note` (text)
- `ai_opinion` (text, nullable), `ai_opinion_generated_at` (timestamptz, nullable): AI-generierte (Qwen als Primary, Claude als automatischer Fallback bei Nichterreichbarkeit, `callModelWithFallback` in `_shared/ai.ts`, kein Reasoning) Coach-artige Einschätzung dieses Check-ins, per Button in `CheckInCard` (`screens-coaching-tabs.jsx`) anstoßbar, für Client **und** Coach identisch sichtbar (dieselbe Komponente rendert beide Seiten unverändert). **Server-authored only**: geschrieben ausschließlich von der Edge Function `ai-checkin-opinion` (Service-Role-Upsert), nie vom `submitCheckin`-Upsert des Clients (der schreibt ohnehin nur die Felder, die er selbst aufbaut, diese beiden also nie mit). `ai_opinion_generated_at` ist zugleich das Once-per-Check-in-Gate. Autorisierung der Edge Function: ein Pre-Read gegen `zane_checkins` mit dem **Bearer-Token des Aufrufers** (nicht Service-Role) lässt die bestehende RLS (`checkins_client`/`checkins_coach_read`) selbst entscheiden, ob Client oder Coach lesen darf, nur der finale Schreibzugriff braucht Service-Role (der Coach hat unter `checkins_coach_read` nur Lesezugriff). Seit Migration 0226 ist „server-authored only" zusätzlich per BEFORE-UPDATE-Trigger (`zane_checkins_guard_ai_opinion`) durchgesetzt, analog zu `ai_summary` auf `zane_daily_logs`: `checkins_client` ist eine breite FOR-ALL-Policy ohne Spalten-Einschränkung, ohne den Trigger hätte der Client beide Spalten direkt per PostgREST-PATCH überschreiben können, besonders heikel, weil der Coach `ai_opinion` unverändert mitangezeigt bekommt. Quota über `bump_api_usage`/`zane_api_usage` (kind `'checkin_opinion'`), fail-open wie beim Label-Scanner. Migration 0225, Guard-Trigger Migration 0226.
- UNIQUE (coaching_id, week_start)

### `zane_glucose_logs`

- `id` (text), `user_id` (uuid), `date` (text, YYYY-MM-DD), `time` (text, HH:MM, lokale Uhrzeit der Messung), `value_mmol` (numeric: immer in mmol/L gespeichert; Anzeige-Einheit ist ein per-User-Setting), `context` (text: 'fasted'|'fed'|'other'), `note` (text, nullable), `created_at` (timestamptz)
- Store field: `store.glucoseLogs`. Mehrere Messungen pro Tag möglich. Wird direkt via Supabase aus der Glucose-Sektion des DailyLogScreen geschrieben (kein syncStore-Diff). Migration 0101.

### `zane_blood_pressure_logs`

- `id` (text), `user_id` (uuid), `date` (text, YYYY-MM-DD), `time` (text, HH:MM, lokale Uhrzeit der Messung), `systolic` (int, mmHg), `diastolic` (int, mmHg), `note` (text, nullable), `created_at` (timestamptz)
- Store field: `store.bloodPressureLogs`. Mehrere Messungen pro Tag möglich. Wird direkt via Supabase aus der Blood-Pressure-Sektion des DailyLogScreen geschrieben (kein syncStore-Diff), strukturell identisch zu `zane_glucose_logs`. Migration 0173.

### `zane_body_temp_logs`

- `id` (text), `user_id` (uuid), `date` (text, YYYY-MM-DD), `time` (text, HH:MM, lokale Uhrzeit der Messung), `value_c` (numeric: immer in Celsius gespeichert; Anzeige-Einheit ist ein per-User-Setting), `note` (text, nullable), `created_at` (timestamptz)
- Store field: `store.bodyTempLogs`. Mehrere Messungen pro Tag möglich. Wird direkt via Supabase aus der Body-Temperature-Sektion des DailyLogScreen geschrieben (kein syncStore-Diff), strukturell identisch zu `zane_glucose_logs`. Migration 0173.

### `zane_water_logs`

- `id` (text), `user_id` (uuid), `date` (text, YYYY-MM-DD), `time` (text, HH:MM, lokale Uhrzeit des Eintrags), `amount_ml` (int: immer in ml gespeichert, Anzeige via `UI.water*`-Helper), `name` (text, nullable: Getränkename), `category` (text, nullable: `null` = reines Wasser, `'other'` = benanntes Getränk, `'custom'` = freier Eintrag, `'summary'` = zusammengefasste Tageszeile, siehe unten), `breakdown` (jsonb, nullable: nur bei `category = 'summary'` gesetzt, `{ drinks: {name: count, ...}, milk: mlSumme }`), `created_at` (timestamptz)
- Store field: `store.waterLogs`. Mehrere Einträge pro Tag (ein Eintrag = ein geloggtes Getränk), Basis des Water-Trackers (`WaterScreen`). Als Store-Collection über den syncStore-Diff gesynct (wie `zane_cardio_logs`), inklusive Boot-Merge/Anti-Resurrection. Bei jeder Mutation schreibt der Client die Tagessumme aller Einträge zurück in `zane_daily_logs.water_ml`, damit Health-Water-Karte und Coaching-`hydration_ml` eine einzige Quelle behalten. Strukturell an `zane_glucose_logs` angelehnt. Migration 0180.
- **Collapse (Migration 0183):** stündlicher pg_cron-Job (`water-log-collapse`) ruft `collapse_water_logs()` (SECURITY DEFINER) auf. **Migration 0213**: 0183 hatte nur `REVOKE ... FROM PUBLIC`, was `authenticated` das EXECUTE nicht nimmt (dieselbe `ALTER DEFAULT PRIVILEGES`-Falle wie bei `bump_api_usage`, Migration 0208), jeder eingeloggte User konnte den tenant-übergreifenden Wartungsjob also selbst starten; jetzt zusätzlich explizit `REVOKE ... FROM authenticated` und `FROM anon`. Pro User und vergangenem lokalem Tag (via `tz_offset_minutes`; anders als die Reminder-Crons kennt `collapse_water_logs()` `time_zone` nicht, reine Offset-Rechnung) werden alle Einträge, sobald mehr als eine Zeile für diesen Tag existiert, zu **einer** Zeile zusammengefasst: `amount_ml` = Tagessumme, `time = '00:00'`, `name = null`, `category = 'summary'`, `breakdown` trägt die Getränke-Aufschlüsselung (Name → Anzahl, plus Milch-ml-Summe), damit `WaterStatsBody`s „Other drinks this period"-Breakdown auch für vergangene Zeiträume funktioniert. Grund: Uhrzeit-Granularität eines vergangenen Tages ist in der UI ohnehin nirgends einsehbar (nur „heute" zeigt Einzel-Einträge/Stundenchart), der Tagestotal steht ohnehin redundant in `zane_daily_logs.water_ml`. Ein Tag mit nur einem Eintrag bleibt unangetastet (schon „eine Zeile").
- RLS: eigene Zeilen + Coach-of-Client-Reads (inline-EXISTS wie glucose/bp/temp).

### `zane_foods`

Geteilter/globaler Referenz-Cache, **keine** Per-User-Daten (einzige Tabelle in diesem Schema mit diesem Charakter). Basis des Zane-eigenen Macro-Trackers (`FoodScreen`, Migration 0186).

- `id` (text, PK: `${source}:${source_id}`, z.B. `off:3017620422003`), `source` (text, CHECK `'off'|'usda'`), `source_id` (text), `name` (text), `brand` (text, nullable)
- `kcal_per_100g`/`protein_per_100g`/`carbs_per_100g`/`fat_per_100g`/`fiber_per_100g` (numeric, nullable), `serving_size_g` (numeric, nullable), `serving_label` (text, nullable)
- `sugar_per_100g`/`sat_fat_per_100g`/`sodium_mg_per_100g` (numeric, nullable, Migration 0204): Zucker, gesättigte Fettsäuren und Natrium je 100 g/ml. Beide Quellen liefern sie im selben Response (OFF: `sugars_100g`, `saturated-fat_100g`, `sodium_100g`; USDA: Nährstoffnummern 269, 606, 307), sie wurden bis 0204 nur nicht gemappt. **Natrium in Milligramm**, OFF meldet Gramm, die Edge Function rechnet um. `null` heißt "Quelle meldet den Wert nicht", nicht "null Gramm".
- `raw` (jsonb, nullable): normalisierter Snapshot der Upstream-Antwort (Migration 0204: wird seither auch tatsächlich geschrieben, davor war die Spalte immer `null`)
- `cached_at`/`created_at` (timestamptz)
- Wird **nicht** bei jedem Such-Treffer befüllt, sondern nur wenn ein User ein Suchergebnis tatsächlich zum Loggen auswählt (Edge Function `search-foods`, `action: 'select'`, Service-Role-Key, holt den Datensatz serverseitig nochmal per `source`/`source_id` nach statt Client-Werten zu vertrauen). `id` ist deterministisch, ein erneutes Auswählen upsert't statt zu duplizieren, die Datenbank wächst so organisch mit den tatsächlich genutzten Lebensmitteln.
- RLS: `SELECT` für `authenticated` (nicht-sensible Referenzdaten). Bewusst **kein** Insert/Update/Delete-Grant für `authenticated`/`anon`: einziger Writer ist die Edge Function per Service-Role-Key, RLS also für Client-Direktzugriff irrelevant, aber die fehlenden Policies verhindern, dass ein Client den Cache direkt mit erfundenen Nährwerten vergiften könnte.

### `zane_food_logs`

Pro-User-Log-Einträge des Macro-Trackers, **zum Schreibzeitpunkt denormalisiert** (wie `zane_water_logs`): ein späteres Refresh eines gecachten `zane_foods`-Eintrags darf einen bereits geloggten Eintrag nie rückwirkend verändern, gleiches Prinzip wie z.B. `planned_reps` auf Session-Entries.

- `id` (text), `user_id` (uuid), `date` (text, YYYY-MM-DD), `time` (text, HH:MM, lokale Uhrzeit des Eintrags)
- `food_id` (text, nullable, FK → `zane_foods.id` `ON DELETE SET NULL`: nur zur Nachverfolgung, nie für die Anzeige gelesen; `null` bei Custom Items **und** bei geloggten Rezepten)
- `food_name` (text: zum Schreibzeitpunkt kopiert), `brand` (text, nullable), `source` (text, nullable: `'off'|'usda'|'custom'|'recipe'|null`)
- `quantity_g` (numeric), `calories` (int: bei Datenbank-Foods der beim Cachen aus den Makros abgeleitete Wert `4P + 4C + 9F` (die Edge Function `search-foods` liest den gedruckten Energiewert der Quelle bewusst **nie**, siehe `zane_foods`), bei Custom Items der vom User eingegebene Wert), `protein`/`carbs`/`fat` (numeric), `fiber` (numeric, nullable)
- `sugar`/`sat_fat`/`sodium_mg` (numeric, nullable, Migration 0204): Zucker, gesättigte Fettsäuren, Natrium (in **Milligramm**) für die geloggte Menge, wie alles andere zum Schreibzeitpunkt kopiert. `null` bei allem, was vor 0204 geloggt wurde, und bei Foods, für die die Quelle den Wert nicht meldet: rückwirkend nicht rekonstruierbar (genau deshalb wandern die Werte auch auf Favoriten und Template-Slots, nicht nur in den Cache). Fließen bewusst **nicht** in `zane_daily_logs`: dort gibt es keine passenden Spalten und die Adherence rechnet weiterhin nur mit P/C/F.
- `recipe_items` (jsonb, nullable, Migration 0191): nur bei `source: 'recipe'` gesetzt, sonst `null`. Snapshot der Zutaten-Aufschlüsselung zum Logging-Zeitpunkt, bereits auf die geloggten Portionen skaliert, Shape wie bei `zane_food_recipes.items` (`{ foodId, brand, foodName, quantityG, calories, protein, carbs, fat, fiber }`, ohne `source`). `foodId`/`brand` seit dem Fix für die Shopping-List-Marken-Anzeige mit dabei (vorher fehlten beide, `applyBlockRecipe`/`confirmRecipeLog`/der Template-Slot-`draftBuilt` whitelisteten sie schlicht nicht raus, obwohl die Quelle sie längst hatte): Zeilen, die vor diesem Fix geloggt wurden, haben beide weiterhin `null`, keine rückwirkende Migration. Gleiches "zum Schreibzeitpunkt kopiert"-Prinzip wie der Rest der Zeile: eine spätere Bearbeitung des Quell-Rezepts darf einen schon geloggten Eintrag nie rückwirkend verändern (foodId/brand sind reine Identität, keine Messung, das Einfrieren betrifft nur die Makros). Treibt den Zutaten-Chevron in `FoodScreen`'s Timeline (Aufklappen zeigt die Liste, kein separater Server-Roundtrip).
- `recipe_id` (text, nullable, FK → `zane_food_recipes.id` `ON DELETE SET NULL`, Migration 0194): nur bei `source: 'recipe'` gesetzt. Stabiler Rückverweis auf das Quell-Rezept für `recipeEntryLiveRecipe` (Namens-Match als Fallback für ältere Einträge ohne diese Spalte, kollidiert sonst bei zwei gleichnamigen Rezepten).
- `logged_total_portions` (integer, nullable, Migration 0194): `recipe.portions` zum Logging-Zeitpunkt, nur bei `source: 'recipe'` gesetzt. Ein späteres Editieren eines Ganz-Batch-Eintrags skaliert gegen diesen Wert, nicht gegen das inzwischen geänderte `recipe.portions` (siehe `openEditRecipeEntry`).
- `logged_cooked_grams`/`logged_cooked_weight_g` (numeric, beide nullable, Migration 0228): Grams-Modus fürs Rezept-Loggen (Alternative zu Portionen, für Batch-Cooking: das fertige Gericht abwiegen statt in Portionen denken). Beide nur gesetzt, wenn im `recipeLogPrompt`-Sheet der Grams-Toggle aktiv war, `null` sonst (inklusive jedem Eintrag vor Migration 0228). `logged_cooked_grams` ist die getippte Gramm-Zahl, `logged_cooked_weight_g` das `zane_food_recipes.cooked_weight_g` zum Logging-Zeitpunkt eingefroren, exakt dasselbe "zum Logging-Zeitpunkt eingefroren"-Prinzip wie `logged_total_portions` (ein späteres Editieren des Rezept-Kochgewichts darf einen schon geloggten Eintrag nie rückwirkend umskalieren). Intern läuft alles weiter über `chosenPortions`/`totalPortions` (`fdEffectiveChosenPortions` in `screens-food.jsx` löst Gramm→Portionen auf), Grams ist rein eine alternative Eingabe-Einheit, keine zweite Skalierungslogik. Store fields `loggedCookedGrams`/`loggedCookedWeightG`.
- `logged_unit` (jsonb, nullable, Migration 0202): die Einheit, in der der Eintrag tatsächlich geloggt wurde, `{ label, grams }` (gleiches Shape wie ein Element von `zane_food_favorites.units`), oder `null` bei direkter Gramm-/kcal-Eingabe. Nur die EINE zum Loggen gewählte Einheit, nicht die volle Picker-Liste (die bleibt exklusiv auf `zane_food_favorites.units`). Genutzt vom Timeline-„Split in mehrere Mahlzeiten"-Sheet (`splitEntryUnit` in `screens-food.jsx`), damit eine Stückzahl-Eingabe (z.B. „2" bei einem 62g-Wrap-Favoriten) korrekt in Gramm zurückgerechnet wird. Bewusst **kein** Favoriten-Fallback mehr: `null` bedeutet immer "in Gramm/kcal geloggt" (auch bei Legacy-Zeilen ohne diese Spalte), ein Raten über einen passenden Favoriten würde sonst einen bewusst in Gramm geloggten Eintrag fälschlich als Stückzahl relabeln. Store field `loggedUnit`.
- `split_batch` (jsonb, nullable, Migration 0203): `{ id, removedEntries }` auf jedem Eintrag, den „Split in mehrere Mahlzeiten" erzeugt hat (`applySplit` in `screens-food.jsx`); `removedEntries` ist der exakte Snapshot der Vor-Split-Einträge. Bewusst redundant auf **jedem** Geschwister-Eintrag desselben Splits gespeichert (nicht nur auf einem): solange irgendeiner der Batch-Einträge noch existiert, egal ob die anderen inzwischen einzeln editiert/verschoben/gelöscht wurden, lässt sich der Split noch rückgängig machen. `undoSplitBatch` sucht alle Einträge mit passender `split_batch.id` (unabhängig vom aktuell angezeigten Tag), löscht sie und fügt `removedEntries` wieder ein. Macht die zuvor nur 6 Sekunden lange Toast-Undo-Möglichkeit dauerhaft verfügbar: erneutes Öffnen des Split-Sheets auf einem Split-Ergebnis zeigt „Undo Split" an. `null` bei direkt geloggten Einträgen. Store field `splitBatch`.
- `planned` (boolean, NOT NULL, Default false, Migration 0196): Plan Mode. `true` = geplanter Eintrag, sitzt in der Timeline, ist aber **noch nicht gegessen** und zählt daher **nicht** in die Tages-Makros / `zane_daily_logs` / Coaching / Adherence, bis er abgehakt wird (planned → logged, Häkchen-Button auf der Karte). `patchDaily` in `FoodScreen` summiert nur `!planned`-Einträge. Legacy-Zeilen ohne Flag → false → logged, Bestandsdaten unverändert. Store-Feld `planned`.
- `template_slot_id` (text, nullable, Migration 0196): Plan Mode, weicher Rückverweis (kein FK, bewusst) auf den Template-Slot, aus dem ein geplanter Eintrag materialisiert wurde, um denselben Slot pro Tag nicht doppelt anzulegen. Store-Feld `templateSlotId`.
- `created_at` (timestamptz)
- Store field: `store.foodLogs`. Mehrere Einträge pro Tag, Basis von `FoodScreen`. Als Store-Collection über den syncStore-Diff gesynct (wie `zane_water_logs`), inklusive Boot-Merge/Anti-Resurrection. Bei jeder Mutation schreibt der Client die Tagessumme aller Einträge zurück in `zane_daily_logs.protein`/`carbs`/`fat`/`calories`/`fiber` (analog zu `water_ml`), damit Health-Macro-Karten und Coaching-Targets eine einzige Quelle behalten; das bestehende manuelle Eingabeformular bleibt als Override verfügbar (gleiches Lock/Unlock-Muster wie beim Water-Feld). "Custom Item"-Einträge (kein Datenbanktreffer) tragen direkt vom User eingetippte Makros, `food_id: null`, `source: 'custom'`.
- **Label-Scanner:** Die Edge Function `scan-label` liest per Foto eine Nährwerttabelle aus und kann das über drei austauschbare Modelle tun, hält im Alltag aber **kein** Client-Toggle mehr: **Qwen läuft als Primary** (Alibaba Cloud Model Studio, OpenAI-kompatibel; braucht `QWEN_API_KEY`, sonst 503; optional `QWEN_URL` für Region/Anbieter und `QWEN_MODEL`, Default `qwen3.7-flash`), mit **Grok als automatischem Fallback** (xAI Grok Vision, OpenAI-kompatible API; braucht `XAI_API_KEY`, sonst 503; Modell über `XAI_MODEL`, Default `grok-4.3`) falls Qwen selbst nicht erreichbar ist (Netzwerkfehler, Non-2xx, fehlender Key). `callModelWithFallback` in `_shared/ai.ts` probiert Qwen, und nur bei `!ok` (nie bei einer inhaltlich leeren aber erfolgreichen Antwort wie „kein Label erkannt") folgt der eine Retry auf Grok; Claude (Anthropic Messages API; `ANTHROPIC_API_KEY`, Modell über `ANTHROPIC_MODEL`, Default `claude-haiku-4-5-20251001`) bleibt als dritter Tabellen-Eintrag nutzbar, ist aber nicht Teil der automatischen Kette. Grok war vor Qwen der langjährige Default, deshalb dieses Paar und nicht Claude als Fallback. Ein optionales `provider`-Feld im Request-Body kann weiterhin exakt einen der drei fest erzwingen (kein Fallback dann, reines Debug-/Curl-Werkzeug, kein Pfad mehr in der App selbst); ein unbekannter Wert wird ignoriert und die normale Primary+Fallback-Kette läuft. Bis zur Konsolidierung waren das drei separate Functions (`scan-label`/`scan-label-claude`/`scan-label-qwen`); jetzt hält `supabase/functions/_shared/ai.ts` die Anbieter-Tabelle, Prompt und Response-Shape existieren nur noch einmal und können nicht mehr auseinanderlaufen. Alle drei bekommen denselben Prompt, das Bild wird clientseitig verkleinert/komprimiert. Der Prompt liest bevorzugt die **Pro-100g/100ml-Spalte** aus. Der Client normalisiert das Ergebnis auf Pro-100g-Raten und öffnet die Mengen-Sheet: der User gibt seine Portion in Gramm ein, die Makros skalieren live, und die erkannten Pro-100g-P/C/F sind editierbar (Korrektur falls das Modell danebenliegt; Kalorien bleiben wie erkannt). Bestätigt loggt es als **Custom Item** (`food_id: null`, `source: 'custom'`). Reine Portionsangaben ohne Gramm-Gewicht fallen auf das manuelle Custom-Formular zurück. Ein gescanntes Label wandert bewusst **nie** in den geteilten `zane_foods`-Cache: es sind Per-User-Daten, keine geprüfte Referenz wie ein OFF-/USDA-Treffer.
- **Describe a meal:** gleiches Ein-Function-drei-Modelle-Prinzip wie der Label-Scanner, hier für freien Fließtext und/oder ein Foto der Mahlzeit (mindestens eines von beiden Pflicht), ebenfalls **ohne** Client-Toggle: **Qwen als Primary**, **Claude als automatischer Fallback** (`callModelWithFallback`, nur bei `!ok`, nicht bei einer inhaltlich leeren aber erfolgreichen Antwort wie „kein Essen erkannt"), weil Claude vor Qwen der langjährige Default war. Grok bleibt als dritter Tabellen-Eintrag nutzbar, ist aber nicht Teil der automatischen Kette. Ein optionales `provider`-Feld im Request-Body erzwingt weiterhin exakt einen der drei ohne Fallback, reines Debug-/Curl-Werkzeug, kein Pfad mehr in der App selbst; Secrets und Modell-Overrides je Anbieter wie beim Label-Scanner. Vorher drei Functions (`parse-meal`/`parse-meal-grok`/`parse-meal-qwen`), inzwischen eine mit der geteilten Anbieter-Tabelle aus `_shared/ai.ts`; der Prompt existiert dadurch nur einmal statt dreimal byte-identisch gehalten werden zu müssen. **Reasoning ist bei allen drei an und auf `REASONING_BUDGET = 1200` gedeckelt** (Haiku `budget_tokens`, Qwen `thinking_budget`, Grok `reasoning_effort: 'low'`, weil xAI keinen Token-Deckel kennt und `low` mit gemessenen 1224 Reasoning-Tokens genau der Referenzwert ist, aus dem die 1200 stammen); ungedeckeltes Qwen brauchte auf demselben Foto rund 2950 Reasoning-Tokens und 24,1s. Der Deckel begrenzt nur das Denken und kann das JSON nicht abschneiden, anders als `max_tokens` (8000). Alle drei teilen denselben Tages-Quota-`kind` `meal_parse` in `zane_api_usage` (Modellwechsel darf das Kontingent nicht vervielfachen). Der Prompt bricht die Beschreibung in einzelne Items auf, schätzt Menge in Gramm + Makros pro Item (großzügig bei unspezifiziertem Kochfett wie Braten/Backen). Explizite Größen-/Mengen-/Kalorienangaben in der Beschreibung ("ein 45cm Pizza", "whole", "family-size", "unterschätze das nicht", "locker über 1500 Kalorien") sind seit einem real aufgetretenen Fall (eine 45cm-Salami-Pizza kam mit nur 980 statt den geforderten >1500 kcal zurück) explizit als harte Vorgabe im Prompt verankert, nicht nur als Kontext. Reicht die qualitative Vorgabe allein nicht (Grok hat sie beim ersten Test befolgt, Claude Haiku trotz identischem Prompt weiterhin nicht), verlangt der Prompt zusätzlich einen expliziten Selbst-Check bei einer genannten Kalorien-Untergrenze: Summe aus `protein*4 + carbs*4 + fat*9` über alle Items vor der Antwort selbst nachrechnen und bei Unterschreitung Menge/Makros hochsetzen, bis die Summe die genannte Grenze erreicht. Ein rein **stiller** Selbst-Check half Claude Haiku aber weiterhin nicht (identisches Ergebnis auf denselben Retest, obwohl der Deploy nachweislich griff): der Prompt verlangte zeitgleich "Return ONLY a JSON object, no prose", der Rechenweg hatte also gar keinen Platz, wo er sichtbar hätte stattfinden können. Der Prompt erlaubt jetzt explizit sichtbares Rechnen als Klartext **vor** dem finalen JSON-Objekt (Selbst-Check-Absatz verlangt das ausdrücklich, klammerfrei, damit `extractJson`s `indexOf('{')`/`lastIndexOf('}')`-Grenzenfindung nicht durcheinanderkommt), `max_tokens` dafür schrittweise auf inzwischen 8000 angehoben, damit weder der Rechenweg noch die Reasoning-Tokens das JSON abschneiden. Kalorien kommen **nie** direkt vom Modell: die Edge Function verwirft ein evtl. vorhandenes `calories`-Feld und berechnet sie serverseitig aus `protein*4 + carbs*4 + fat*9` (`caloriesFromMacros`), dieselbe Regel wie bei jeder anderen externen Quelle in der App. Ein Em-Dash im Modell-Output wird serverseitig ersetzt (`stripEmDash`, matcht per Unicode-Escape U+2014, nicht das Literalzeichen, damit `check-emdash.cjs` an dieser Zeile nicht anschlägt). Die zurückgegebenen Items werden clientseitig wie ein manuell getippter Custom Item behandelt: erst eine Review-Liste, nichts landet vor dem expliziten "Add"/"Plan it"/"Log it" in `zane_food_logs`. **Foto-Ergänzung:** das Sheet kann zusätzlich ein Foto der Mahlzeit anhängen (`mealPhoto`-State in `screens-food.jsx`, client-seitig auf ~1568px verkleinert über denselben `fdDownscaleImage`-Helper wie der Label-Scanner, `ALLOWED_MIME`/`MAX_IMAGE_CHARS` von dort ebenfalls übernommen). Text und Foto sind unabhängig optional, nur `missing description or image` (400) wenn beide fehlen. Bei beidem zusammen behandelt der Prompt den Text als maßgeblich für alles, was das Foto allein nicht zeigen kann (echte Portionsgröße ohne Referenzobjekt im Bild, unter Sauce/Käse verborgene Zutaten, dass nur ein Teil eines größeren Gerichts abgebildet ist), dieselbe Priorität, die reiner Text für explizite Größen-/Mengen-/Kalorien-Signale im Prompt schon hatte. Läuft über denselben Request wie der Text-Pfad, kein zweiter Call und kein anderes Modell: bei Claude als zusätzlicher `image`-Content-Block in derselben Messages-API-Anfrage (Text zuerst, Bild danach, wie im Label-Scanner), bei den beiden OpenAI-kompatiblen Anbietern als `image_url`-Block in derselben Chat-Completion. Bleibt im selben `meal_parse`-Quota-`kind`, kein separates Kontingent fürs Foto. **Reiterieren:** Im Review-Screen (`FoodScreen`, Sheet „Review meal") bleiben Beschreibung und Foto nach der ersten Schätzung bewusst erhalten (`mealDescription`/`mealPhoto` werden nicht mehr am Ende von `handleDescribeMeal` gelöscht, sondern erst über `clearMealDraft` an den drei Stellen, an denen der Review-Flow wirklich endet: verworfener Discard in `requestCloseMealReview`, erfolgreicher Commit in `commitMealItems`, letztes Item entfernt in `removeMealItem`), damit ein „Reiterate"-Button dort den Text editierbar und erneut abschickbar macht. `handleReiterateMeal` schickt den (editierten) Text plus die aktuell angezeigten `mealItems` als `previousItems` (`{ name, quantityG, protein, carbs, fat, fiber, sugar, satFat, sodiumMg }`, ohne Kalorien, wie immer serverseitig abgeleitet) an dieselbe Edge Function, kein separater Endpoint, kein zusätzliches Kontingent. Der Prompt bekommt dafür einen zusätzlichen Absatz, der die vorherige Schätzung als JSON-Block vor die neue Nutzerangabe stellt und anweist, nur das anzupassen was die Korrektur wirklich verlangt statt komplett neu zu schätzen (Prompt-Anweisung, keine harte Garantie, das Modell kann trotzdem mal eine unbeteiligte Zahl mitverändern). Antwort-Contract bleibt exakt `{ items: [...] }`, dieselbe clientseitige Mapping-Funktion (`mapParsedMealItems`, gemeinsam für ersten Versuch und jedes Reiterate) baut daraus frische `mealItems`-Zeilen mit neuen `tempId`s: ein Reiterate ersetzt die ganze Liste, patcht nicht chirurgisch einzelne Felder.
- RLS: eigene Zeilen + Coach-of-Client-Reads (inline-EXISTS wie water/glucose/bp/temp). Migration 0186.

### `zane_meal_reminder_deliveries`

Service-role-only Versandstatus für den Plan-Mode-Meal-Reminder (Migration 0259). `user_id` (uuid, FK auf `auth.users.id` mit Cascade), `food_log_id` (text, die ID des geplanten `zane_food_logs`-Eintrags), `last_attempt_at` und `delivered_at` (timestamptz, nullable) sowie `created_at` (timestamptz). Pro Nutzer und Food-Log merkt das Zustandsbuch, wann der letzte Zustellversuch gestartet wurde und ob der Provider den Handoff angenommen hat. Fehlgeschlagene Versuche bleiben dadurch bis zu einem lokalen Tag retrybar; erfolgreich zugestellte Mahlzeiten werden bei den nächsten Cron-Läufen übersprungen. Die Tabelle enthält keinen User-Inhalt und ist deshalb nicht Teil des Backups und hat keine Client-RLS-Policies.

### `zane_food_favorites`

User-markierte Lieblings-Lebensmittel im Food Tracker ("Quick Add"), gleiches Shape wie eine `zane_food_logs`-Zeile (minus `date`/`time`): ein erneutes Hinzufügen leitet daraus lokal eine Pro-100g-Rate ab (`rate = wert / quantity_g * 100`), genau wie es der Client für den Recent-Strip ohnehin schon tut, ein Favorit ist also weiter auf jede beliebige Menge skalierbar, nicht auf die Menge fixiert mit der er favorisiert wurde.

- `id` (text), `user_id` (uuid), `food_id` (text, nullable, FK → `zane_foods.id` `ON DELETE SET NULL`: `null` bei favorisierten Custom Items), `food_name` (text), `brand` (text, nullable), `source` (text, nullable: `'off'|'usda'|'custom'|null`)
- `quantity_g` (numeric), `calories` (int), `protein`/`carbs`/`fat` (numeric), `fiber` (numeric, nullable), `created_at` (timestamptz)
- `sugar`/`sat_fat`/`sodium_mg` (numeric, nullable, Migration 0204): wie auf `zane_food_logs`, bei der favorisierten Menge. Werden beim erneuten Hinzufügen genauso über die Pro-100g-Rate skaliert wie die Makros, `null` bleibt `null`.
- `units` (jsonb, Default `[]`): optionale Liste von Stück-/Packungsgrößen, je `{ label, grams }` (z.B. `{ label: 'Pc', grams: 62 }`), mehrere pro Favorit möglich. Beim erneuten Hinzufügen bietet die Mengen-Sheet einen Grams/Unit-Picker: bei gewählter Einheit ist das Eingabefeld eine Stückzahl, `quantity_g` wird daraus als `count * grams` abgeleitet. Editierbar im Favorites-Tab über den Stift-Button (Liste hinzufügen/entfernen). Migration 0188 führte ursprünglich ein einzelnes `unit_label`/`unit_g`-Paar ein, Migration 0189 ersetzte es durch dieses Array (bestehende Werte automatisch übernommen) und droppte die beiden alten Spalten.
- Store field: `store.foodFavorites`. Eigene, einfache User-Collection (kein Health-Tab-Live-Polling, kein Coach-Read), als Store-Collection über den syncStore-Diff gesynct + Boot-Merge/Anti-Resurrection, gleiches Muster wie `zane_workout_templates`.
- RLS: nur eigene Zeilen (`FOR ALL USING/WITH CHECK (select auth.uid()) = user_id`), kein Coach-Zugriff. Migration 0187, `auth.uid()`-Wrapping (initPlan-Caching) Migration 0192.

### `zane_food_recipes`

Eine benannte Liste von Zutaten, die der User zusammen loggt (z.B. "Breakfast bowl"). `items` ist ein jsonb-Snapshot (kein Kind-Table), gleiches "strukturierter Inhalt als jsonb"-Muster wie z.B. `zane_schedules.days`/`zane_workout_templates.exercises`. Jedes Item ist geformt wie eine `zane_food_logs`-Zeile minus `date`/`time`, plus ein optionales `note` (`{ foodId, foodName, brand, source, quantityG, calories, protein, carbs, fat, fiber, note }`); Kalorien werden bei jeder Nutzung live aus den gespeicherten Makros neu berechnet (`recipeItemsCalories` in `screens-food.jsx`), nicht aus dem gespeicherten `calories`-Feld gelesen. `foodName` ist ein reiner Anzeige-Snapshot, keine Live-Referenz auf `zane_foods` (auch mit gesetztem `foodId`), und im Rezept-Editor per Zutat umbenennbar (`editName`-State im "Edit ingredient"-Sheet, `RecipeEditorScreen`): rein kosmetisch, ändert nie `foodId` oder die Makros, reist aber unverändert in den Rezept-Share-Snapshot mit (Zutaten mit unhandlichen Rohnamen aus der Produktdatenbank, z.B. "Haltbare Leichtmilch", lassen sich so vor dem Teilen in etwas Lesbares umbenennen). Anders als `zane_food_shopping_prefs.name_override` gibt es dafür **kein** separates Override-Feld, `foodName` selbst wird überschrieben, deshalb müssen Cooking Mode/Share/das geloggte Snapshot keine Resolve-Logik kennen. Das optionale `originalFoodName` ist einzig der Rückweg: beim Hinzufügen (`addItems`), beim Übernehmen eines geteilten Rezepts (`adopt` in `RecipeShareSheet`) oder, für Items von vor diesem Feature, beim nächsten Öffnen im Rezept-Editor (Backfill im `useEffectFd` neben dem bestehenden id-Backfill, `RecipeEditorScreen`) auf den jeweils aktuellen `foodName` gesetzt, danach nie wieder überschrieben (`i.originalFoodName || i.foodName`, greift also nur beim ersten, noch fehlenden Wert). Gelesen im Edit-Sheet als Platzhalter/"Original: …"-Hinweis sowie als Fallback beim Leeren des Namensfelds. Für ein Item, das schon vor diesem Feature umbenannt wurde, ist der damalige (Roh-)Name unwiederbringlich weg, der Backfill kennt nur den Namen, der zum Zeitpunkt des ersten Öffnens danach gerade aktuell war, kein Bug, sondern die Grenze dessen was rekonstruierbar ist. `note` (string, nullable) ist eine permanente, kurze Zubereitungs-Notiz je Zutat ("dice small", "add right at the end"): editierbar sowohl im Rezept-Editor als auch live im Cooking Mode (`CookingModeScreen`), dort prominent auf der jeweiligen Zutaten-Karte gezeigt. Kein Migration nötig gewesen (flexibles jsonb ohne festes Schema), reist über den Rezept-Share-Snapshot (`zane_recipe_shares.recipe`) mit. Bewusst **nicht** Teil des zum Logging-Zeitpunkt eingefrorenen `recipe_items`/`recipeItems`-Snapshots (siehe oben): die Notiz ist Zubereitungs-, keine Log-Information. Optional dazu `stepId` (opaque String) und `stepLabel` (string, Instruktions-Text): **Cooking Steps** bündeln aufeinanderfolgende Zutaten zu einem nummerierten Arbeitsschritt mit gemeinsamer Anweisung ("schneiden und in einen Behälter geben"), dasselbe Gruppen-Muster wie `supersetGroup` im Trainings-Bereich. Ein Schritt ist ein maximaler Run aufeinanderfolgender Items mit gleicher `stepId` (rein Adjazenz-basiert, zwei getrennte Runs mit derselben Id sind zwei Schritte); `stepLabel` liegt invariant nur auf dem ersten Item des Runs. Der Editor markiert Zeilen per Schritt-Anfang-Toggle (`fdToggleStepStart`: ungruppiert → die markierte Zeile gründet die Gruppe mit nur dieser Zeile, weitere Zutaten werden per Drag hineingezogen, mid-run → Split mit frischer Id, Kopf → Toggle aus). `fdReorderWithSteps` setzt die Drag-Semantik um: eine ungruppierte Zeile, die direkt neben einem Run abgelegt wird (vor dem ersten, hinter dem letzten oder zwischen zwei Mitgliedern), tritt dem Schritt bei; ein Mitglied, das aus dem Run herausgezogen wird (kein gleich-Id-Nachbar mehr), wird wieder ungruppiert, außer es trägt die Schritt-Anweisung (dann bleibt es ein eigener 1-Mitglied-Schritt). `fdNormalizeSteps`/`fdTransferStepLabel` in `screens-food.jsx` halten die Invarianten (Label wandert beim Wegziehen/-löschen des Kopfs zum nächsten Mitglied; ein 1-Mitglied-Run bleibt immer ein legitimer Schritt, explizit getoggte Schritte werden nie still entfernt; aber nie zwei Runs mit derselben Id: `fdNormalizeSteps` remappt spätere Vorkommen einer Id auf frische uids) und trimmen leere Labels beim Speichern. Die Badges (Editor, Cooking Mode, Poster) zeigen bei Schritten die Schritt-Ordnungszahl (`fdStepOrdinal`, ungruppierte Zeilen zählen als eigene Nummer), der Cooking Mode navigiert schritt-weise (Chip pro Schritt, Step-Zähler, Schritt-Karte mit Instruktion und Mitgliedernamen). Schritte reisen über den Share-Snapshot (adopt-Whitelist in `RecipeShareSheet`) und werden bei Duplikat/Explode auf frische Ids remappt; `duplicateRecipe` (gidMap-Muster) und `confirmExplodeRecipe` (Sub-Rezept-Schritte überleben die Explosion als eigene Schritte, nie mit Parent-Schritten verschmolzen). Bewusst **nicht** im frozen `recipeItems`-Snapshot und in `applyBlockRecipe`/Template-Slots: gleiche Begründung wie `note`, Zubereitungs- statt Log-Information. Optional dazu `loggedUnit` (`{ label, grams }`, gleiches Shape wie `zane_food_logs.logged_unit`): die exakte Einheit, in der die Zutat beim Picken gewählt wurde (z.B. `{ label: 'pcs', grams: 62 }` bei einem 62g-Wrap, von dem der User 4 Stück genommen hat). Gesetzt beim Stagen im `FdIngredientPicker` (`confirmStageItem`, nur wenn im Mengen-Sheet eine Einheit gewählt war, sonst `null` wie bei einer reinen Gramm-Auswahl). Die Anzeige in Rezept-Editor, Cooking Mode, Poster und Share-Preview leitet daraus live den Stückzahl-Suffix ab (`fdUnitCountLabel` in `screens-food.jsx`: `quantityG / grams`, auf eine Nachkommastelle gerundet), also „248g (4 pcs)" statt nacktem Gramm; beim Rescalen im Edit-Sheet oder beim Explode rechnet sich der Wert neu, die Einheit selbst wird nie skaliert. Reist über die adopt-Whitelist (validiert) und `confirmExplodeRecipe` mit; und wird wie `foodId`/`brand` in den frozen `recipeItems`-Snapshot eingefroren (pure Identität, nie skaliert): die Timeline-Expanded-View eines geloggten Rezepts zeigt daraus `248g (4 pcs)` (`fdUnitCountLabel`), und das Per-Zutat-Edit-Sheet des Log-Eintrags sowie das Edit-Ingredient-Sheet im Rezept-Editor bieten einen Grams/{label}-Toggle, der die Menge wieder in Stücken eingeben lässt (`count × grams`, Gramm bleibt der kanonische Wert). Ältere Einträge ohne das Feld bleiben nackte Gramm. `items` sind die ganze Batch wie gekocht; `portions` sagt, in wie viele Portionen sie sich aufteilt. Hinzufügen zum Log fragt immer per Portionen-Stepper (Halbe-Portion-Schritte, kein Deckel nach oben) wie viele Portionen geloggt werden, auch bei `portions === 1`: „1 Kuchen" heißt nicht, dass nur der ganze Kuchen loggbar ist. Schreibt **eine** `zane_food_logs`-Zeile mit `source: 'recipe'`, `food_id: null`, Makros auf `(Batch-Summe / portions) * gewählte Portionen` skaliert. Im Rezept-Editor kann ein anderes eigenes Rezept als Zutat hinzugefügt werden (Recipes-Tab im `FdIngredientPicker`, nur dort: der Picker bekommt `showRecipes` nur am `RecipeEditorScreen`-Aufrufer): das Unter-Rezept wird dabei in seine Zutaten **aufgeschachtelt** (`fdExplodeRecipeItems` in `screens-food.jsx`), jede Zutat linear skaliert (gleiche Faktor-Mathematik wie das Edit-Sheet: `calories` gerundet, Makros auf eine Nachkommastelle, `null`-Felder bleiben `null`) mit Faktor `gewählte Portionen / portions` bzw. `gewählte Gramm / cooked_weight_g`, dann wie normale gepickte Items gehandhabt (staged → `addItems`). Keine Live-Referenz: ein Snapshot zum Einfüge-Zeitpunkt, spätere Änderungen am Unter-Rezept wirken nicht nach. Das aktuell editierte Rezept ist ausgeschlossen (`excludeRecipeId`), ein Rezept kann sich nicht selbst enthalten; ein Item mit `source: 'recipe'` + `recipeId` wird eine Ebene tiefer rekursiv aufgeschachtelt, skaliert relativ zu den Roh-Gramm des Unter-Rezepts, mit pfad-basiertem Zyklus-Guard (defensiv: aktuell setzt kein Writer von `zane_food_recipes.items` eine `recipeId`, der Block-Fold kopiert nur `source`, solche Items fallen auf den skalierten Blatt-Pfad). Explodierte Zutaten behalten `foodId`/`brand`/`source`/`note` und ihr `originalFoodName` (falls vorhanden), `addItems` überschreibt `originalFoodName` seitdem nur noch mit `item.originalFoodName || item.foodName`. `calories` wird beim Explode nie aus dem gespeicherten Feld skaliert, sondern aus den skalierten Makros mit dem aktuellen Net-Carbs-Setting neu berechnet (gleiche Semantik wie alle anderen kcal-Oberflächen: `fdRecipeItemsCalories`/`LB.caloriesFromMacros`), und eine gewichtstragende Zutat floort beim Runden auf 1g, damit keine un-rescalbare 0g-Zeile entsteht (`saveEditItem` verlangt `quantityG > 0`).

- `id` (text), `user_id` (uuid), `name` (text), `items` (jsonb, Default `[]`), `portions` (integer, Default `1`), `created_at`/`updated_at` (timestamptz)
- `cooked_weight_g` (numeric, nullable, Migration 0228): optionales Gewicht des fertigen (gekochten) Gerichts in Gramm, frei getippt im `RecipeEditorScreen`, **nie** aus der Zutaten-Summe abgeleitet (Kochen verändert das Gewicht durch Wasserverlust/-aufnahme, genau der Punkt des Felds). Gesetzt, schaltet das Feature frei, das Rezept beim Loggen (direkt oder als Meal-Template-Slot) per Gramm statt Portionen zu wählen (`recipeLogPrompt`-Sheet bzw. `FoodTemplateScreen`s Slot-Sheet, jeweils ein Portions/Grams-Toggle). `null` heißt: nur der bestehende Portionen-Weg verfügbar. Store field `cookedWeightG`.
- Store field: `store.foodRecipes`. Gleiches Sync-/Merge-Muster wie `zane_food_favorites`.
- RLS: eigene Zeilen + Coach-of-Client (SELECT/INSERT/UPDATE/DELETE via `zane_is_coach_of`, Migration 0200, damit `pushMealPlanToClient` beim Push-Kopieren eines Rezepts in den Client-Account schreiben und gegen dessen bestehende Rezepte deduplizieren kann). Migration 0187, `portions` Migration 0190, `auth.uid()`-Wrapping (initPlan-Caching) Migration 0192.

### `zane_food_template_slots`

Plan-Mode-Meal-Template (Migration 0197): wiederkehrende „Fixum"-Slots, die den Tagesplan automatisch befüllen. Ein Slot trägt denselben denormalisierten Food-/Rezept-Snapshot wie eine `zane_food_logs`-Zeile (damit die Materialisierung eines geplanten Eintrags kein Re-Fetch braucht) plus eine feste Stunde und einen Tag-Typ-Filter. Beim Öffnen des heutigen Food-Tags legt `FoodScreen` je passendem Slot (Tag-Typ) einen `planned`-Eintrag an der Slot-Stunde an (einmal pro Tag, geräteübergreifend markiert über `zane_food_template_days`, Dedup über `zane_food_logs.template_slot_id`). Verwaltung in `FoodTemplateScreen` (nur bei Plan Mode). Slot-Foods kommen aus den bestehenden Favorites/Recipes.

- `id` (text), `user_id` (uuid)
- `food_id` (text, nullable), `food_name` (text), `brand` (text, nullable), `source` (text, nullable: `'off'|'usda'|'custom'|'recipe'|null`)
- `quantity_g` (numeric), `calories` (integer), `protein`/`carbs`/`fat` (numeric), `fiber` (numeric, nullable)
- `sugar`/`sat_fat`/`sodium_mg` (numeric, nullable, Migration 0204): wie auf `zane_food_logs`, für die Slot-Menge; wandern unverändert in den materialisierten `planned`-Eintrag.
- `recipe_items` (jsonb, nullable): Zutaten-Snapshot bei `source: 'recipe'`, sonst null
- `recipe_id` (text, nullable): weicher Verweis auf das Quell-Rezept (kein FK: ein gelöschtes Rezept darf den Slot nicht mitlöschen), nur bei Rezept-Slots
- `logged_total_portions` (integer, nullable): Rezept-Batch-Total zum Slot-Erstellungszeitpunkt, nur bei Rezept-Slots
- `logged_cooked_grams`/`logged_cooked_weight_g` (numeric, beide nullable, Migration 0228): Grams-Modus-Pendant zu `logged_total_portions`, gleiche Semantik wie auf `zane_food_logs` (siehe dort), nur zum Slot-Erstellungszeitpunkt statt zum Log-Zeitpunkt eingefroren. Wandert unverändert in den materialisierten `planned`-Eintrag (`fdMaterializeSlotEntry`), damit dessen eigenes spätere Editieren korrekt wieder im Grams-Modus aufgeht.
- `hour` (integer, NOT NULL, Default 12): 0-23, die feste Zeit, an der der materialisierte `planned`-Eintrag landet
- `day_type` (text, NOT NULL, Default `'any'`): `'any' | 'training' | 'rest'` (via `LB.isTrainingDayForDate`)
- `sort_idx` (integer, NOT NULL, Default 0), `created_at` (timestamptz)
- `meal_plan_id` (text, nullable, Migration 0199): welchem `zane_food_meal_plans`-Plan der Slot gehört (weicher Verweis, kein FK: Slot-Cleanup ist clientseitig, vermeidet Cascade-Races mit dem Sync-Diff). Store-Feld `mealPlanId`.
- Store field: `store.foodTemplateSlots`. Gleiches Collection-Sync-/Boot-Merge-Muster wie `zane_food_recipes`.
- RLS: eigene Zeilen + Coach-of-Client (SELECT/INSERT/UPDATE/DELETE via `zane_is_coach_of`, Migration 0199, damit ein Coach beim Push einen Plan samt Slots in den Client-Account schreibt). Migration 0197, Coach-Policies + `meal_plan_id` Migration 0199.

### `zane_food_meal_plans`

Plan-Mode-Meal-Plans (Migration 0199): benannte Container (Cut/Bulk), spiegelt `zane_schedules`. Ein User hat mehrere, genau **einer aktiv** (`zane_user_settings.active_meal_template_id`, Skalar-Zeiger wie `active_schedule_id`, nie ein Flag pro Zeile). Nur die Slots des aktiven Plans befüllen den Tag automatisch. `is_template` ist das Coach-Bucket-Flag (My Plans / Client Templates, wie bei Schedules); `coach_id` attributiert einen coach-authorten/gepushten Plan. Migration backfillt bestehende flache Slots in einen Default-Plan „My meals" je User.

- `id` (text, PK, `LB.uid()`, Default-Plan aus Migration deterministisch `mp_<user_id>`), `user_id` (uuid)
- `name` (text), `archived` (boolean, NOT NULL, Default false), `is_template` (boolean, NOT NULL, Default false)
- `coach_id` (uuid, nullable): Coach, der den Plan authort/gepusht hat, sonst null
- `created_at`/`updated_at` (timestamptz)
- Store field: `store.foodMealPlans` (`{ id, name, archived, isTemplate, coachId, createdAt, updatedAt }`). Aktiv-Zeiger `store.activeMealTemplateId` (Setting, coupled-Boot-Merge wie `activeScheduleId`). Verwaltung in `FoodTemplateScreen` (Plan-Liste + Slots des gewählten Plans).
- RLS: eigene Zeilen + Coach-of-Client (SELECT/INSERT/UPDATE/DELETE via `zane_is_coach_of`, wie `zane_schedules`). Migration 0199.

### `zane_food_template_days`

Plan-Mode-Auto-Fill-Marker (Migration 0198): eine Zeile je (User, aktivem Meal-Plan, Tag), für den das Meal-Template bereits automatisch materialisiert wurde, **geräteübergreifend** (ersetzt einen früheren Per-Gerät-localStorage-Flag). Die deterministische ID ist `ft_<user_id>_<meal_plan_id>_<date>`, damit ein Planwechsel am selben Tag nicht den neuen Plan mit einem alten Marker unterdrückt. Verhindert, dass ein gelöschter geplanter Eintrag beim erneuten Öffnen auf irgendeinem Gerät wieder auftaucht. Läuft nicht nur für heute, sondern für ein rollierendes Fenster von `FD_PLAN_LOOKAHEAD_DAYS` (7) Tagen ab heute (`FoodScreen`, `screens-food.jsx`), damit z.B. morgen schon geplante Einträge zeigt, ohne dass der Tag erst geöffnet werden muss. Der „Apply to today"-Button (`FoodTemplateScreen`) bleibt auf heute beschränkt und ist die manuelle Escape-Hatch, um die Fixums nach bewusstem Leeren zurückzuholen.

- `id` (text, PK, deterministisch `ft_<user_id>_<meal_plan_id>_<date>`, damit zwei Geräte denselben Plan/Tag idempotent upserten), `user_id` (uuid), `date` (text, YYYY-MM-DD), `created_at` (timestamptz)
- Store field: `store.foodTemplateDays`. Synchronisiert (Boot-Load auf jüngere Tage gefenstert, syncStore-Diff, Boot-Merge), aber **nicht im Backup** (abgeleiteter Geräte-/Sync-Status, EXCLUDED in `tools/check-backup-coverage.cjs`).
- RLS: nur eigene Zeilen, kein Coach-Zugriff. Migration 0198.

### `zane_food_shopping_prefs`

Per-Food-Präferenzen der Shopping List (Migration 0215/0216/0217/0227/0231/0232), geräteübergreifend synchronisiert (löste die rein lokalen localStorage-Keys `logbook-shopping-name-overrides`/`logbook-shopping-exclusions` ab). Eine Zeile je (User, `shopping_key`) fasst alles, was ein User an einem Food für die Shopping List angepasst hat: Anzeigename-Override, Ausschluss-Flag, temporärer Ausschluss, Packungsgröße, Bestands-Tracking. Alle Felder außer `food_name`/`brand`/`shopping_key` sind unabhängig voneinander optional; sind sie alle leer/false/null (ein aktiver `excluded_until`-Wert zählt als gesetzt, ein abgelaufener nicht, siehe unten), hat die Zeile keinen Existenzgrund mehr, der Client löscht sie dann statt eine No-Op-Zeile stehen zu lassen (`fdSetShoppingPref` in `screens-food.jsx`).

- `id` (text, PK, `LB.uid()`), `user_id` (uuid), `food_id` (text, nullable, FK → `zane_foods.id` `ON DELETE CASCADE`, Migration 0227 hat NOT NULL entfernt)
- `shopping_key` (text, NOT NULL, Migration 0227): Identitäts-/Unique-Spalte der Tabelle, hat `food_id` in dieser Rolle abgelöst. Spiegelt exakt das Format von `fdShoppingKey(foodId, foodName)` im Client (`screens-food.jsx`), `'id:<food_id>'` bei echtem Produkt-Match, sonst `'name:<normalisierter Name>'`. Grund: Rezept-explodierte Zutaten und Custom Items (u.a. jeder Eintrag aus "Describe a meal", das KI-Freitext-Logging, schreibt immer `foodId: null`, siehe `commitMealItems`) hatten vor dieser Migration keine Möglichkeit, überhaupt eine Präferenz-Zeile zu bekommen (Exclude-Checkbox war hart `disabled`, Edit-Sheet ließ sich nicht öffnen), weil `food_id NOT NULL` die einzige Identität war. `shopping_key` ist derselbe Key, unter dem `fdBuildShoppingList` die Bedarfs-Schätzung ohnehin schon tallyt, ein namensbasierter Pref matcht sein Item also genauso zuverlässig wie ein id-basierter
- `name_override` (text, nullable): Anzeigename statt des generischen DB-Namens (z.B. "Original" bei Marke "Weetabix")
- `excluded`/`excluded_until` (boolean NOT NULL Default false / timestamptz nullable, Migration 0231 für letztere): ein gemeinsamer Exclude-Picker im Edit-Sheet von `ShoppingListScreen` (`Field label="Exclude"`, State `excludeDraft`), vier sich gegenseitig ausschließende Segmente: **1 week/2 weeks/1 month** setzen `excluded_until` auf `now() + N Tage` (feste Presets, kein Datepicker, `fdSnoozeUntil`) und lassen `excluded` false; **Permanent** setzt `excluded: true` und räumt `excluded_until` leer. Ein entfernter Haken/"Clear" räumt beide auf einmal weg. `excluded_until` ist kein Live-Countdown und kein Cron: `fdApplyShoppingPrefs` vergleicht bei jedem Read gegen `new Date()` und behandelt die Zeile nur dann als temporär excluded, wenn der Wert noch in der Zukunft liegt (`item.tempExcludedUntil`, nur gesetzt solange aktiv, unabhängig vom rohen `item.permanentExcluded`); ein abgelaufener Wert lässt das Item von selbst wieder in die normale Liste zurückfallen, ganz ohne manuelles Re-Toggle. Effektiv excluded (permanent ODER aktiv temporär) nimmt das Item nur aus Export/Screenshot raus, die Zeile bleibt (gedimmt) sichtbar in einem eigenen "Excluded"-Abschnitt der Liste. Reset im Edit-Sheet lässt beide Felder unangetastet (eigene Abgrenzung, das Reset ist nur für Rename/Packungsgröße/Bestand)
- `package_size_g` (numeric, nullable): Packungsgröße in Gramm, z.B. `400` bei einer 400g-Packung. Treibt `fdFormatShoppingQty`: die vorgeschlagene Kaufmenge rundet auf ganze Packungen auf (`Math.ceil`), statt die rohe Gramm-Schätzung zu zeigen. `null`/nicht gesetzt heißt Fallback auf die reine Gramm-Rundung wie zuvor (`fdRoundShoppingQty`)
- `stock_baseline_g`/`stock_set_at` (numeric/timestamptz, beide nullable, Migration 0216, immer gemeinsam gesetzt oder beide null): Inventory-Tracking. Kein live runtergezähltes Feld (das bräuchte einen Hook in jedem Logging-Pfad plus einen Undo-Hook bei jeder Bearbeitung/Löschung eines Log-Eintrags), stattdessen wird der aktuelle Bestand zur Laufzeit hergeleitet als `stock_baseline_g` minus allem, was seit `stock_set_at` tatsächlich gegessen wurde (`fdConsumedSince` summiert rezept-explodierte `quantity_g` aus `store.foodLogs` im Zeitraum, `fdEffectiveStockG`). Zwei Schreibpfade, beide setzen frischen Nullpunkt "ab jetzt": manuell im Edit-Sheet (Betrag eintippen), oder die "Mark as bought"-Checkbox links neben einer Shopping-List-Zeile (nicht in `inventoryMode` und nicht bei excluded/`!hasEstimate` Items): öffnet ein kurzes Popup (`boughtPrompt`, `openBoughtPrompt`/`confirmBoughtPrompt` in `screens-food.jsx`), vorausgefüllt mit `fdShoppingBuyQtyG(item.grams, item.packageSizeG)`, exakt der im Row-Headline gezeigten Kaufmenge, aber frei editierbar (package-aware wie die Stock-Felder im Edit-Sheet: mit gesetzter Packungsgröße ein Feld in ganzen Packs samt Live-Summe, sonst ein reines Gramm-Feld), weil die real gekaufte Menge nicht zwingend dem Vorschlag entspricht. Erst Confirm addiert die (ggf. angepasste) Menge auf den vorherigen hergeleiteten Bestand drauf statt ihn zu ersetzen (ein angebrochenes Paket von vor dem Einkauf ist weiter echter Bestand); Cancel verwirft das Popup ohne jede Schreibaktion. Die Checkbox selbst ist rein lokaler, Session-only UI-State (`boughtSet` in `ShoppingListScreen`, startet bei jedem Öffnen leer, kein DB-Feld, kein Store-Field), ein bestätigtes Abhaken schreibt sofort und ist nicht rückgängig zu machen, ein erneutes Abwählen (ohne Popup) räumt nur die lokale Markierung weg. Fällt der hergeleitete Bestand unter den wirksamen Schwellwert (`low_stock_threshold_g`, Default `package_size_g`, siehe unten), rutscht die Zeile in `ShoppingListScreen`s Shopping-List-Tab in einen eigenen, prominent gestylten "Running Low"-Abschnitt (zusätzlich einmalig ein dismissbares Banner, Tracking dafür rein lokal per `logbook-low-stock-acked`, kein DB-Feld nötig: der Ack ist an den jeweiligen `stock_set_at`-Wert gekeyt, ein neu gesetzter Bestand invalidiert alte Acks also automatisch). Umgekehrt gilt auch `fdIsWellStocked` (getrackt, echter Schwellwert vorhanden, aktuell darüber): so eine Zeile fliegt komplett aus `includedList` raus, taucht im Shopping-List-Tab also nirgends mehr auf (weder normale Liste noch Export/Poster), es gibt ja gerade nichts zu kaufen. Bleibt trotzdem jederzeit im Inventory-Tab sichtbar (unabhängig von `fdBuildShoppingList`s Staple-/Projection-Filter, siehe `food_name`/`brand` oben), und taucht von selbst wieder in der normalen Liste auf, sobald der Bestand tatsächlich unter den Schwellwert fällt, kein manuelles Zurückholen nötig. Ohne Schwellwert (weder `low_stock_threshold_g` noch `package_size_g` gesetzt) greift dieser Ausschluss bewusst nicht: ohne Vergleichsbasis wäre ein komplett leerer, aber nie mit Schwellwert versehener Bestand sonst fälschlich als "gut bevorratet" von der Liste verschwunden
- `low_stock_threshold_g` (numeric, nullable, Migration 0232): überschreibt `package_size_g` als Vergleichswert für `fdIsLowStock` (`threshold = item.lowStockThresholdG ?? item.packageSizeG; threshold > 0 && effectiveStockG < threshold`). Grund: "unter einer Packung" ist bei einem Food, das schneller leer geht als eine Packung reicht (500g-Becher Skyr, der an einem Tag komplett weggegessen wird), zu spät für eine nützliche Vorwarnung, der Bestand ist dann schon quasi null statt "bald nachkaufen". Leer heißt reines Default-Verhalten (Vergleich gegen `package_size_g` wie vor Migration 0232), ein gesetzter Wert ersetzt das komplett, unabhängig davon ob überhaupt eine Packungsgröße existiert (funktioniert also auch für lose gekaufte Ware ganz ohne `package_size_g`). Eingabefeld im selben Stock-Tracking-Block des Edit-Sheets wie Packungsgröße/Bestand, package-aware wie das Bestandsfeld darüber: mit gesetzter Packungsgröße ein Feld in ganzen Packungen (`thresholdPacksDraft`, z.B. "2" bei 500g-Packung → 1000g), sonst ein reines Gramm-Feld (`thresholdGramsDraft`). Zwei getrennte Drafts statt einem gemeinsamen, der beim Umschalten der Einheit (Packungsgröße im selben Sheet-Besuch getippt/gelöscht) eine bereits eingetippte Zahl sonst stillschweigend als andere Einheit re-interpretieren würde; `saveEdit` liest beim Speichern ausschließlich das gerade sichtbare Feld, gegen die frisch eingetippte (nicht die alte gespeicherte) Packungsgröße umgerechnet. Reset im Edit-Sheet räumt es zusammen mit Rename/Packungsgröße/Bestand mit weg (anders als bei `excluded`/`excluded_until`, die Reset bewusst nicht anfasst)
- `food_name` (text, NOT NULL, Migration 0217), `brand` (text, nullable, Migration 0217): Identitäts-Snapshot zum Schreibzeitpunkt, gleiches Prinzip wie bei `zane_food_logs`/`zane_food_favorites`. Bestehende Zeilen wurden bei der Migration einmalig aus `zane_foods` (über `food_id`) befüllt, jeder spätere Write geht über `fdSetShoppingPref`, das `foodName`/`brand` immer mitschreibt. Grund: der **Inventory-Tab** in `ShoppingListScreen` listet jede Zeile mit gesetztem `stock_baseline_g` unabhängig von `fdBuildShoppingList`s Staple-/Projection-Filter (`fdBuildInventoryList`), ein selten gekaufter Bulk-Artikel braucht also eine eigene Namensquelle, die nicht davon abhängt, ob er gerade im 14-Tage-Fenster oder noch im geladenen `foodLogs`-Fenster auftaucht
- `created_at`/`updated_at` (timestamptz)
- **UNIQUE (`user_id`, `shopping_key`)**: höchstens eine Präferenz-Zeile je `shopping_key` und User (Migration 0227, vorher `UNIQUE (user_id, food_id)`)
- Store field: `store.foodShoppingPrefs`. Einfache eigene User-Collection wie `zane_food_favorites`, gleiches Collection-Sync-/Boot-Merge-Muster (`diffCollectionById`), kein Coach-Zugriff.
- RLS: nur eigene Zeilen (`(select auth.uid()) = user_id`, initPlan-Caching von Anfang an, nicht erst nachträglich gefixt wie bei `zane_food_favorites`/`zane_food_recipes` in Migration 0192). Migration 0215, Stock-Felder Migration 0216, Identitäts-Snapshot Migration 0217, temporärer Exclude Migration 0231.

### Medications (Übersicht)

Migration 0218 (Basis), Migration 0221 (Mehrfach-Plan-Zugehörigkeit), Migration 0223 (Weekly Prep). Persönlicher (optional coach-verwalteter) Tracker für Medikamente/Vitamine/Supplements, architektonisch am Food Tracker Plan Mode orientiert (Container → Items → Schedule → Log), aber mit eigenem Wochentags-Schedule (nicht an Trainings-/Rest-Tag gekoppelt) und eigener Inventory-Einheit (nicht immer Gramm). Sechs Tabellen, jede unten mit eigenem Abschnitt: `zane_medication_plans`, `zane_medications`, `zane_medication_plan_items`, `zane_medication_schedule_slots`, `zane_medication_logs`, `zane_medication_pillbox_checks`.

**RLS (die ersten fünf Tabellen identisch):** eigene Zeilen (`(select auth.uid()) = user_id`) plus volles Coach-of-Client SELECT/INSERT/UPDATE/DELETE über `zane_is_coach_of` (Migration 0199-Muster, `zane_food_meal_plans`/`zane_food_template_slots`). Der Coach sieht damit nicht nur selbst gepushte Pläne, sondern die komplette Medikation, den Schedule und die tatsächlich geloggte Einnahme eines Clients (Adherence). Migration 0218/0221. Die sechste Tabelle, `zane_medication_pillbox_checks`, ist bewusst nur eigene Zeilen ohne Coach-Zugriff, siehe ihr eigener Abschnitt unten.

**Push-Mechanismus:** `LB.pushMedicationPlanToClient` in `store.js`, 1:1 nach Vorbild von `pushMealPlanToClient` (Meal Plans): kopiert Plan + Medikamente + deren Plan-Zugehörigkeit (`zane_medication_plan_items`) + Schedule-Slots frisch in den Client-Account, postet eine Nachricht in einen eigenen "Medications"-Coaching-Thread. Die Kopie ist immer `active: true`, unabhängig vom Active-Status des Coach-eigenen Plans (gleiche Begründung wie das erzwungene `medsEnabled: true` dort: ein gepushter Plan, den der eigene Toggle des Clients versteckt, wäre witzlos).

**Reminder:** eigene Edge Function `medication-reminder` (pg_cron stündlich, Migration 0219), 1:1 nach Vorbild von `meal-reminder` (siehe `medication_reminder_enabled` weiter oben), prüft `zane_medication_logs` auf noch-`planned` Einträge und feuert nach 1h Grace Period. Seit Migration 0246 state-basiert statt fenster-basiert (die drei Reminder-Spalten auf `zane_medication_logs`, siehe deren Abschnitt unten): erster Nudge auf jedem Tick nach der Grace (catch-up-sicher, ein übersprungener Cron-Tick kann den Nudge nicht mehr lautlos verlieren), zweiter Nudge frühestens 2h nach dem ersten, hart gedeckelt auf 2 pro Tag. Der Client kann eine fällige Dosis per „Snooze 1h"-Button stumm schalten (schreibt `snoozed_until` über den normalen Log-Sync, kein localStorage); nach Ablauf kommt der nächste Nudge direkt beim ersten Tick ab Ablauf (das Snooze-Ende ist auf die nächste volle Stunde aufgerundet und damit ein echter Nudge-Zeitpunkt). Yesterday-Zeilen behalten die Fenster-Grenze: nur eine späte (>=23:00) Dosis darf nach Mitternacht feuern, gewöhnliche vergessene Dosen von gestern re-nerven nie. Materialisiert seit dem Audit-Fix 2026-08 (M8) fehlende Doses vorher selbst (`materializeDueDoses`): liest dafür `zane_medication_plans` (aktive Pläne), `zane_medication_schedule_slots` und `zane_medications` (nicht archiviert) direkt und upsertet fehlende `planned: true`-Zeilen mit derselben deterministischen Id (`md_<date>_<slotId>`) wie der Client (`mdAutoFillToday`, `screens-medications.jsx`), sonst hätte ein an dem Tag nie geöffneter Meds-Tab schlicht keine Zeile zum Finden und die Erinnerung fiele lautlos aus. Eigene TypeScript-Kopie von `dsSlotAppliesOn` (`slotAppliesOn` in `medication-reminder/index.ts`, `store.js` ist kein ladbares Modul), bei Änderungen an der Schedule-Matching-Logik beide Stellen nachziehen. Damit jetzt sehr wohl von Migration 0221 betroffen (`medication_plan_id`/`active`), nicht mehr nur Leser von `zane_medication_logs`.

**Backup:** die ersten fünf Tabellen sind Teil des User-Backups (eigene, personenbezogene Daten wie Food-Logs/-Pläne). `zane_medication_pillbox_checks` ist bewusst **nicht** im Backup (abgeleiteter Wochen-Status, siehe ihr eigener Abschnitt unten).

**Weekly Prep:** `WeeklyPrepScreen` (`screens-medications.jsx`) zeigt für die kommenden 7 Tage, gruppiert nach Tag und nutzerdefiniertem Pillendosen-Fach (`settings.pillboxSlots`, siehe oben), was aus den Schedule-Slots aktiver Pläne gepackt werden muss, mit einer Abhak-Checkliste je (Tag, Slot). Ein Medikament mit `exclude_from_pillbox` (siehe unten) taucht dort nie auf.

### `zane_medication_plans`

Benannter Container (z.B. "Vitamine", "Prep Cycle Q1"), siehe Medications-Übersicht oben.

- `id` (text, PK), `user_id` (uuid), `name` (text), `archived`/`is_template` (boolean, Default false), `coach_id` (uuid, nullable), `created_at`/`updated_at`
- `is_template`/`coach_id` sind das Coach-Bucket-Flag wie bei `zane_food_meal_plans` (My Plans vs. Client Templates, ein Push kopiert Plan + Medikamente + deren Schedule-Slots in den Client-Account)
- `active` (boolean, NOT NULL, Default true, Migration 0221): nur ein aktiver Plan feuert Doses (Timeline-Auto-Fill/Preview prüfen das über die Schedule-Slots, siehe deren `medication_plan_id` unten). **Anders als Food**: kein einzelner Active-Pointer, mehrere Pläne können gleichzeitig aktiv sein (z.B. tägliche Vitamine parallel zu einem vom Coach verordneten Cycle), das trifft die Realität besser als ein Entweder-Oder. Toggle in der Plan-Detail-Ansicht (`screens-medications.jsx`). Ein pausierter Plan bleibt voll editierbar, er feuert nur keine Doses
- Store field: `store.medicationPlans`
- RLS: siehe Medications-Übersicht. Migration 0218, `active` Migration 0221.

### `zane_medications`

Das getrackte Medikament selbst (reine Identität + hergeleitetes Inventory), siehe Medications-Übersicht oben. Hat seit Migration 0221 **keinen** eigenen Plan-Verweis mehr: welche(n) Plan(e) ein Medikament angehört, steht ausschließlich in `zane_medication_plan_items` (many-to-many, siehe unten), damit dasselbe Medikament (ein gemeinsamer Bestand) in mehreren Plänen mit jeweils eigenem Schedule laufen kann.

- `id` (text, PK), `user_id` (uuid)
- `name` (text), `brand` (text, nullable)
- `category` (text, nullable): freier Text, keine DB-CHECK-Konstraint, das UI bietet Presets wie Vitamin/Compound/Peptide/Other an, eine neue Kategorie braucht also nie eine Migration
- `unit_label` (text, NOT NULL, Default `'pills'`): die Zähleinheit, z.B. "pills"/"ml"/"drops", frei editierbar da Medikamente sehr unterschiedlich dosiert werden
- `package_size`/`stock_baseline`/`stock_set_at` (numeric/numeric/timestamptz, alle nullable): gleiches hergeleitetes Inventory-Prinzip wie `zane_food_shopping_prefs` (aktueller Bestand = `stock_baseline` minus allem seit `stock_set_at` Geloggten, client-seitig zur Laufzeit berechnet, kein Live-Countdown-Feld)
- `low_stock_threshold` (numeric, nullable, Migration 0233): überschreibt `package_size` als Vergleichswert für `mdIsLowStock` (`threshold = med.lowStockThreshold ?? med.packageSize; threshold > 0 && effectiveStock < threshold`), 1:1 dasselbe Prinzip wie `zane_food_shopping_prefs.low_stock_threshold_g` (Migration 0232): manche Medikamente sind schneller aufgebraucht als eine Packung reicht, "unter einer Packung" allein warnt dann zu spät. Leer heißt reines Default-Verhalten (Vergleich gegen `package_size`), ein gesetzter Wert ersetzt das komplett. Anders als beim Food Tracker kein Packs/Grams-Dual-Draft nötig: Medikamenten-Mengen sind ohnehin schon eine flache Zahl im freien `unit_label` (z.B. "pills"), kein Gramm-vs-Packungs-Umrechnungsschritt. Eingabefeld in `stockSheet` (Inventory-Tab, Stock-Subtab, `lowStockThresholdStr`, `screens-medications.jsx`), nicht in `medSheet`: dort, wo auch Bestand und Packungsgröße gepflegt werden, nicht bei der Medikament-Identität
- `exclude_from_low_stock` (boolean, NOT NULL, Default false, Migration 0234): `mdIsLowStock` gibt für so ein Medikament immer `false` zurück, unabhängig vom tatsächlichen Bestand (weder Banner noch der dauerhafte, nicht wegtippbare "Running Low"-Abschnitt feuern), aber **Stock-Tracking selbst bleibt an**: `effectiveStock` wird weiter ganz normal hergeleitet und angezeigt, nur die Warnung wird unterdrückt. Unterscheidet sich damit bewusst von "Stop tracking" im Stock-Sheet (setzt `stock_baseline`/`stock_set_at` komplett auf `null`, kein neues Feld dafür nötig, siehe oben): dieses Flag ist für einen Compound, den man z.B. für einen Cycle aufbraucht und danach nicht nachbestellt, den Bestand aber trotzdem im Blick behalten will. UI-Label bewusst nicht wörtlich "Exclude from low stock" sondern **"Cycle only"** (Row in `stockSheet`, Inventory-Tab-Stock-Subtab, nicht in `medSheet`), der Spaltenname selbst bleibt technisch/mechanismus-benannt wie `exclude_from_pillbox`
- `track_stock` (boolean, NOT NULL, Default false, Migration 0235, Default-Korrektur auf false + Backfill Migration 0236): Master-Schalter, unabhängig von `low_stock_threshold`/`exclude_from_low_stock` und unabhängig vom tatsächlichen Trackingstand (`stock_baseline`). Aus bedeutet zweierlei zum Aufräumen: (1) `medSheet` zeigt statt Eingabefeldern nur einen Hinweistext, der auf Inventory > Stock verweist: "Warn when below" und "Cycle only" werden seit ihrem Umzug (siehe deren Bullets oben) ausschließlich dort in `stockSheet` editiert, das ohnehin nur für ein bereits `trackStock`-aktives Medikament überhaupt öffnbar ist (`renderMedRow`, nur aus `lowStockList`/`mainInventoryList`, beide auf `trackStock` gefiltert), ein eigenes Ausblenden dieser Felder braucht `medSheet` also gar nicht mehr zu tun; `package_size` bleibt davon unberührt sichtbar/editierbar (reine Behälter-Tatsache unabhängig vom Tracking), (2) `mainInventoryList` in `MedicationsScreen` (Inventory-Tab, Stock-Subtab) filtert das Medikament komplett raus, es taucht dort nicht mal mehr als "Not tracked" auf. `mdIsLowStock` prüft `!med.trackStock` als allerersten Short-Circuit, noch vor dem `exclude_from_low_stock`-Check. Der Medications-Subtab (CRUD-Liste) ist davon **nicht** betroffen, ein Medikament bleibt dort immer auffindbar/editierbar. Rein additive UI-Sichtbarkeit, nicht destruktiv: `stock_baseline`/`stock_set_at` bleiben unangetastet, ein späteres Zurückschalten auf an zeigt einen eventuell noch vorhandenen alten Bestand sofort wieder. **Default false** (Opt-in, Migration 0236 korrigiert 0235s ursprüngliches `DEFAULT true`): die meisten Medikamente (Vitamine etc.) werden nie physisch gezählt, sollen also nicht standardmäßig im Inventory-Tab aufschlagen. Migration 0236 backfillt dafür gezielt `true` bei jeder Zeile, die bereits einen `stock_baseline`-Wert hat (klar schon bewusst getrackt), sonst `false`, sowohl für Bestandsdaten als auch für ältere Backups ohne dieses Feld (`importFromBackup` in `store.js` wendet dieselbe Herleitung an: `m.trackStock != null ? !!m.trackStock : m.stockBaseline != null`). Neue Medikamente starten ebenfalls mit `trackStock: false` in `medSheet`. UI-Label des Toggles: „Track Medication Stock" (`medSheet`)
- `archived` (boolean, Default false), `created_at`/`updated_at`
- `exclude_from_pillbox` (boolean, NOT NULL, Default false, Migration 0223): blendet dieses Medikament komplett aus `WeeklyPrepScreen` aus, z.B. für ein Injectable, das ohnehin nie in eine physische Pillendose kommt. Toggle in `medSheet`.
- Store field: `store.medications`
- RLS: siehe Medications-Übersicht. Migration 0218, `medication_plan_id` entfernt in Migration 0221, `exclude_from_pillbox` Migration 0223, `low_stock_threshold` Migration 0233, `exclude_from_low_stock` Migration 0234, `track_stock` Migration 0235 (Default-Korrektur + Backfill Migration 0236).

### `zane_medication_plan_items`

Reine Mitgliedschafts-Tabelle (Medikament ↔ Plan, many-to-many), keine Schedule-Info. Migration 0221.

- `id` (text, PK), `user_id` (uuid), `created_at`
- `medication_plan_id` (text, NOT NULL, **weicher Verweis, kein FK**: gleicher Grund wie zuvor bei `zane_medications.medication_plan_id`, ein Cascade könnte hier mit dem clientseitigen Sync-Diff kollidieren)
- `medication_id` (text, NOT NULL, **FK → `zane_medications.id` ON DELETE CASCADE**: eine Mitgliedschafts-Zeile hat kein eigenständiges Leben ohne ihr Medikament, gleicher Grund wie `medication_id` bei `zane_medication_schedule_slots`)
- `UNIQUE(medication_id, medication_plan_id)`: ein Medikament kann nicht doppelt demselben Plan hinzugefügt werden
- Store field: `store.medicationPlanItems`
- RLS: siehe Medications-Übersicht. Migration 0221, inkl. `zane_guard_user_id_immutable`-Trigger von Anfang an.

### `zane_medication_schedule_slots`

Wiederkehrende Dosis, siehe Medications-Übersicht oben.

- `id` (text, PK), `user_id` (uuid), `medication_id` (text, NOT NULL, **FK → `zane_medications.id` ON DELETE CASCADE**: ein Schedule-Slot hat kein eigenständiges Leben ohne sein Medikament)
- `medication_plan_id` (text, nullable, **weicher Verweis, kein FK**, Migration 0221): welchem Plan-Mitgliedschaft dieser Slot zugeordnet ist, macht pro Plan einen eigenen Schedule fürs selbe Medikament möglich (z.B. eine andere Dosis/Zeit im Coach-Cycle-Plan als im eigenen Baseline-Plan). Ein Slot feuert nur, wenn sein Plan existiert **und** `active` ist (siehe oben), sonst gilt er als ruhend, ganz ohne eigenes Pause-Flag auf dem Slot selbst
- `weekdays` (integer[], NOT NULL, Default `{0,1,2,3,4,5,6}`): welche Wochentage der Slot gilt, **0=Montag…6=Sonntag** wie `isoWd` in `store.js`, "jeden Tag" braucht also nur eine Zeile statt sieben. **Wird ignoriert, wenn `interval_days` gesetzt ist** (siehe dort), im Interval-Modus steht hier üblicherweise `[]` (nicht erzwungen, nur was `saveSlotDraft` schreibt)
- `hour` (integer, NOT NULL, Default 8, 0-23), `dose_qty` (numeric, NOT NULL)
- `interval_days` (integer, nullable, Migration 0237): zweiter, sich gegenseitig mit `weekdays` ausschließender Schedule-Modus, **"alle N Tage"** statt fixer Wochentage, für Compounds (v.a. Peptide), die alle 2 (oder x) Tage statt an festen Wochentagen genommen werden. `null` (Default, alle Bestandszeilen) heißt Wochentags-Modus wie bisher, unverändert. Gesetzt (`> 0`) schaltet den Slot in den Interval-Modus um: er feuert an jedem Tag, für den `(dateISO − start_date) % interval_days === 0` gilt, `start_date` ist damit der **Anker** und im Interval-Modus clientseitig zur Pflichtangabe erhoben (UI erzwingt sie, keine DB-Constraint), statt wie im Wochentags-Modus nur eine optionale Phasen-Grenze zu sein. `end_date` bleibt in beiden Modi identisch die optionale obere Phasen-Grenze. Ausgewertet in `dsSlotAppliesOn` (`store.js`, exportiert als `LB.dsSlotAppliesOn`): seit dem Refactor im Zuge des Multi-Device-Sync-Reviews die **einzige** Implementierung, die Medications-Screens rufen dieselbe Funktion statt einer eigenen hand-synchronisierten Kopie (das frühere `mdSlotAppliesOn` in `screens-medications.jsx` ist entfernt); sie liegt in `store.js`, weil der `store.test.cjs`-Sandbox `screens-medications.jsx` nicht lädt. UI: ein Weekdays/„Every X days"-Segmented-Toggle im Add/Edit-Time-Sheet (`slotDraft.mode`), darunter je nach Modus entweder der bestehende Wochentags-Picker oder ein Stepper "Every N days" (2-90)
- `start_date`/`end_date` (date, beide nullable): **optionale zeitliche Phase** im Wochentags-Modus, beide `null` (Default/Normalfall) heißt der Slot läuft unbegrenzt wie jeder normale Schedule; gesetzt begrenzt er den Slot auf ein Datumsfenster, so lassen sich echte gestaffelte Cycles abbilden (z.B. zwei Slots für dasselbe Medikament mit unterschiedlicher Dosis in unterschiedlichen Wochen), ganz ohne ein separates Phasen-System. Im Interval-Modus (siehe `interval_days` oben) ist `start_date` **kein optionales Extra mehr, sondern der Zähl-Anker** selbst, `end_date` bleibt optional
- `created_at`/`updated_at`
- Store field: `store.medicationScheduleSlots`
- RLS: siehe Medications-Übersicht. Migration 0218, `medication_plan_id` statt `active` seit Migration 0221 (das eigene Pause-Flag pro Slot wurde entfernt: als verwirrend empfunden, "aus dem Plan entfernen" ist jetzt der einzige Hebel), `interval_days` Migration 0237.

### `zane_medication_logs`

Die Einnahme-Timeline, spiegelt `zane_food_logs`, siehe Medications-Übersicht oben.

- `id` (text, PK), `user_id` (uuid)
- `medication_id` (text, nullable, FK → `zane_medications.id` **ON DELETE SET NULL**: Historie überlebt das Löschen des Medikaments, wie `zane_food_logs.food_id`), `medication_name` (text, NOT NULL, zum Schreibzeitpunkt kopiert)
- `date` (text, YYYY-MM-DD lokal), `time` (text, HH:MM lokal), `dose_qty` (numeric, NOT NULL)
- `planned` (boolean, NOT NULL, Default false): aus dem Schedule materialisiert vs. spontan geloggt, gleiche Bedeutung wie bei Food
- `skipped` (boolean, NOT NULL, Default false, Migration 0257): der Nutzer hat diese Dosis **bewusst** aus der Timeline entfernt. Eine gelöschte Zeile hinterlässt keine Spur, und sowohl `materializeDueDoses` (Edge) als auch `mdAutoFillToday` (Client) entscheiden allein danach, ob für Slot und Datum schon eine Zeile existiert; die Dosis kam also beim nächsten Lauf zurück, mit zurückgesetzten Reminder-Zählern. `skipped` ist diese Spur: die Zeile bleibt stehen, damit beide Materialisierer sie sehen, `planned: false` hält sie aus der Reminder-Abfrage und den Pending-Zählungen. Bewusst **verschieden** von einem bloßen `planned: false`, das „eingenommen" bedeutet. Nur slot-materialisierte Zeilen brauchen das: eine handgeloggte Dosis trägt keine `schedule_slot_id`, nichts legt sie neu an, sie wird weiterhin hart gelöscht.
- `schedule_slot_id` (text, nullable, FK → `zane_medication_schedule_slots.id` ON DELETE SET NULL)
- `reminder_sent_at` (timestamptz, nullable, Migration 0246): Zeitpunkt des letzten Nudges der `medication-reminder`-Cron für diese Zeile (null = noch nie). Server-geschrieben (Service-Role-PATCH direkt nach dem Senden), der Client schreibt das Feld nie (siehe `reminder_count`)
- `reminder_count` (integer, NOT NULL, Default 0, Migration 0246): wie viele Nudges für diese Dosis bereits gefeuert wurden, hart gedeckelt auf 2 (danach feuert der Cron nie wieder für diese Zeile). Bewusst pro Zeile und damit pro Tag (jeder lokale Tag materialisiert seine eigene geplante Zeile), kein separater Tages-Reset nötig. Server-geschrieben; `syncStore`'s medicationLogs-Upsert sendet das Feld absichtlich nicht, damit ein stales Gerät den Zähler nicht zurücksetzen und so den Tages-Cap verlängern kann
- `snoozed_until` (timestamptz, nullable, Migration 0246): Client-geschrieben über den „Snooze 1h"-Button auf einer noch fälligen Timeline-Zeile (`screens-medications.jsx`), synchronisiert über den normalen Log-Sync (kein localStorage). Der Button erscheint erst, wenn der erste Nudge gefeuert hat (`reminder_count >= 1`, man kann keinen Alarm snoozen, der noch nicht kam), und ein erneuter Tap nimmt den Snooze wieder zurück (setzt das Feld auf `null`). Der Cron überspringt die Zeile, solange `snoozed_until > now` gilt. Das Snooze-Ende wird clientseitig auf die nächste volle Stunde aufgerundet (der Cron tickt stündlich) und ist ein echter Nudge-Moment: Der nächste Nudge feuert beim ersten Tick ab Ablauf (unabhängig von der 2h-Regel, die nur für nie gesnoozte Dosen gilt), nicht irgendwann dazwischen. Snooze verbraucht keinen Nudge und setzt den Zähler nicht zurück
- `created_at`
- Store field: `store.medicationLogs`
- RLS: siehe Medications-Übersicht. Migration 0218. Reminder-Cron liest diese Tabelle und schreibt die drei Reminder-Spalten, Migration 0219/0246.

### `zane_medication_pillbox_checks`

Weekly-Prep-Abhak-Marker (Migration 0223): eine Zeile je (User, Tag, Schedule-Slot), die bereits in die physische Pillendose gepackt wurde, geräteübergreifend. Genau wie `zane_food_template_days` eine reine Existenz-Markierung (Zeile da = abgehakt, Zeile weg = nicht abgehakt), kein Update-Pfad nötig.

- `id` (text, PK, deterministisch `<user_id>_<date>_<schedule_slot_id>`, damit zwei Geräte denselben Haken idempotent upserten), `user_id` (uuid)
- `date` (text, YYYY-MM-DD), `schedule_slot_id` (text, **weicher Verweis, kein FK**: gleicher Grund wie `medication_plan_id` in dieser Tabellenfamilie, ein Cascade könnte mit dem clientseitigen Sync-Diff kollidieren; eine verwaiste Zeile ist ohnehin unsichtbar, `WeeklyPrepScreen` iteriert nur über aktuell existierende Slots)
- `created_at` (timestamptz)
- Store field: `store.medicationPillboxChecks`. Synchronisiert (Boot-Load ab heute, `syncStore`-Diff, Boot-Merge), aber **nicht im Backup** (abgeleiteter Wochen-Status, EXCLUDED in `tools/check-backup-coverage.cjs`), gleiche Begründung wie `zane_food_template_days`.
- RLS: **nur eigene Zeilen, kein Coach-Zugriff** (Ausnahme in dieser Tabellenfamilie, siehe Medications-Übersicht), kein `zane_guard_user_id_immutable`-Trigger (der wird nur gebraucht, wo eine Coach-UPDATE-Policy existiert, siehe Migration 0220). Migration 0223.

### `zane_recipe_shares`

Share-Links für Rezepte (Share-Button im Recipes-Tab des Food Trackers). Eine Zeile pro geteiltem Rezept, gekeyt über einen nicht erratbaren Token, der zugleich der Deep-Link ist (`…/?share=<token>`). `recipe` ist ein jsonb-**Snapshot** zum Zeitpunkt des Teilens (`{ name, portions, items }`, Shape wie bei `zane_food_recipes`): der Link funktioniert weiter, wenn der Sharer das Original später editiert oder löscht, und leakt umgekehrt keine späteren Änderungen. Erneutes Teilen desselben Rezepts refresht den Snapshot und liefert **denselben** Token (Upsert über `user_id`+`recipe_id`), Re-Shares stapeln also keine Zeilen.

- `token` (text, PK: 32 Hex-Zeichen aus `gen_random_uuid()`), `user_id` (uuid, der Sharer), `recipe_id` (text: `zane_food_recipes.id` des Sharers, bewusst **ohne** FK, der Share überlebt das Rezept), `recipe` (jsonb-Snapshot), `created_at` (timestamptz, bei Re-Share gebumpt)
- **RLS ohne Policies**: Clients erreichen die Tabelle nie direkt, alle Zugriffe laufen über die beiden RPCs `create_recipe_share`/`get_recipe_share` (siehe „Sonstiges"). Kein Store-Field, kein Sync, nicht im Backup (die Kopie des Empfängers wird eine ganz normale eigene `zane_food_recipes`-Zeile).
- Client-Flow: Share-Button → `create_recipe_share` → Link über OS-Share-Sheet bzw. Copy. Der Empfänger öffnet den Link, `app.jsx` stasht den Token (localStorage `logbook-pending-share`, überlebt so den Login-/Signup-Roundtrip; gelöscht, sobald das `RecipeShareSheet` geschlossen wird) und säubert die URL; sobald die App `ready` ist, zeigt `RecipeShareSheet` (`screens-food.jsx`) die Vorschau, „Add to my recipes" kopiert den Snapshot als neues eigenes Rezept (frische Item-Ids, Namens-Dedupe per `(2)`-Suffix). Die Vorschau zeigt `recipe.items` in genau der Reihenfolge, in der der Sharer sie im Rezept-Editor per Drag angeordnet hat (die Kochreihenfolge), **nicht** `fdSortIngredientsByQty` (Mengen-Sortierung, für andere Ansichten wie die Timeline-Detailansicht oder die Meal-Plan-Block-Preview gedacht, die kein Schritt-für-Schritt-Nachkochen sind). Migration 0193.

### `zane_api_usage`

Tages-Kontingent pro User für die Food-Edge-Functions (`search-foods`, `scan-label`, `parse-meal`, Migration 0207) und, seit Migration 0224/0225, für die AI-Coach-Edge-Functions (`ai-daily-summary` kind `'daily_summary'`, `ai-checkin-opinion` kind `'checkin_opinion'`). Bis dahin war Auth die einzige Hürde: eine Suche feuert zwei Drittanbieter-APIs, ein Etikettenscan nimmt bis zu 8 MB Base64 entgegen und bezahlt pro Aufruf ein Vision-Modell. Ein einzelner Account (absichtlich oder durch eine hängende Retry-Schleife) konnte so unbemerkt echte Kosten erzeugen. `kind` ist generisch über beliebige Features hinweg: jede neue Edge Function bringt einfach ihren eigenen `kind`-String und ein eigenes Limit mit, keine Migration nötig. `search-foods` selbst trägt zwei getrennte Kinds: `'search'` (nur die eigentliche Suche, Limit 400) und, seit einem Audit-Fix, `'food_lookup'` (die Actions `select`/`cache`, ebenfalls Limit 400) — beide machen live Drittanbieter-Calls (`select` bei Cache-Miss, `cache` schreibt zusätzlich per Service-Role in `zane_foods`), hingen bis dahin aber komplett ungedeckelt am `search`-geschützten Endpoint, konnten `search-foods` also am Quota vorbei ausreizen. Bewusst kein gemeinsamer Kind mit `'search'`: die dem User gezeigte Fehlermeldung „400 food searches" wäre sonst nicht mehr wörtlich wahr, wenn `select`/`cache`-Aufrufe (die der User nie direkt als „Suche" wahrnimmt) mitzählen würden.

- `user_id` (uuid, FK → `auth.users` `ON DELETE CASCADE`), `day` (date, Default `current_date`), `kind` (text, kein CHECK: `'search'` | `'scan'` | `'daily_summary'` | `'checkin_opinion'`, weitere Features ergänzen frei neue Werte), `count` (integer, Default 0), `updated_at` (timestamptz). PK `(user_id, day, kind)`.
- **RLS ohne Policies** (gleiche Bauart wie `zane_recipe_shares`): erreichbar ausschließlich über die RPC `bump_api_usage`, die wiederum **nur** `service_role` ausführen darf. Kein Client liest oder schreibt hier direkt, niemand kann sein eigenes Kontingent einsehen oder zurücksetzen.
- **Beratend, nicht bindend:** die Edge Functions failen bei einem Fehler der RPC bewusst **offen** (Aufruf geht durch). Ein Problem am Quota-Mechanismus selbst darf niemandem das Loggen seines Essens verbauen. Limits stehen in den Functions (aktuell 400 Suchen und 60 Scans pro Tag), Überschreitung liefert **429** mit Klartext-Meldung.
- Kein Store-Field, kein Sync, nicht im Backup (reiner Server-Zähler, `EXCLUDED` in `tools/check-backup-coverage.cjs`). Alte Zeilen werden bewusst nicht aufgeräumt: es sind wenige Zeilen je aktivem User und Tag, und sie machen einen Ausreißer im Nachhinein nachvollziehbar.

### `zane_cardio_logs`

- `id` (text), `user_id` (uuid), `date` (text, YYYY-MM-DD), `type` (text, nullable), `duration_minutes` (int), `distance_m` (numeric, nullable), `pace_feeling` (int 1-6, nullable), `effort` (int 1-10, nullable), `note` (text, nullable), `created_at` (timestamptz)
- `session_id` (text, nullable): verknüpft einen Cardio-Log, der als Teil einer Training-Session geloggt wurde, mit seiner `zane_sessions`-Zeile

### `zane_cardio_plans`

- `id` (text), `user_id` (uuid), `name` (text), `activity_type` (text: 'running'|'walking'|'cycling'|'swimming'|'rowing'|'elliptical'|'hiking'), `archived` (boolean, default false), `mode` (text: 'manual'|'goal'), `created_at` (timestamptz)
- `days` (jsonb: `{ mon: true, wed: true, ... }`)
- `manual_targets` (jsonb, nullable: `{ mon: { target_type, distance_m, duration_minutes }, ... }`)
- `goal` (jsonb, nullable: `{ type: 'distance'|'pace', target_distance_m, target_duration_minutes }`), `goal_due_date` (date, nullable)
- `start_fitness` (jsonb, nullable: `{ distance_m, duration_minutes, pace_s_per_km }`)
- `generated_weeks` (jsonb, nullable: Array von `{ distance_m, duration_minutes, pace_s_per_km }`, indiziert nach Woche)
- `plan_start_date` (date, nullable: Start des Goal-Plans)
- Store field: `store.cardioPlans` (camelCase-Mapping). Migration 0094.

### `zane_daily_logs`

Eine Zeile pro Tag, Quelle für den Health-Tab. UNIQUE (user_id, date). Migration 0069.

- `id` (text), `user_id` (uuid), `date` (text, YYYY-MM-DD), `weight` (numeric, nullable), `steps` (int, nullable), `calories` (int, nullable), `protein` (int, nullable), `carbs` (int, nullable: immer **total** carbs), `fat` (int, nullable), `water_ml` (int, nullable), `note` (text, nullable), `created_at` (timestamptz)
- `fiber` (int, nullable): nur im Net-Carb-Modus gesetzt; Kalorien dann = `(protein + carbs - fiber)×4 + fat×9`. Migration 0073.
- `adherence` (numeric, nullable): Makro-Adherence-% persistiert **zum Speicherzeitpunkt**; auf Total-Carbs gerechnet, fiber beeinflusst sie nicht.
- `targets_snap` (jsonb, nullable): `{ protein, carbs, fat, calories, dayType }` Snapshot, damit eine spätere Target-Änderung nie vergangene Adherence umschreibt. Gilt nur für **vergangene** Tage: der heutige Tag ist von diesem Freeze bewusst ausgenommen, sowohl beim Lesen (`dayTarget` in `screens-food.jsx`) als auch beim Schreiben (Food-Reconciler-Effect in `screens-health.jsx`), beide ignorieren einen für `date === today` evtl. schon vorhandenen Snapshot und rechnen live gegen `effectiveTargets`/`macroTargets` weiter. Ohne diese Ausnahme friert das erste beliebige Ereignis, das den Tag heute überhaupt scort, das Target fürs erste dort Berechnete ein, jede spätere Live-Änderung am selben Tag hätte dann bis Mitternacht keine Wirkung mehr, weil `LB.dailyLogAdherence`s `dayTargetOverride` einen einmal vorhandenen Snapshot immer gewinnen lässt. Root Cause eines real aufgetretenen Falls (Ergebnis: Coach-/Self-Coaching-Makros haben nie gewonnen, obwohl aktiv und unverändert): `coachingId` steht dank `store.coaching` schon beim Boot fest, `coachingMacros` selbst ist aber ein separater Fetch, den `HealthScreen`/`FoodScreen` erst danach feuern (eigener `useEffectH`/`useEffectFd`) und der eine Weile braucht. Liefen die beiden `targetsSnap`-schreibenden Reconciler-Effects (der Food-Touched-Dates-Reconciler oben und der zweiseitige Day-Type-Heal weiter unten, beide `screens-health.jsx`) in diesem Fenster, rechneten sie mit `coachingMacros === null`, also exakt so, als gäbe es keine Coach-/Self-Coaching-Makros, wodurch das personal target (z.B. ein frisch applyter Weekly-Check-in) per Default gewann und für einen bereits vergangenen Tag dauerhaft eingefroren wurde, obwohl die echten (unveränderten) Coaching-Makros Sekundenbruchteile später korrekt nachgeladen wurden. Fix: beide Effects gaten jetzt zusätzlich auf `coachingId && !coachingMacrosLoaded` (early return, kein Scoring) und laufen ihre eigentliche Logik erst, sobald der Fetch settled ist. `dayType` (`'training'`|`'rest'`) trägt zusätzlich den **Flex-Plan-Tagestyp-Override**: ein Flex-Plan hat keine programmierten Rest-Tage, `isTrainingDayForDate` default't ihn deshalb auf **rest** ("earn it") und zählt einen Flex-Tag nur als Training, wenn eine Session geloggt ist oder der User proaktiv Training gesetzt hat. Die Wahl sitzt auf einem kleinen **Training | Rest**-Slider (fa-dumbbell/fa-bed) im **Health-Tab-Header** (`HealthDateStrip`, mittig zwischen Kalender-Icon und LOG, nur Flex, ausgeblendet während eines Status-Tags oder sobald eine Session geloggt ist, nur im User-Tab via `setStore`-Prop). Er schreibt **sofort** in diese Spalte (ein inhaltsloser Rest-Override wird verworfen, da rest der Default ist; ein inhaltsloser Log lässt den "logged"-Marker im Header nie aufleuchten). `LB.flexDayTypeOverride(state, date)` liest ihn zurück. Cycle-/Weekday-Pläne behalten die optimistische "Plan wird befolgt"-Annahme (geplanter Trainingstag heute/zukünftig = training) und ignorieren den Flex-Override. Ein **zweiseitiger Daily-Log-Heal** (`screens-health.jsx`) gleicht vergangene Tage ab: **Downgrade** training→rest, wenn ein training-getaggter Tag keine Session hatte (Cycle/Week: geplanter Tag geskippt, oder Flex: proaktives Training nicht durchgeführt), und **Upgrade** rest→training (alle Modi), wenn ein rest-getaggter Tag eine geloggte Session bekam (inkl. Freestyle-Session an einem Rest-Tag); die beiden Mengen sind disjunkt, es gibt also keine Oszillation.
- `off_plan_note` (text, nullable): täglicher Off-Plan-Hinweis; store field `offPlanNote`; `dailyLogsWeekPrefill` akkumuliert alle Tages-Notizen mit "DD.MM.YYYY - "-Präfix in `off_plan_notes`. Migration 0079.
- `meal_of_choice` (boolean, nullable, default false, Migration 0211): der Tag ist ein bewusst deklarierter **Meal-of-Choice-Tag** (eine Mahlzeit nimmt auf, was an Makros übrig ist, der Rest des Tages wird leicht und proteinlastig gegessen). Ein solcher Tag wird **nicht bewertet**: `adherence` ist `null` und der Tag fällt damit aus jedem Schnitt, genau wie ein Sick-/Vacation-Tag. **Anders als dort bleibt `targets_snap` erhalten**, denn ein Joker-Tag ist ein ganz normaler Trainings- oder Ruhetag und ein Flex-Plan liest seinen Tagestyp aus genau diesem Feld zurück (der Training|Rest-Slider bleibt sichtbar, anders als an Status-Tagen). Die Sperre sitzt in `LB.dailyLogAdherence`, das Flag steht auf der Zeile, die die Funktion ohnehin bekommt. Der **Name** der Mahlzeit liegt in `off_plan_note`, nicht hier: das ist der Weg, auf dem der Coach ihn sieht (via `dailyLogsWeekPrefill` → `off_plan_notes` im Check-in), und zwei beschreibbare Kopien desselben Strings würden auseinanderlaufen. `null` und `false` heißen beide „nicht markiert"; nullable ist die Spalte, damit der Sync-RPC „alter Client hat den Key nicht geschickt" von „User hat entmarkiert" unterscheiden kann. Die Anzahl je Woche wird angezeigt (`LB.mealOfChoiceWeekCount`), aber **nie erzwungen**. Store field `mealOfChoice`.
- `meal_of_choice_hour` (smallint, nullable, Migration 0212): die Stunde (0-23), fuer die die Meal-of-Choice-Mahlzeit geplant ist, damit die abgeleitete Zeile in der Timeline **in ihrer Mahlzeiten-Kategorie** sitzt statt in einer freistehenden Karte darueber. Ohne sie lag die unbestaetigte Mahlzeit woanders als der daraus entstehende Eintrag. `null` heisst „nicht gewaehlt", die UI setzt dann einen Default. Nur sinnvoll solange `meal_of_choice` true ist. Store field `mealOfChoiceHour`.
- `food_day_closed` (boolean, nullable, default false, Migration 0263): expliziter Tagesabschluss des Food Trackers. Heute wird erst in Weekly Adherence berücksichtigt, wenn der User im Food-Hero „Done for today" bestätigt hat; das ist bewusst keine automatische Prüfung aller konfigurierten Meal-Kategorien, weil Kategorien optional sein können. Offene Plan-Einträge bleiben sichtbar, die Bestätigung fragt bei ihnen nach. Der `meal-reminder` überspringt geschlossene Tage auch dann, wenn geplante Karten noch sichtbar sind. `null` und `false` bedeuten beide „nicht abgeschlossen". Store field `foodDayClosed`.
- `waist_cm`/`hips_cm`/`chest_cm`/`arm_cm`/`thigh_cm`/`calf_cm` (numeric, alle nullable, Migration 0248): Körpermaße in cm (Taille, Hüfte, Brust, Oberarm, Oberschenkel, Wade), pro Tag im BODY-Abschnitt des Daily Logs eintragbar und auf der Body-Measurements-Karte des Health-Tabs chartbar (Dropdown-Metrik-Umschalter, Trend-Overlay). Store fields `waistCm`/`hipsCm`/`chestCm`/`armCm`/`thighCm`/`calfCm`. **Bewusst reminder-frei:** kein Reminder liest oder gate-t diese Spalten, der Daily-Log-Reminder prüft ausschließlich `weight`.
- `body_fat_pct` (numeric, nullable, Migration 0248): Körperfettanteil in %, wie die cm-Maße. Store field `bodyFatPct`. BMI wird clientseitig abgeleitet (Gewicht in kg ÷ (Größe in m)², Größe aus `settings.macroCalc.heightCm`), keine eigene Spalte.
- `daily_coach_fields` (jsonb, nullable): beliebige key→value-Map für coach-konfigurierte tägliche Tracking-Felder; Keys matchen `checkin_schema`-Feld-Keys mit `show_in_health_log: true`. Store field `coachFields`. Migration 0078.
- `updated_at` (timestamptz, default now()): Staleness-Guard für den Multi-Device-Upsert; store field `updatedAt`, bei jedem Save gesetzt. Migration 0096.
- `ai_summary` (text, nullable), `ai_summary_generated_at` (timestamptz, nullable): AI-generierte (Qwen als Primary, Claude als automatischer Fallback bei Nichterreichbarkeit, `callModelWithFallback` in `_shared/ai.ts`, kein Reasoning) Kurzeinordnung des Tages, per Button auf der Health-Tab-Karte `AiSummaryCard` einmal am Tag anstoßbar (nicht automatisch, User-Aktion). **Server-authored only**: geschrieben ausschließlich von der Edge Function `ai-daily-summary` (Service-Role-Upsert, `on_conflict=user_id,date` unter Beibehaltung der bestehenden `id`), nie vom normalen Client-Sync-Pfad. Beide Spalten fehlen deshalb bewusst in `sync_daily_logs_batch`s `ON CONFLICT DO UPDATE SET`-Liste (siehe unten): Postgres überschreibt dort nur explizit genannte Spalten, ein Edit an einem anderen Feld desselben Tages kann diese beiden also nie clobbern. `ai_summary_generated_at` ist zugleich das Once-per-day-Gate: gesetzt heißt „für diesen Tag schon generiert", die Edge Function ruft dann nicht erneut das Modell auf. Seit Migration 0226 ist „server-authored only" zusätzlich per BEFORE-UPDATE-Trigger (`zane_daily_logs_guard_ai_summary`) durchgesetzt, nicht mehr nur per Konvention: `"Users manage own daily logs"` ist eine breite FOR-ALL-Policy ohne Spalten-Einschränkung, ohne den Trigger könnte ein User beide Spalten direkt per PostgREST-PATCH auf beliebigen Text setzen. Der Trigger blockt jeden Write mit `auth.uid() IS NOT NULL` (eine echte User-Session); der Service-Role-Write der Edge Function (dort ist `auth.uid()` NULL) bleibt unangetastet. Quota über `bump_api_usage`/`zane_api_usage` (kind `'daily_summary'`), fail-open wie beim Label-Scanner. Der Button erscheint nur, wenn der Vortag überhaupt Daten hat (`LB.dailySummaryDayIsEmpty`); auf der Coach-Ansicht ist die Karte reines Read-only ohne Button, ein Coach-Klick würde sonst serverseitig als der Coach selbst aufgelöst und in die falsche Zeile schreiben. Store fields `aiSummary`/`aiSummaryGeneratedAt`. Migration 0224.

Sync-Verhalten:

- **Sync via `sync_daily_logs_batch`-RPC** (kein plain Upsert): löst Konflikte auf (user_id, date) unter Beibehaltung der bestehenden id und überschreibt nur, wenn das eingehende `updated_at` neuer ist. So kollidieren zwei Geräte, die denselben Tag loggen, nicht auf UNIQUE(user_id, date), und ein staler Offline-Edit kann keinen neueren Stand clobbern (Migration 0096). `food_day_closed` ist ein normaler user-owned Sync-Wert und bleibt deshalb in INSERT, SELECT und `ON CONFLICT DO UPDATE` der RPC-Spaltenliste.
- Der Cache-first-Merge in `app.jsx` dedupliziert Daily Logs zusätzlich **per Datum** (Server gewinnt), damit eine vor der RPC entstandene divergente id nicht als doppelter Tag erscheint.
- RLS: eigene Zeilen + Coach-of-Client-Reads (so füllt `loadClientStore` das `clientStore.dailyLogs` für den Coach-„Daily"-Tab, keine extra RPC).

### `zane_adaptive_tdee_history`

Historie der automatisch berechneten TDEE-Werte (Migration 0260). Die Werte
werden nicht aus einem TDEE-Textfeld in den Settings gelesen, sondern bleiben
aus zane_daily_logs nachrechenbar. Die Tabelle hält zusätzlich fest, was der
Nutzer mit dem Ergebnis gemacht hat, damit ein späterer Check-in den vorherigen
Stand nicht unlesbar macht. Eine Zeile je User und Berechnungstag.

- id (text, deterministisch aus User und as_of_date), user_id (uuid)
- as_of_date (date), window_start/window_end (date): lokaler Berechnungstag und das verwendete 14-Tage-Fenster
- tdee_kcal/avg_calories_kcal (integer): gelöster Erhaltungsbedarf und durchschnittliche Kalorienaufnahme
- weight_start_kg/weight_end_kg/weight_change_kg (numeric): Mittelwerte der ersten und zweiten Hälfte der Wiegedaten sowie deren Veränderung
- weight_rate_kg_week (numeric), day_span (integer), calorie_days/weigh_ins (integer): Wochenrate und Transparenzwerte zur Datenbasis
- decision (applied | skipped | reconstructed): Apply, Skip oder aus den vorhandenen Logs nachgebaut
- source (live | reconstructed): beim Check-in live berechnet oder nachträglich rekonstruiert
- targets_snapshot (jsonb, nullable): eingefrorener Check-in-Vorschlag mit Training/Rest-Zielen, Wochenmittel, deltaKcal, Ziel und Rate; decision zeigt, ob er angewendet oder übersprungen wurde
- Ältere Snapshots ohne Wochenmittel werden beim Laden kompatibel ergänzt und einmalig zurückgeschrieben; die ursprüngliche Entscheidung und der Live-/Rebuilt-Status bleiben dabei erhalten.
- calculated_at, decided_at, created_at, updated_at (timestamptz)
- Store field: store.adaptiveTdeeHistory. Die bestehende zane_user_settings.macro_calc-Shape bleibt der aktuelle Formular-/Check-in-Zustand; die Historie wird nicht in dieses JSONB hineingeschrieben.
- RLS: ausschließlich eigene Zeilen, kein Coach-Zugriff. Data-API-Rechte sind ausdrücklich auf authenticated beschränkt.
- Backup: Teil des persönlichen User-Backups. Beim Restore wird die deterministische ID für den Ziel-User neu gebildet.

Die erste historische Zeile kann beim Laden aus lastCheckinAt/lastAppliedAt
und den Daily Logs rekonstruiert werden. Bei jedem weiteren Apply oder Skip
wird der bisherige Check-in vor dem Überschreiben der Settings gesichert. Die
Rekonstruktion kennt den Berechnungstag, aber keinen exakten alten Zeitstempel;
solche Zeilen tragen deshalb source = reconstructed.

### `zane_skips`

- `id` (text), `user_id` (uuid), `date` (text), `day_id` (text), `day_name` (text), `skip_reason` (text), `skipped_at` (timestamptz)

### `zane_status_periods`

Historie der Sick/Vacation/Deload/Cleanup-Phasen. Store field: `store.statusPeriods`. Migration 0083.

- `id` (text), `user_id` (uuid), `mode` (text: 'sick'|'vacation'|'deload'|'cleanup'), `started_at` (timestamptz), `ended_at` (timestamptz, nullable: null = aktuell aktiv)
- **Deload** (Migration 0108, Overlay-Modell) nutzt diesen Mechanismus mit `mode='deload'`: der Cycle läuft normal weiter, aber `buildSeedSets` befüllt Lasten mit ~50% vor (via `window.__DELOAD`-Global, gespiegelt aus `statusMode` in `app.jsx`), der Home-Strip-Titel zeigt `DELOAD`, der Training-Header ein `DELOAD · 50%`-Badge, und geloggte Sessions werden `is_deload` geflaggt. Start/Ende via `LB.startDeload`/`LB.endDeload`; endet automatisch nach einem Cycle/einer Woche (bzw. für Flex: dem Wochen-Session-Ziel an Deload-Sessions) via `LB.deloadElapsed`, geprüft auf dem Home-Screen. Die Plan-Tab-Karte hat den Toggle-Button; ein 8-Wochen-Nudge (verankert an `deload_prompt_dismissed_at`) bietet einen Deload an.
- **Cleanup Week** (Migration 0251, Overlay-Modell wie Deload) nutzt denselben Mechanismus mit `mode='cleanup'`: eine bewusste Technik-Aufbauwoche, die Lasten auf `(100 - cleanup_percent)%` vorbefüllt (einstellbar 10-30, Default 20, via `window.__CLEANUP`-Global aus `app.jsx`) statt auf die fixen 50% eines Deloads. **Der entscheidende Unterschied zum Deload:** die reduzierten Gewichte fließen in die Seed-Kette, die Folgewoche baut also darauf auf. Damit die Woche selbst nicht compoundet (jede Session von der vorigen reduzierten), blenden `recentSessionsForExercise`/`fetchSeedEntries` während `statusMode === 'cleanup'` alle Sessions ab `status_mode_since` aus der Seed-Historie aus; nach dem Auto-Ende sind sie die neue Basis. Cleanup-Sessions behalten bewusst `signal_weight='full'` (sonst läse die Wiederaufbauwoche als e1RM-Regression gegen die Pre-Cleanup-Exposures und könnte einen Emergent-Deload auslösen); ausgeschlossen werden stattdessen gezielt Earn/Cut, rep-miss-Streak, PR-Baselines, `detectStall` und die Live-Recovery-Fragen, alle über das `is_cleanup`-Flag. Pro Übung kann der Nutzer während des Trainings auf volle Last zurückschalten (Chip im Exercise-Header, gerätelokal). Start/Ende via `LB.startCleanup`/`LB.endCleanup`, Auto-Ende nach einem Cycle/einer Woche via `LB.cleanupElapsed`. Umsetzungsdetails: `docs/cleanup-week.md`.
- RLS: eigene Zeilen + Coach-of-Client-Reads (Migration 0084, damit `computeWeeklyAdherence` Sick-/Vacation-Tage aus dem Trainings-Adherence-Score des Clients ausschließen kann).
- Spiegel von `zane_user_settings.status_mode`/`status_mode_since` (das ist der schnelle Current-State-Cache; diese Tabelle ist die volle Historie für Stats). Genutzt von den StatsTab-Konsistenzkarten "Missed Workouts" / "Of Which Sick/Away". Geschrieben von `openStatusPeriod`/`closeStatusPeriod`/`updateStatusPeriodStart` in `store.js`.

### `zane_user_settings`

Eine Zeile je User. Neue Settings immer an den drei Stellen in `store.js` ergänzen (siehe CLAUDE.md, Abschnitt „Store").

Basisspalten: `user_id` (uuid), `active_schedule_id` (text), `active_meal_template_id` (text, Plan-Mode aktiver Meal-Plan-Zeiger, Store `activeMealTemplateId`, Migration 0199), `cycle_index` (int), `cycle_start_date` (text), `last_advanced_date` (date), `week_plan_start_date` (date), `in_progress_session_id` (text), `unit` (text), `rest_default`/`rest_big`/`rest_medium`/`rest_small` (int), `push_enabled` (boolean), `pushover_user_key` (text), `cycle_week_view` (boolean), `accent_color` (text), `dark_mode` (text), `tempo_enabled` (boolean), `tempo_eccentric` (numeric), `tempo_concentric` (numeric), `smart_progression` (boolean), `progression_range_top` (int), `equipment_config` (jsonb), `custom_day_types` (text[]), `reminder_enabled` (boolean), `reminder_time` (text, HH:MM), `next_reminder_at` (timestamptz), `show_warmup_in_summary` (boolean), `show_coaching_tab` (boolean), `be_your_own_coach` (boolean), `session_timeout_minutes` (int, default 90)

**RLS:** eigene Zeile voll (`FOR ALL`) · ein aktiver Coach (`zane_is_coach_of`) darf eine Client-Zeile lesen, updaten **und** inserten (Migration 0163; die INSERT-Policy fehlte ursprünglich als einzige der Coach-Client-Tabellen, obwohl `syncStore`s `upsert()` sie auch für den reinen Update-Fall braucht, Postgres prüft bei `ON CONFLICT DO UPDATE` immer erst die INSERT-Policy). Praktisch relevant für Coach-Writes wie „Push to client" (`PlanViewerScreen`), die `active_schedule_id`/`cycle_index`/`cycle_start_date`/`week_plan_start_date` fürs Aktivieren eines Plans setzen.

Weitere Spalten:

- `use_pushover` (boolean, default false): wenn true und ein `pushover_user_key` gesetzt ist, gehen Rest-Timer-Notifications via Pushover statt Web Push. Store field `usePushover`. Migration 0081.
- `auto_close_notify` (jsonb, nullable): `{ dayName, date, durationMinutes }`, von der Edge Function geschrieben, von der App beim ersten Lesen gecleart.
- `macro_targets` (jsonb, nullable): persönliche Health-Tab-Targets `{ proteinTraining, carbsTraining, fatTraining, caloriesTraining, proteinRest, carbsRest, fatRest, caloriesRest }`. Store field `macroTargets`.
- `macro_calc` (jsonb, nullable, Migration 0205): die letzten Eingaben des Ziel-Rechners (`MacroTargetSheet` → „Estimate targets"), `{ birthYear, heightCm, sex, activity, goal, rateKgPerWeek, trainingDays, weightKg, lowFat, fatPerKg, proteinFixed, proteinG, fatFixed, fatG, restRatioPct, checkinEnabled, lastCheckinAt, lastAppliedAt, lastAppliedTargets }`. **Diese Liste hier ist die Pflege-Quelle der Shape**, der Spalten-Kommentar in der DB verweist bewusst nur noch hierher (er war zweimal in zwei Migrationen veraltet). Reines Prefill, damit ein erneutes Öffnen nicht bei null anfängt: das **Ergebnis** landet ganz normal in `macro_targets`, diese Spalte merkt sich nur, wie es zustande kam. Bewusst ein jsonb statt sechs Spalten (ein Formularzustand, wird als Ganzes gelesen/geschrieben, nie nach einzelnen Feldern gefiltert, und der Fragenkatalog ist eine Heuristik und darf sich ändern). Das Gewicht kommt bei jeder Berechnung frisch aus dem letzten Daily Log; `weightKg` ist nur der **Fallback** für Nutzer, die noch nie eins geloggt haben (ein geloggtes Gewicht gewinnt beim Öffnen immer). `lowFat`/`fatPerKg` sind die Low-Fat-Option: ein **Zielwert** für Fett in g je kg Körpergewicht (lbs-Nutzer sehen den Faktor je lb, gespeichert wird immer je kg), die frei werdenden Kalorien gehen in die Kohlenhydrate. Der Fett-Boden `LB.FAT_FLOOR_PER_KG` (0,5 g/kg) gilt weiterhin für die **automatische** Aufteilung; ein vom User gesetzter Faktor darunter wird respektiert, das Sheet warnt aber sichtbar. `proteinFixed`/`proteinG` und `fatFixed`/`fatG` sind je eine dritte Alternative neben der automatischen Aufteilung (bzw. neben `lowFat`/`fatPerKg` bei Fett): ein fixer Gramm-Wert statt einer Formel, `proteinFixed`/`fatFixed` (boolean) schaltet den jeweiligen Modus ein, `proteinG`/`fatG` (Zahl, nullable) ist der fixe Zielwert in g, nur gültig wenn das zugehörige `*Fixed` an ist. `fatFixed` und `lowFat` schließen sich gegenseitig aus (UI erzwingt das, siehe `screens-health.jsx`s `fatModeUI`). `restRatioPct` ist die Trainings-/Ruhetag-Ratio als Slider-Wert (Ruhetag-Kalorien in Prozent eines Trainingstags): `null` heißt „automatische Aufteilung", die zugleich die schärfste wählbare ist und deshalb den Slider-Boden bildet (`LB.minRestRatio`, nur vom Trainingstage-Zähler abhängig), 100 gibt beiden Tagestypen dieselben Kalorien. Die Wochensumme bleibt bei jeder Ratio gleich, der Wert verschiebt die Kalorien nur **innerhalb** der Woche. `checkinEnabled` (boolean, fehlt/false = aus) und `lastCheckinAt` (ISO-Datumsstring `YYYY-MM-DD`, nullable) gehören zum wöchentlichen adaptiven Check-in (`MacroSourceCard`/`WeeklyCheckinSheet`, `LB.estimateAdaptiveTdee`): expliziter Opt-in des Users, `lastCheckinAt` wird sowohl bei „Apply" als auch bei „Skip for now" auf heute gesetzt (beides zählt als „diese Woche erledigt", das ist es, was das Nagging stoppt, nicht ob sich die Zahlen tatsächlich geändert haben). `lastAppliedAt` (ISO-Datumsstring, nullable) wird dagegen nur bei „Apply" gesetzt, nie bei „Skip", zusammen mit `lastAppliedTargets` (Objekt, nullable, ein Snapshot der beim Apply tatsächlich übernommenen Werte, Shape `{ caloriesTraining, caloriesRest, proteinTraining, proteinRest, carbsTraining, carbsRest, fatTraining, fatRest }`): die Karte zeigt daraus „vor X Tagen zuletzt angewendet" plus die damaligen Zahlen, bewusst aus diesem eingefrorenen Snapshot statt den (mittlerweile eventuell abweichenden) live `macro_targets`. Nur relevant, solange kein Coach-Macro-Target aktiv ist (`coachHasMacros`), sonst gar nicht erst angeboten. Shape-Ergänzungen dokumentiert in den Migrationen 0209 und 0210 (nur Kommentar, jsonb bleibt unverändert); `checkinEnabled`/`lastCheckinAt` selbst brauchten keine weitere Migration (reine Shape-Doku-Pflege hier, kein Schema-Touch, siehe 0210s eigener Zweck), ebenso wenig `proteinFixed`/`proteinG`/`fatFixed`/`fatG`/`lastAppliedAt`/`lastAppliedTargets`. Rechenlogik: `LB.estimateTdee` (Mifflin-St Jeor) und `LB.macroTargetsFromGoal` in `store.js`, unit-getestet in `tools/test/store.test.cjs`. Store field `macroCalc`.
- `meal_windows` (jsonb, nullable, Migration 0206): die sechs aufsteigenden **Startstunden** der Mahlzeiten-Kategorien des Food Trackers (Breakfast / Snack 1 / Lunch / Snack 2 / Dinner / Snack 3), erster Wert immer `0`. Jede Kategorie läuft bis zur Startstunde der nächsten, die letzte bis 24. Bewusst sechs Zahlen statt sechs `{start,end}`-Paaren: so lässt sich eine Lücke oder Überlappung gar nicht erst abbilden. `null` = die eingebauten Defaults `[0, 9, 11, 13, 16, 20]` (der vorherige, fest verdrahtete Stand). Aufgelöst über `LB.mealCategories(settings)` in `store.js`, das einen kaputten gespeicherten Wert auf die Defaults zurückfallen lässt. Editor in Settings → Health → Food. Store field `mealWindows`.
- `meal_categories` (jsonb, nullable, Migration 0261): frei konfigurierbare Food-Tracker-Kategorien als Array `[{ id, label, startHour }]`. `id` bleibt stabil, `label` ist der sichtbare Name, `startHour` ist die inklusive Startstunde. Die erste Kategorie muss bei `0` beginnen, alle weiteren Startstunden müssen streng aufsteigend und innerhalb `0..23` sein; jede Kategorie läuft bis zur nächsten, die letzte bis `24`. `null` nutzt die eingebauten Kategorien beziehungsweise bei älteren Accounts deren `meal_windows`-Zeitgrenzen. Der Editor in Settings → Health → Food erlaubt Umbenennen, Hinzufügen, Löschen und Verschieben der Startstunden. Food Logs speichern weiterhin ihre echte Uhrzeit und werden nur anhand der aktuellen Benutzerkonfiguration gruppiert. Store field `mealCategories`.
- Water-Tracker-Config (Migration 0180): `water_goal_ml` (int, default 2000, Store `waterGoalMl`), `water_start_time` (text, default '08:00', Store `waterStartTime`), `water_end_time` (text, default '22:00', Store `waterEndTime`), `water_bottles_today` (int, default 0, Store `waterBottlesToday`) und `water_bottles_date` (text, nullable, Store `waterBottlesDate`): Ziel plus Tages-Zeitfenster für die Soll-Kurve und die "aktuelle Flasche"-Zählung (per Tag zurückgesetzt). Alle vier Store-Stellen wie üblich. Boot-Merge: Goal/Start/End sind base-aware (propagieren geräteübergreifend wie die Plan-Position-Felder); die Bottle-Zähler bleiben gerätelokal.
- `water_drinks` (jsonb, nullable, Store `waterDrinks`): bis zu 6 nutzerdefinierte Getränke `[{ name, ml, icon, color }]` für die "Other drinks"-Buttons im Water-Tracker (keine Presets); `icon` (FA6-Klasse) und `color` (Hex) sind optional, fehlen sie fällt die UI auf einen Default-Icon/-Farbton zurück. Migration 0181.
- `water_bottle_enabled` (boolean, default true, Store `waterBottleEnabled`) und `water_bottle_ml` (int, default 1500, Store `waterBottleMl`): schaltbarer und in der Größe konfigurierbarer "aktuelle Flasche"-Zähler. Migration 0181.
- `water_coffee_sizes` (jsonb, nullable, Store `waterCoffeeSizes`): nutzerkonfigurierbare Kaffee-Größen `[{ label, ml }]` für den Kaffee-Preset-Button (Größe + Milch-Flow); null = eingebaute Defaults. Migration 0181.
- Water-Reminder (Migration 0182): `water_reminder_enabled` (boolean, default false, Store `waterReminderEnabled`): eigener An/Aus-Schalter für die "du liegst zurück"-Pushes (nutzt die bestehende Push-Infra, zusätzlich `push_enabled` nötig). `water_last_push_at` (timestamptz, nullable): Throttle, server-geschrieben von der `water-reminder` Edge Function (nicht im Backup, allowlisted). `tz_offset_minutes` (int, nullable, Store `tzOffsetMinutes`): UTC-Offset des Nutzers in Minuten, vom Client gesetzt, damit ältere bzw. noch nicht aktualisierte Zeilen eine Fallback-Uhr haben (nicht im Backup, allowlisted). `time_zone` (text, nullable, Store `timeZone`, Migration 0259): IANA-Zeitzone wie `Europe/Vienna`, vom Client gesetzt und von allen Reminder-Crons bevorzugt verwendet, damit Sommer-/Winterzeit und Reisen korrekt bleiben (nicht im Backup, allowlisted).
- `meal_reminder_enabled` (boolean, NOT NULL, default false, Store `mealReminderEnabled`, Migration 0201): An/Aus-Schalter für Plan-Mode-Meal-Reminder (Toggle in Settings → Health unter „Meal planning", nur sichtbar wenn `plan_mode` an; zusätzlich `push_enabled` nötig). Die `meal-reminder` Edge Function (pg_cron stündlich `0 * * * *`, gated auf `meal_reminder_enabled` + `plan_mode` + `push_enabled`) prüft je Nutzer die heutigen und gestrigen noch-`planned` (nicht abgehakten) `zane_food_logs`-Einträge und feuert einen Push, sobald eine geplante Mahlzeit 1h nach ihrer Uhrzeit noch offen ist. Migration 0259 führt dafür `zane_meal_reminder_deliveries` als service-role-only Zustandsbuch ein: nach einem fehlgeschlagenen Provider-Handoff bleibt die Mahlzeit retrybar, nach erfolgreichem Handoff wird sie nicht erneut gesendet. Die Retry-Frist beträgt einen lokalen Tag. Das Zustandsbuch ist abgeleiteter Versandstatus und nicht Teil des User-Backups. Im Backup bleibt nur die Präferenz `meal_reminder_enabled`.
- `meds_enabled` (boolean, NOT NULL, default false, Store `medsEnabled`, Migration 0218): Feature-Master-Switch für Medications, zugleich der „Show tab"-Schalter für den Medications-Slot (Toggle in Settings → Health & Nutrition → Medications, off by default: „nicht jeder will was mit Medikamenten zu tun haben"). Aus: der Medications-Slot im Health/Water/Food/Medications-TabBar-Zyklus (`ui.jsx`) ist nicht vorhanden, alles andere unverändert. Siehe `show_health_tab`/`show_water_tab`/`show_food_tab` unten: alle vier sind seit Migration 0222 unabhängig voneinander schaltbar.
- `medication_reminder_enabled` (boolean, NOT NULL, default false, Store `medicationReminderEnabled`, Migration 0218): An/Aus-Schalter für Medications-Dosis-Reminder (zusätzlich `meds_enabled` + `push_enabled` nötig). Die `medication-reminder` Edge Function (pg_cron stündlich `0 * * * *`, Migration 0219) ist 1:1 nach Vorbild von `meal-reminder` gebaut: prüft je Nutzer die heutigen noch-`planned` `zane_medication_logs`-Einträge und feuert einen Push, sobald eine geplante Dosis 1h nach ihrer Uhrzeit noch offen ist, state-basiert seit Migration 0246 (erster Nudge catch-up-sicher nach 1h Grace, zweiter nach 2h, gedeckelt auf 2 pro Tag, per-Dosis-Snooze, siehe `zane_medication_logs`-Abschnitt und Reminder-Absatz in der Medications-Übersicht).
- Daily-Log-Reminder (Migration 0247): `daily_log_reminder_enabled` (boolean, NOT NULL, default false, Store `dailyLogReminderEnabled`): An/Aus-Schalter für die „Gewicht noch nicht geloggt"-Nudges (zusätzlich `push_enabled` nötig). `daily_log_reminder_time` (text, NOT NULL, default '19:00', Store `dailyLogReminderTime`): Uhrzeit (HH:MM) in der lokalen Zeit des Users, ab der der Nudge feuern darf. `daily_log_reminder_last_date` (text, nullable): Tages-Throttle, server-geschrieben von der `daily-log-reminder` Edge Function (lokales Datum des letzten Nudges, null = noch nie; nicht im Backup, allowlisted, der Client mappt es nie). Die Edge Function (pg_cron `*/15 * * * *`, Migration 0247) prüft je Nutzer das heutige lokale Datum (via `time_zone`, `tz_offset_minutes` nur Fallback für Zeilen ohne gesetzte Zeitzone, siehe `time_zone`-Spalte oben) und feuert genau einmal pro lokalem Tag einen Push, sobald die gesetzte Uhrzeit erreicht ist und `zane_daily_logs.weight` für diesen Tag noch null ist; wird das Gewicht später geloggt, feuert der Nudge erst am nächsten lokalen Tag wieder.
- `fasting_protocol` (text, nullable, Store `fastingProtocol`, Migration 0249): Intermittent-Fasting-Protokoll-Preset, einer der Client-Ids aus `LB.FD_FASTING_PRESETS` (`'16:8'`/`'18:6'`/`'20:4'`/`'omad'` für Tages-Rhythmen, `'36h'`/`'48h'`/`'72h'` für Langfasten ohne Essensfenster (eatH 0, direkt fasting → complete), `'custom:N'` für frei wählbare 24-168h (N = Stunden, im Id-String selbst getragen, kein Schema-Change)), null = Feature aus. Nur die Präferenz synchronisiert; der laufende Zyklus liegt per Gerät in `logbook-fasting-state` (localStorage, siehe `docs/internals.md`). Fasting-Timer-Karte + Timeline-Eating-Window-Tint in `screens-food.jsx` (Log-Tab), On-Ramp in Settings → Health → Food → Intermittent Fasting.
- `pillbox_slots` (jsonb, nullable, Store `pillboxSlots`, Migration 0223): beliebig viele nutzerdefinierte Pillendosen-Fächer `[{ id, label, startHour, endHour }]` (z.B. "Morgens" 6-12, "Abends" 18-22), kein Limit, leer/null bis der User sie in Settings → Health & Nutrition → Medications → Pillbox anlegt. Treibt `WeeklyPrepScreen`s Sieben-Tage-Ansicht: eine fällige Dosis (`zane_medication_schedule_slots.hour`) wird dem Fach mit der frühesten `startHour` zugeordnet, unter das `hour` fällt (Fächer dürfen sich überschneiden); passt sie in kein Fach, landet sie in einem eigenen "Other times"-Fach statt zu verschwinden.
- `show_health_tab` (boolean, default false): eigener „Show tab"-Schalter für den Health-Slot (Settings → Health & Nutrition → Health). Store field `showHealthTab`.
- `show_water_tab` (boolean, NOT NULL, default false, Store `showWaterTab`, Migration 0222): eigener „Show tab"-Schalter für den Water-Slot (Settings → Health & Nutrition → Water). Bis Migration 0222 gab es nur `show_health_tab`, das Health/Water/Food gebündelt ein-/ausblendete; seither ist jeder der vier Slots (Health/Water/Food via diese drei Spalten, Medications via `meds_enabled`) unabhängig schaltbar, jede Kombination erlaubt. Die Migration backfillt bestehende Nutzer mit `show_health_tab = true` auf `true`, damit niemandem sichtbare Tabs beim Deploy verschwinden; neue Accounts starten wie `show_health_tab` auf `false` (Opt-in).
- `show_food_tab` (boolean, NOT NULL, default false, Store `showFoodTab`, Migration 0222): eigener „Show tab"-Schalter für den Food-Slot (Settings → Health & Nutrition → Food), gleiche Backfill-Logik wie `show_water_tab`.
- `show_friends_tab` (boolean, NOT NULL, default false, Store `showFriendsTab`, Migration 0264): opt-in-Schalter für den Friends-Preview-Tab. Friends und Coaching teilen sich einen Navigations-Slot; solange dieser Schalter aus ist, lädt der Client keine Social-Daten oder Realtime-Kanäle.
- `onboarding_completed` (boolean, default false): gesetzt nach Welcome-Tour oder erster Session. Store field `onboardingCompleted`.
- `net_carbs` (boolean, default false): Health-Tab-Carb-Modus, Net-Carb-Tracking ergänzt ein Fiber-Feld. Store field `netCarbs`. Migration 0073.
- `plan_mode` (boolean, NOT NULL, default true): Food-Tracker Plan Mode (Toggle in Settings → Health, **an by default seit Migration 0253**). An: geplante vs. geloggte Food-Einträge (`zane_food_logs.planned`), "Log it"/"Plan it" beim Hinzufügen, Projektions-Zeile im Hero. Aus: der Food-Tracker verhält sich exakt wie vor 0196 (kein Eintrag ist je planned). Store field `planMode`. Migration 0196 (opt-in, default false), Migration 0253 (Default auf true plus Backfill aller bestehenden Zeilen). Der Backfill war bewusst nicht per Nutzer zurücknehmbar: die Spalte ist seit jeher NOT NULL mit explizitem false, „bewusst ausgeschaltet" und „nie angefasst" waren darin nie unterscheidbar. Kein Push-Nebeneffekt, weil `meal_reminder_enabled` ein eigener Opt-in mit Default false bleibt (der Cron gated auf beide plus `push_enabled`).
- `hide_food_categories` (boolean, NOT NULL, default false): Food-Tracker Timeline zeigt eine durchgehende Stunden-0-23-Liste statt der sechs Meal-Kategorie-Header-Cards (Toggle in Settings → Health → Food, off by default). Rein kosmetisch: `LB.mealCategories` bleibt die Grundlage für "Repeat yesterday" und das Manage-Entries-Sheet, jede Stunde (leer oder gefüllt) rendert weiterhin mit eigenem "+", Plan Mode bleibt also unangetastet. Store field `hideFoodCategories`. Migration 0214.
- `status_mode` (text, nullable: 'sick'|'vacation'|'deload'|'cleanup'): schneller Current-State-Cache des aktiven Status-Modus. Store field `statusMode`. Migration 0082, 'deload' ergänzt in 0108 (CHECK-Constraint nachgezogen in 0109), 'cleanup' in 0251.
- `status_mode_since` (timestamptz, nullable): Start des aktuellen Status-Modus. Store field `statusModeSince`. Migration 0082.
- `deload_prompt_dismissed_at` (timestamptz, nullable): Anker für den 8-Wochen-Deload-Nudge; gebumpt, wann immer der Prompt gezeigt/beantwortet wird. Store field `deloadPromptDismissedAt`. Migration 0108.
- `cleanup_percent` (integer, NOT NULL, default 20): um wie viel Prozent eine Cleanup Week die zuletzt geloggte Last reduziert (UI clampt auf 10-30 in 5er-Schritten). Bleibt über Aktivierungen hinweg stehen, die nächste Cleanup Week startet also mit der letzten Wahl. Gelesen von `buildSeedSets` über den `window.__CLEANUP`-Global. Store field `cleanupPercent`. Migration 0251.
- `food_force_grams` (boolean, NOT NULL, default false): Opt-out für imperiale Nutzer (`settings.unit === 'lbs'`), hält den Food-Tracker trotzdem auf Gramm (Portionen, Zutaten, Kochgewichte, Shopping-Mengen, Packungsgrößen). Der globale Unit-Picker steuert Körpergewicht/Wasser/Food-Massen sonst gemeinsam; US-Nährwertlabels sind selbst in Gramm gedruckt, manche Nutzer wollen den Food-Tracker deshalb separat metrisch lassen. Nur relevant bei `unit === 'lbs'`, Toggle im Appearance-Sheet erscheint nur dort. Gelesen über `UI.massInOz()` (`ui.jsx`), das zusätzlich zu `UI.unit() === 'lbs'` dieses Flag prüft. Store field `foodForceGrams`. Migration 0252.
- `active_cardio_plan_id` (text, nullable): id des einen aktuell aktiven Cardio-Plans; nur dieser erscheint auf dem Home-Widget und befüllt Cardio-Logs vor. Store field `activeCardioPlanId`; neue Pläne werden bei Anlage auto-aktiviert. Migration 0095.
- `show_regression` (boolean, default true): wenn false, ist das Regression-Overlay im Training-Screen unterdrückt. Store field `showRegression`. Migration 0100.
- `pin_all_notes` (boolean, default false): globaler Override für gepinnte Notes. Wenn true, verhält sich jede Übung mit nicht-leerer `note` wie gepinnt (das Must-Acknowledge-Sheet poppt beim Exercise-Start), unabhängig vom Übungs-Flag `note_pinned`; wenn false, pinnen nur Übungen mit `note_pinned`. Toggle in Training › Session. Store field `pinAllNotes`. Im User-Backup. Migration 0168.
- `glucose_unit` (text, default 'mmol'): Anzeige-Einheit für Blutzucker, 'mmol' = mmol/L, 'mgdl' = mg/dL; Werte in `zane_glucose_logs` sind immer in mmol/L gespeichert. Store field `glucoseUnit`. Migration 0101.
- `weight_fill_down` (boolean, default true): wenn true, füllt das Editieren eines Set-Gewichts im Training dasselbe Gewicht in nachfolgende noch nicht erledigte Working-Sets. Store field `weightFillDown`.
- `equipment_config` (jsonb, default `{}`): pro-Equipment-Kategorie hinterlegter Schrittweiten-/Deckel-Override, Shape `{ [equipmentKey]: { increment?: number, maxKg?: number } }` (`equipmentKey` ∈ `EQUIPMENT_TYPES` minus `no_equipment`/`bodyweight`). Editiert in Settings → „Equipment setup" (`screens-settings.jsx`). `increment` ist der Fallback-Schritt für Smart Progression/Meso, sobald keine `zane_exercises.progression_increment`-Übung-Override gesetzt ist (siehe dort); muss wie dieser strikt positiv sein, `incrementForExercise` behandelt 0/negativ als nicht gesetzt (kein CHECK möglich, liegt in jsonb, `NumInput`s `positiveOnly` verhindert es im UI). `maxKg` deckelt die von Smart Progression vorgeschlagene nächste Last nach oben. Store field `equipmentConfig`.
- `manual_calories` (boolean, default false): Health-Tab-Modus, Kalorien direkt eingeben statt aus Makros ableiten. Store field `manualCalories`.
- `default_checkin_schema` (jsonb, nullable): wiederverwendbares Default-Check-in-Formular-Schema eines Coaches, angewandt auf neue Coaching-Beziehungen. Store field `defaultCheckinSchema`.
- `vip_background` (text, nullable): admin-vergebener dekorativer Background-Key; gesetzt via `set_user_vip_background`. Store field `vipBackground`. Migration 0103.
- `sw_version` (text, nullable): letzte SW-Cache-Version (z.B. `'v2.445'`), die dieses Gerät beim Boot gemeldet hat, direkt aus dem Cache Storage gelesen. Store field `swVersion`; lässt den Admin erkennen, ob ein User mit Bug-Report auf einem stalen Cache festhängt, ohne ihn nach den Settings fragen zu müssen. Migration 0123.
- `temp_unit` (text, nullable): Anzeige-Einheit für Körpertemperatur, 'c' = Celsius, 'f' = Fahrenheit; Werte in `zane_body_temp_logs` sind immer in Celsius gespeichert. `NULL` = User hat nie explizit gewählt, die App leitet dann den Default aus `unit` ab (`LB.defaultTempUnit`: 'lbs' → F, sonst C, siehe `store.js`). Store field `tempUnit`. Migration 0173, Default entfernt in Migration 0174.
- `hidden_health_cards` (jsonb, nullable): Array von Card-Ids, die der User im Health-Tab ausgeblendet hat (z.B. `["cardio","glucose"]`). Anders als die Card-**Reihenfolge** (`logbook-health-card-order`, per-device localStorage) ist die Sichtbarkeit eine echte, geräteübergreifend synchronisierte Einstellung. Store field `hiddenHealthCards`. Migration 0173.
- `fever_threshold_c` (numeric, default 38): Schwellwert in Celsius für den "Als Sick markieren?"-Prompt nach dem Loggen einer Körpertemperatur (`>= fever_threshold_c` bei `date === heute` löst den Prompt aus). Storage immer in Celsius, unabhängig von `temp_unit`; die Settings-UI (Body Temperature Sheet) rechnet für Anzeige/Eingabe live in die aktuell gewählte Einheit um (°F-Nutzer sehen/tippen °F), speichert aber immer den umgerechneten Celsius-Wert zurück. Store field `feverThresholdC`. Migration 0175.
- `watermark_opacity` (integer, nullable): User-Override für die Opacity des Home-Screen-Wasserzeichens (Default-ZANE-Logo oder admin-vergebenes VIP-Background-Bild), als Prozentwert 0-100. `NULL` (Default) heißt "nimm die eingebauten Theme-/Bild-abhängigen Defaults" (0.04 dunkel / 0.14 hell fürs Logo, 0.16 fürs VIP-Bild, hartcodiert in `screens-home.jsx`), betrifft also keinen Bestandsnutzer beim Deploy. Gesetzt, gilt der Wert flach für Logo **und** VIP-Bild, unabhängig vom Theme. Slider im Appearance-Sheet. Store field `watermarkOpacity`. Migration 0179.

## RPCs & Realtime

### Feature-Grants & Admin

- **`check_active_users_access()`** → `boolean`: gibt true zurück, wenn der aufrufende User das `active_users`-Feature hat (Admin oder per `zane_feature_grants`)
- **`get_active_users_grants()`** → `TABLE(email text)`: listet alle Emails mit `active_users`-Grant (nur Admin)
- **`set_active_users_grant(p_email text, p_granted boolean)`** → `void`: erteilt oder entzieht den `active_users`-Grant (nur Admin)
- **`get_all_users_admin()`** → `TABLE(user_id uuid, name text, email text, x_handle text, x_handle_public boolean, x_handle_prompt_opted_out boolean, sw_version text, created_at timestamptz, tier text, plan_count int, last_workout timestamptz)`: alle registrierten Accounts (nur Admin, hardcoded auf `office@btc-prime.biz`, unabhängig vom `active_users`-Feature-Grant); die einzige Datenquelle des „All users"-Screens im Admin-Sheet (Settings). Der vereint die früheren separaten „Recent sign-ups"- und „Onboarded"-Ansichten als Client-seitige Filter (neue Sign-ups anhand `created_at`/localStorage `logbook-seen-signups`, Onboarded anhand `plan_count > 0`) plus Suche nach Name/Email/X Handle und Filter „nur veraltete Version" (gegen die eigene, per Cache Storage ermittelte Version) und „nur in den letzten 7 Tagen trainiert" (gegen `last_workout`); deckt jeden Account ab, unabhängig von Aktivität. `last_workout` = `MAX(ended)` über `zane_sessions`, `null` wenn nie eine Session beendet wurde. Migration 0123, `plan_count` in 0124, `last_workout` in 0185, X-Felder in 0262.
- **`get_user_detail_admin(p_user_id uuid)`** → `jsonb` (nur Admin): liefert zusätzlich `x_handle`, `x_handle_public` und `x_handle_prompt_opted_out` sowie die vollen Plan-Details eines Users (`active_schedule_id` + alle `plans` mit Tagen/Items inkl. aufgelöstem Übungsnamen/movement_type/unilateral) für die „All users"-Detailansicht. Migration 0262 ergänzt die Profilfelder.
- **`get_active_sessions_overview()`** → `TABLE(...)`: aktive + kürzlich beendete Sessions aller User inkl. Sets/Dauer-Statistik (gated by feature grant)
- **`get_active_session_detail(p_user_id uuid, p_session_id text)`** → `TABLE(...)`: Volldetail einer Session inkl. Historienvergleich (avg. Dauer, Sets, letzte Session; gated by feature grant). Gibt zusätzlich die `unit` ('kg'|'lbs') des Trainierenden zurück (Migration 0068), damit die Coach-Spectator-/Comparison-Ansicht Gewichte im Einheiten-Label des Clients zeigt; gespeicherte Zahlen werden nie umgerechnet.

### Signup & Approval

- **`get_recent_signups(p_limit int default 50)`** → `TABLE(user_id uuid, name text, email text, created_at timestamptz)`: jüngste Registrierungen, nur Admin. **Nicht** die Quelle des Account-Tab-Feeds: der liest `get_all_users_admin` und filtert clientseitig (`screens-settings.jsx`). „Got it"-Dismiss pro Gerät via localStorage `logbook-seen-signups`. Migration 0075.
- **`get_users_with_plans()`** → `TABLE(user_id, name, email, joined_at, plan_count)` ist die **Legacy**-„Onboarded"-Ansicht (nur User mit ≥1 Plan); von `get_all_users_admin` abgelöst, aber noch vorhanden. Der Approval-Workflow (`get_pending_users`, `approve_user`, `decline_user`) ist mit Migration 0241 entfernt.
- **Founding-Member-Vergabe** (Migration 0240): **`grant_lifetime_if_qualified()`** ist die Trigger-Funktion hinter `zane_sessions_grant_lifetime` (AFTER INSERT OR UPDATE OF `ended` auf `zane_sessions`). Sie springt früh raus, wenn die Session nicht beendet ist oder der Account bereits ein Tier hat (das ist der überwiegende Teil aller Writes), prüft dann Workout-Zahl und Minutensumme und serialisiert die Sitzplatz-Vergabe über `pg_advisory_xact_lock`, damit zwei gleichzeitig qualifizierende Accounts nicht denselben letzten Platz nehmen. **`zane_profiles_protect_tier()`** ist die BEFORE-UPDATE-Trigger-Funktion auf `zane_profiles`, die Client-Writes auf `tier` zurückdreht. **`zane_sessions_stamp_completion()`** (Migration 0242) ist die BEFORE-INSERT/UPDATE-Trigger-Funktion auf `zane_sessions`, die `completed_server_at` mit `now()` stempelt, jeden Client-Wert dafür verwirft und den Stempel danach einfriert. Beide: `REVOKE EXECUTE FROM PUBLIC, anon, authenticated` (Trigger-Funktionen, kein direkter Aufruf vorgesehen).
- **`get_founding_seats()`** → `TABLE(total int, taken int, remaining int)` (SECURITY DEFINER, STABLE, Migration 0244): Sitzplatz-Zähler für den Live-Counter in der Founding-Offer-Karte auf `welcome.html`. `total` = `zane_app_config.lifetime_seats_total` (Fallback 75, falls die Config-Zeile fehlt), `taken` = `count(*)` über `zane_profiles` mit `tier = 'lifetime'`, `remaining` auf 0 geklemmt (ein nachträglich gesenktes `lifetime_seats_total` darf nie negativ herauskommen). **An `anon` gegrantet**, zweite Funktion überhaupt mit anon-Zugriff nach `get_public_feature_map` (`welcome.html` läuft ohne Login); gibt ausschließlich drei Aggregate zurück, keine Identitäten und keine Zeitstempel, aus der Antwort ist also nicht ableitbar **wer** einen Platz hält. DEFINER ist nötig, weil `anon` weder `zane_profiles` (RLS) noch `zane_app_config` lesen darf. **Die Sitzplatz-Definition dupliziert `grant_lifetime_if_qualified()` bewusst: ändert sie sich, beide Stellen anfassen**, sonst bewirbt die Seite Plätze, die der Trigger nicht mehr vergibt.

### Sync-RPCs (Staleness-Guards)

- **`sync_sets_batch(p_sets jsonb)`** → `void`: Batch-Upsert für Sets mit `updated_at`-Guard; updated eine Zeile nur, wenn das eingehende `updated_at` neuer ist als das gespeicherte (verhindert, dass stale kbApply-Writes fertige Sets überschreiben). Das eingehende `updated_at` wird seit Migration 0238 auf `LEAST(Client-Wert, now())` gekappt, bevor es gespeichert oder für den Guard-Vergleich genutzt wird: ein Client mit vorgehender Systemuhr kann so keinen Zeitstempel in der Zukunft mehr beanspruchen und dadurch nicht mehr jeden künftigen Konflikt automatisch gewinnen. Ein Wert bei oder vor der echten Zeit bleibt unverändert (ein Offline-Write behält seine ursprüngliche Bearbeitungszeit, auch wenn er erst später synct). **Die Spaltenliste steht wie bei `sync_daily_logs_batch` dreimal in der Funktion** (INSERT-Liste, SELECT-Projektion, `ON CONFLICT SET`): `added_kg` (Migration 0243) fehlte hier bis Migration 0245 in allen dreien, siehe deren Bug-Notiz oben bei `zane_sets.added_kg`. Kein CI-Gate vergleicht RPC-Spaltencoverage gegen das Schema, dieselbe Lücke ist also jederzeit für eine neue Spalte wieder möglich.
- **`sync_daily_logs_batch(p_logs jsonb)`** → `void` (SECURITY INVOKER): Batch-Upsert für Daily Logs, löst Konflikte auf (user_id, date) unter Beibehaltung der bestehenden id, mit `updated_at`-Staleness-Guard. Ersetzt den plain Upsert, damit Multi-Device-Edits desselben Tages nicht auf UNIQUE(user_id, date) kollidieren und ein staler Write keinen neueren überschreibt. Migration 0096. **Die Spaltenliste steht dreimal in der Funktion** (INSERT-Liste, SELECT-Projektion, `ON CONFLICT SET`): eine neue Spalte auf `zane_daily_logs`, die hier fehlt, synchronisiert still nie, und kein CI-Gate merkt es. `meal_of_choice` ergänzt in 0211, dort **bewusst im `DO UPDATE` gecoalesct statt in der SELECT** (`COALESCE(EXCLUDED.meal_of_choice, zane_daily_logs.meal_of_choice)`): ein alter Client schickt den Key nicht, und ein Coalesce in der SELECT würde „Key fehlt" und „bewusst gelöscht" gleich behandeln, ein altes Gerät würde also beim Ändern des Gewichts den markierten Tag entmarkieren. Gleicher `LEAST(..., now())`-Clock-Skew-Guard auf `updated_at` seit Migration 0238 (siehe `sync_sets_batch` oben). Die sieben Body-Measurement-Spalten (`waist_cm`/`hips_cm`/`chest_cm`/`arm_cm`/`thigh_cm`/`calf_cm`/`body_fat_pct`) sind seit Migration 0248 in allen drei Listen ergänzt, **plain `EXCLUDED`-Zuweisung** wie `weight`/`steps`. Ein kurzer COALESCE-Versuch (Review-Fix, in 0248 nachgezogen) wurde in Migration 0250 zurückgenommen: Messwerte werden für geleerte Felder als null gesendet, ein COALESCE hätte ein bewusst gelöschtes Feld bei jedem Sync wiederbelebt (anders als `meal_of_choice`, das nie als null kommt). Akzeptierter Tradeoff: Ein Pre-Update-Client, der einen gemessenen Tag synct, nullt die Messwerte während der Rollout-Phase, bis das alte Gerät das Update bekommt. BMI wird clientseitig abgeleitet, keine eigene Spalte.
- **`sync_meso_states_batch(p_states jsonb)`** → `void` (SECURITY INVOKER): Batch-Upsert für Meso-States mit `updated_at`-Staleness-Guard, damit zwei Geräte, die denselben Meso-Plan trainieren, sich nicht still gegenseitig `deltas`/`joint_flags`/`weight_boosts`/`weight_boost_declines`/`growth_counts`/`rep_miss_counts`/`affinity` clobbern. Migration 0122 (`growth_counts` ergänzt in 0130, `started_at` in 0138: beim Update COALESCEd, damit ein älterer Client, der es weglässt, den Anker nicht nullen kann; `rep_miss_counts` ergänzt in 0165, `affinity` in 0169, `weight_boost_declines` in 0178). Gleicher `LEAST(..., now())`-Clock-Skew-Guard auf `updated_at` seit Migration 0238 (siehe `sync_sets_batch` oben).

`food_day_closed` ist in `sync_daily_logs_batch` seit Migration 0263 in allen drei Spaltenlisten enthalten und wird im Update per `COALESCE(EXCLUDED.food_day_closed, zane_daily_logs.food_day_closed)` aktualisiert. So kann ein älterer Client ohne dieses Feld einen bestehenden Abschluss nicht zurücksetzen, während ein neuer Client `false` weiterhin bewusst zum Reopen senden kann.

### Session-Daten & History-Aggregate

- **`zane_entries_json(p_session_id text)`** → `jsonb` (SECURITY DEFINER, **internal-only**): baut das store-förmige (camelCase) `entries`-Array einer Session aus den relationalen Tabellen (`zane_session_entries`/`zane_sets`). Quelle der Wahrheit seit Migration 0058; von `get_active_session_detail`/`get_active_sessions_overview` genutzt, damit die Coach-/Spectator-Ansicht nicht mehr vom Legacy-JSONB abhängt. **Kein Client-Rollen-Grant** (Migration 0136): die Funktion filtert nur nach `session_id` ohne Owner-/Coach-Check, ein direkter Aufruf wäre also ein Cross-Tenant-Read (IDOR). `EXECUTE` ist für `anon`/`authenticated`/`PUBLIC` entzogen; die definer-owned Reporting-RPCs rufen sie intern trotzdem auf (laufen als Owner). Der Client schreibt das JSONB nicht mehr (`sessionToRow` in `store.js` lässt `entries` aus). Gibt seit Migration 0134 zusätzlich `plannedRepsMax` und seit 0135 `plannedProgressionOffset` zurück.

Serverseitige History-Aggregate (Migrationen 0059/0060, SECURITY INVOKER, optional `p_user_id` für Coach-Zugriff):

- **`get_exercise_best_e1rm(p_user_id?)`** → `TABLE(ex_id, best_e1rm)`: bestes All-Time-e1RM (Epley) je Übung über beendete Sessions. Beim Boot geladen und als `store.exerciseBests` gecacht; `bestE1rmForExercise` = max(Aggregat, lokal geladenes Fenster). Beim Training-Mount refresht (`refreshExerciseBests`). Assisted-Übungen (`movement_type = 'assisted'`, negative Last) sind ausgeschlossen (Migration 0148), sonst würde ein negatives „best e1RM" gecacht; ihre PR-Erkennung läuft ohnehin über `bestAssistLoad`/`bestTimeForExercise`. **Deload- und Cleanup-Sessions ausgeschlossen** (`NOT s.is_deload` Migration 0239, `NOT s.is_cleanup` Migration 0251): jede Client-seitige PR-Erkennung (`isPR` in `screens-lib.jsx`) schließt Deload-Sessions bewusst als PR-Baseline aus (absichtlich leichte Sätze), `store.exerciseBests` wird von `SessionDetailScreen`s `isPR` aber als harte Untergrenze genutzt, ein durch eine Deload-Session verzerrter Wert hätte einen echten neuen PR sonst dauerhaft blockieren können (live bestätigt).
- **`update_daily_log_derived(p_date, p_adherence?, p_targets_snap?)`** → `void` (SECURITY INVOKER, Migration 0256): schreibt **ausschließlich** `adherence` und `targets_snap` eines Tages und lässt `updated_at` unangetastet. Grund: beide Werte sind **abgeleitet** (der Client rechnet sie aus den Food-Einträgen und den aktiven Makro-Zielen neu). Sie liefen vorher über `sync_daily_logs_batch` wie jedes vom Nutzer getippte Feld, weshalb ein reines Neuberechnen `updated_at` hochziehen musste, um am Staleness-Guard dieser RPC vorbeizukommen, und damit gewann die **ganze** Zeile Last-Write-Wins: ein Gerät mit veraltetem Tagesstand konnte beim Neuberechnen sein altes Gewicht, Schritte, Körpermaße und Makros über die neueren Eingaben eines anderen Geräts schieben. Legt bewusst **keine** Zeile an (Adherence ohne Tageszeile ist bedeutungslos, und ein Insert würde gelöschte Tage wiederbeleben); eine fehlende Zeile ist ein No-op. Der Sync-Diff in `store.js` klammert die beiden Felder aus dem Änderungsvergleich aus (`normDaily`), damit ein Neuberechnen die Zeile gar nicht erst als dirty markiert; im Payload werden sie weiterhin mitgeschickt, wenn die Zeile aus einem echten Grund hochgeht. Aufgerufen über `LB.updateDailyLogDerived` (fire-and-forget, Fehler werden geloggt, die Werte entstehen beim nächsten Boot ohnehin neu).
- **`get_exercise_history(p_ex_id, p_day_id?, p_limit?, p_user_id?)`** → `TABLE(session_id, day_id, date, ended, sets jsonb)`: jüngste beendete Sessions mit dieser Übung. Das `sets`-jsonb je Satz: `kg, reps, repsL, repsR, timeSec, addedKg, hornLoads, done, skipped, warmup, technique, drops` (`technique`/`drops` ab Migration 0170, damit die Exercise-History Intensitätstechniken annotieren kann und die Server-Rows die lokalen nicht entwerten; `addedKg`/`hornLoads` ab Migration 0255). **Die Projektion ist eine feste Feldliste, alles nicht Genannte fällt still weg**, und genau daran hingen `addedKg` und `hornLoads`: beide beschreiben, *wie* eine Last zustande kam, und genau das trägt `buildSeedSets` in die nächste Session vor. Ohne sie kam beim Seeding immer „kein Vorwert" an, `kg` blieb korrekt, es sah also nichts kaputt aus, die Gürtel-Last und die Dorn-Verteilung haben nur nie vorbefüllt. Dieselbe Klasse stiller Auslassung wie der `sync_sets_batch`-Bug aus Migration 0245, eine Ebene höher. Genutzt von `fetchSeedEntries` (Seeds/Progression beim Session-Start, nur wenn das lokale Fenster < 3 Treffer hat), der „Last time"-Karte im Training (Fallback) und beiden Exercise-History-Ansichten (lokal sofort, Server erweitert auf volle Historie).
- **`get_user_volume_stats(p_user_id?)`** → `TABLE(session_count, total_volume, total_minutes, total_done_sets)`: All-Time-Summen. Vom Client derzeit **nicht** aufgerufen: die Stats summieren lokal über `totalVolume()`/`doneSetCount()`, die für gefensterte Sessions auf die `get_session_stats`-Aggregate zurückfallen (exakt, offline-fähig, schließt frisch beendete Sessions sofort ein). Volumen-Semantik inkl. Assisted (Migration 0147) identisch zu `get_session_stats`.
- **`get_session_stats(p_user_id?)`** → `TABLE(session_id, exercise_count, done_sets, volume)`: per-Session-Aggregate aller beendeten Sessions (Migration 0060). Beim Boot geladen und als `aggVolume`/`aggDoneSets`/`aggExercises` an die Sessions gehängt; `totalVolume`/`doneSetCount` nutzen sie als Fallback für Sessions ohne geladene Sets (History-Liste, Best Session, Coach-Listen). Semantik = Client-Logik für beendete Sessions (done-Flag nicht erforderlich); `done_sets` zählt kg+reps-Sets **oder** Sets mit geloggter `time_sec`-Dauer (Migration 0146, Parität zu `doneSetCount`). Assisted-Übungen (`movement_type = 'assisted'`) zählen `GREATEST(0, Körpergewicht + kg) * reps` mit dem `zane_daily_logs`-Gewicht am nächsten zum Session-Datum, ohne geloggtes Gewicht `GREATEST(0, kg)` (Migration 0147, Parität zu `entryVolume`). `sessionToRow` filtert die `agg*`-Felder beim Sync wieder heraus.
- **`bump_api_usage(p_user_id, p_kind, p_limit)`** → `boolean`: zählt den Tageszähler in `zane_api_usage` für `(User, heute, kind)` hoch und antwortet, ob dieser Aufruf noch im Kontingent liegt (`true` beim `p_limit`-ten Aufruf, ab `p_limit + 1` `false`). SECURITY DEFINER, Migration 0207. **Nur `service_role`** darf sie ausführen. `REVOKE ... FROM PUBLIC` allein reichte dafür **nicht**: eine `ALTER DEFAULT PRIVILEGES`-Regel von `postgres` vergibt EXECUTE auf jede neue Funktion zusätzlich direkt an `authenticated` (Migration 0132 hat seinerzeit nur die `anon`-Regel entfernt). Migration 0208 nimmt `authenticated` das Recht explizit weg, sonst hätte jeder eingeloggte User die Funktion mit **fremder** `p_user_id` aufrufen und dieses Konto für den Rest des Tages aus Suche und Etikettenscan aussperren können (die Funktion prüft bewusst keine Auth, ihr einziger vorgesehener Aufrufer hält den Service-Role-Key): einzige Aufrufer sind die Food-Edge-Functions mit dem Service-Role-Key. Die failen bei einem Fehler bewusst offen, das Limit ist beratend.
- **`get_top_exercises(p_user_id?, p_limit? default 5)`** → `TABLE(ex_id, session_count)`: All-Time-Ranking der meistgeloggten Übungen (Count der `zane_session_entries`-Zeilen je Übung über alle beendeten Sessions), Migration 0195. Ersetzt in `StatsTab` (Stats-Tab, „TOP EXERCISES") die rein clientseitige `exCounts`-Zählung über `session.entries`, die nur die Sessions im 70-Tage-Boot-Fenster (`HISTORY_WINDOW_DAYS`) plus zufällig anderswo nachgeladene Sessions erfasste und für ältere Accounts silently unvollständig war. Wird per `fetchTopExercises` (`store.js`) in einem `useEffect` von `StatsTab` geladen, Ergebnis lebt nur in lokalem Component-State (kein Store-Feld, kein Backup-Impact).

### Coaching & Support

- **`get_coach_info()`** → `TABLE(coaching_id text, coach_id uuid, coach_email text, coach_name text, status text)`: gibt die Coach-Beziehung des aufrufenden Clients zurück (wer coacht mich); Self-Coaching-Zeilen ausgeschlossen.
- **`get_coaching_clients()`** → `TABLE(coaching_id text, client_id uuid, client_email text, client_name text, status text, checkin_enabled boolean)`: listet alle Clients des aufrufenden Coaches inkl. `checkin_enabled`-Flag; Self-Coaching-Zeilen ausgeschlossen.
- **`get_coach_clients_status()`** → `TABLE(client_id uuid, in_progress_session_id text, status_mode text, status_mode_since timestamptz)`: Live-Trainingsstatus **und** Sick/Vacation/Deload-Status aller aktiven Clients eines Coaches (SECURITY DEFINER, umgeht RLS auf `zane_user_settings`); seit Migration 0082 alle aktiven Clients statt nur die mit laufender Session, damit `status_mode` auch ohne Training sichtbar ist. Self-Coaching- und Support-Zeilen ausgeschlossen (Migration 0085).
- **`get_coach_checkin_status()`** → `TABLE(coaching_id text, checked_in_at timestamptz)`: Check-in-Status je Coaching-Beziehung des aufrufenden Coaches für die zuletzt abgeschlossene Woche (`week_start` = Montag der Vorwoche); Self-Coaching- und Support-Zeilen ausgeschlossen. Die Wochenberechnung nutzt `EXTRACT(ISODOW)` (Mo=1..So=7), damit Sonntag als Tag 7 der laufenden Woche zählt und die „fällige" Woche erst am Montag weiterspringt, exakt wie `store.js checkinWeekStart()` (Migration 0149; vorher `DOW` mit So=0, was sonntags eine Woche zu früh sprang und einen bereits eingecheckten Client fälschlich auf „CHECK-IN DUE" zurückflippte).
- **`invite_client(p_email text)`** → `text` (SECURITY DEFINER): Coach lädt einen Client per Email ein; legt eine `pending`-Zeile (id-Präfix `cch_`) an und gibt deren id zurück. Seit Migration 0150 wird `checkin_schema` dabei aus dem `default_checkin_schema` des Coaches vorbefüllt (null bleibt null). Fehlerfälle als String: `ERROR:not_found` (Email unbekannt), `ERROR:self`, `ERROR:exists:<id>` (Beziehung existiert schon), `ERROR:already_coached` (Client hat bereits einen aktiven Coach). Beide Duplikat-Checks schließen seit Migration 0151 `support_%`-Zeilen aus (ein früherer Support-Chat ist keine Coaching-Beziehung), der `already_coached`-Check zusätzlich `coach_id = client_id` (Self-Coaching blockt keine echte Einladung durch einen anderen Coach).
- **`respond_to_coaching_invite(p_coaching_id text, p_accept boolean)`** → `void`: Client beantwortet eine Pending-Einladung. Accept: setzt `status='active'`, löscht dabei andere aktive Fremd-Coach-Beziehungen des Clients (Self-Coaching und Support-Tickets bleiben, Migration 0229) und alle übrigen Pending-Einladungen. Decline: löscht die Zeile.
- **`find_user_by_email(p_email text)`** → `uuid` (STABLE SECURITY DEFINER, **internal-only**): case-insensitiver Lookup in `auth.users`. Kein direkter Grant für `anon`/`authenticated` (Migration 0125); wird nur intern von `invite_client` aufgerufen.
- **`enable_self_coaching()`** → `text`: legt (idempotent) eine Self-Coaching-Zeile an (`coach_id = client_id = auth.uid()`, status active, id-Präfix `self_`) und gibt deren id zurück. Aktiviert „be your own coach".
- **`admin_broadcast_message(p_body text)`** → `int` (nur Admin): sendet eine Nachricht an **alle** User auf einmal, indem für jeden User (der noch keins hat) ein Support-Ticket (`support_<user_id>`) angelegt und eine `zane_coaching_notes`-Zeile vom Admin eingefügt wird. Nutzt bewusst die bestehende, bereits überall ausgelieferte Support-/Realtime-Infrastruktur (kein neuer Client-Code nötig, erreicht auch User auf altem App-Stand) statt eines neuen Banner-Systems. Gibt die Anzahl benachrichtigter User zurück. Migration 0127. Admin-UI: Settings → Admin → „Message all users". **`get_support_chats()` blendet ein Ticket aus, solange der User selbst noch keine Nachricht geschickt hat** (Migration 0129): sonst würde ein Broadcast die Admin-Inbox mit einem Eintrag pro User fluten und echte offene Anfragen verdecken; taucht wieder auf, sobald der User antwortet.
- **Support-Tickets** (Migrationen 0085/0086, Archiv `archive_support_tickets`): ein Support-Ticket ist eine `zane_coaching`-Zeile mit id-Präfix `support_` (siehe Tabellen-Doku). RPCs: **`open_support_chat(p_category text default 'question')`** → `text` (User öffnet ein Ticket, `p_category` ∈ feature_request|bug|question, gibt die id zurück), **`get_user_support_chats()`** → `TABLE(...)` (die eigenen Tickets des Users inkl. `archived`/`unread_count`), **`get_support_chats()`** → `TABLE(...)` (Admin-Inbox offener Tickets inklusive `x_handle`; blendet Tickets ohne User-Nachricht aus, Migration 0129), **`get_archived_support_chats()`** → `TABLE(...)` (Admin, archivierte Tickets, ebenfalls mit `x_handle`), **`set_support_status(p_coaching_id, p_status)`** → `void` (Admin, open|in_progress|resolved), **`archive_support_ticket(p_coaching_id)`** / **`delete_support_ticket(p_coaching_id)`** → `void` (Admin). Migration 0262 ergänzt die Handle-Spalten.

### Sonstiges

- **Recipe-Sharing** (Migration 0193, Tabelle `zane_recipe_shares` oben): **`create_recipe_share(p_recipe_id text, p_recipe jsonb)`** → `text` (SECURITY DEFINER): upsertet den Rezept-Snapshot (Konflikt auf `user_id`+`recipe_id`, Re-Share desselben Rezepts liefert denselben Token) und validiert Shape (`name` non-empty, `items` Array) sowie Größe (max 20 KB). / **`get_recipe_share(p_token text)`** → `jsonb` (STABLE SECURITY DEFINER): `{ recipe, sharedBy, createdAt }` zu einem Token, `sharedBy` aus `zane_profiles.name` (Fallback `'A Zane user'`); `null` bei unbekanntem Token. Beide: `REVOKE FROM PUBLIC` + Grant nur an `authenticated` (kein anon: ein Empfänger braucht zum Übernehmen ohnehin einen Account, die anon-Fläche bleibt null). Der Token selbst ist die Autorisierung, `get_recipe_share` hat bewusst keinen Owner-Check. / **`delete_my_recipe_shares()`** → `void` (SECURITY DEFINER, Migration 0213, `REVOKE FROM PUBLIC` + Grant an `authenticated`): löscht alle Share-Zeilen des Aufrufers. Nötig, weil die Tabelle keine Policies hat und `deleteAllData` sie deshalb nicht erreichen konnte, ein alter `?share=<token>`-Link also den Snapshot samt Anzeigenamen über die Account-Löschung hinaus weiter ausgeliefert hat. Wird nur im echten „Delete all data"-Flow aufgerufen, **nicht** beim Restore (der nutzt dieselbe Funktion mit `keepPush`, und bereits verschickte Links sollen einen Restore überleben).
- **`zane_is_coach_of(p_client_id uuid)`** → `boolean` (STABLE SECURITY DEFINER): RLS-Helfer, true wenn `auth.uid()` ein aktiver Coach des Clients ist (`zane_coaching` mit `status='active'`). Basis aller Coach-of-Client-Policies (Profiles, Exercises, Schedules, Sessions, Entries, Sets, Daily Logs, Status Periods). Seit Migration 0148 sind Support-/Broadcast-Zeilen (id-Präfix `support_`) ausgeschlossen (`and id not like 'support_%'`): ein Admin, der einen Support-Chat mit einem User teilt, bekommt dadurch keinen Coach-Zugriff auf dessen komplette Trainingsdaten mehr. Die Inline-Coach-Read-Policies auf `zane_cardio_logs`/`zane_status_periods`/`zane_glucose_logs` (die nicht über diese Funktion laufen) tragen dieselbe Exclusion.
- **`zane_coaching_notes_guard_update()`** → `trigger` (SECURITY DEFINER, Migration 0148): BEFORE-UPDATE-Guard auf `zane_coaching_notes`. Die „recipient can mark read"-UPDATE-Policy lässt sich per RLS nicht spaltenweise beschränken; der Trigger erlaubt einem Nicht-Autor daher nur, `read_at` zu ändern (jede andere Spaltenänderung wirft `recipient may only update read_at`). Kein direkter Grant.
- **`zane_daily_logs_guard_ai_summary()`** / **`zane_checkins_guard_ai_opinion()`** → `trigger` (SECURITY DEFINER, Migration 0226): dasselbe Muster wie `zane_coaching_notes_guard_update` oben, für `ai_summary`/`ai_summary_generated_at` auf `zane_daily_logs` bzw. `ai_opinion`/`ai_opinion_generated_at` auf `zane_checkins`. Die jeweilige „User/Client verwaltet eigene Zeile"-Policy ist breit (FOR ALL, keine Spalten-Einschränkung möglich), der Trigger wirft `... is server-generated only`, sobald `auth.uid() IS NOT NULL` (eine echte User-Session) eine dieser Spalten ändern will. Der Service-Role-Write der jeweiligen Edge Function (`ai-daily-summary`/`ai-checkin-opinion`, dort ist `auth.uid()` NULL) bleibt unangetastet. Kein direkter Grant.
- **`zane_guard_user_id_immutable()`** → `trigger` (SECURITY DEFINER, Migration 0148): BEFORE-UPDATE-Guard, wirft wenn sich `user_id` ändert. Auf `zane_exercises`/`zane_schedules`/`zane_sessions`/`zane_session_entries`/`zane_sets`/`zane_user_settings` sowie (Migration 0213) `zane_food_meal_plans`/`zane_food_template_slots`/`zane_food_recipes` und (Migration 0220) `zane_medication_plans`/`zane_medications`/`zane_medication_schedule_slots`/`zane_medication_logs`, damit ein Coach eine Zeile nicht zwischen seinen Clients umhängen kann (die Coach-UPDATE-Policies haben kein OLD-fähiges `WITH CHECK`). Kein direkter Grant.
- **`handle_new_user()`** → `trigger` (SECURITY DEFINER): legt beim Anlegen eines Auth-Users die `zane_user_settings`-Zeile an (ON CONFLICT DO NOTHING); Trigger `on_auth_user_created` auf `auth.users`.
- **VIP-Backgrounds** (Migration 0103): **`get_user_vip_backgrounds()`** → `TABLE(email text, bg_key text)` (Admin, alle gesetzten Backgrounds) / **`set_user_vip_background(p_email text, p_bg_key text)`** → `text` (Admin, setzt `zane_user_settings.vip_background` für den User per Email; leerer Key = löschen).
- **`get_force_update_nonce()`** → `text` / **`admin_force_update()`** → `void` (setzen/lesen nur `zane_app_config.force_update_nonce`, Admin-Setter): pusht das „New version available"-Update-Banner an **alle** verbundenen Clients, ohne dass ein `sw.js`-Cache-Bump nötig ist (der laut Deployment-Regeln nur auf ausdrückliche Aufforderung passiert, die meisten Deploys lösen also keinen Banner aus). `app.jsx`s `checkForceUpdate` pollt die Nonce im selben Rhythmus wie den bestehenden `sw.js`-Text-Versions-Check (`checkSwUpdate`) und vergleicht sie gegen den localStorage-Key `logbook-force-nonce-seen`, nach demselben „erstes Sichten = Baseline, kein Banner"-Prinzip, damit ein brandneues Gerät nie fälschlich ein Update angezeigt bekommt. Ein Klick auf „Update" führt über `applyUpdate`/`LB.clearCachesAndReload()` immer zu einem echten frischen Reload, unabhängig davon, ob wirklich ein neuer Service Worker existiert. Migration 0131. Admin-UI: Settings → Admin → „Force refresh all users". Daneben „Test update banner" (kein Server-Call, setzt nur lokal `updateAvailable=true` zum Testen der Banner-UI).
- **`admin_schema_inventory()`** → `jsonb` (STABLE SECURITY DEFINER, **internal/ops-only**): liefert das komplette Schema-Inventar für den wöchentlichen Drift-Check (`tools/check-db-live.cjs` via `.github/workflows/db-drift.yml`): alle public-Spalten (`information_schema`), je Funktion `has_function_privilege('anon', ...)` **und** `has_function_privilege('authenticated', ...)` (Feld `authenticated_exec`, seit Migration 0258) sowie die `supabase_realtime`-Publikation. Kein Grant für `anon`/`authenticated`, nur `service_role` (Aufruf per PostgREST mit dem Service-Key aus dem GitHub-Actions-Secret `SUPABASE_SERVICE_ROLE_KEY`). Migrationen 0142, 0258.
  - `authenticated_exec` speist die Liste `EXPECTED_NO_AUTHENTICATED_EXEC` im Drift-Tool: eine kleine Allowlist von Funktionen, die **kein** eingeloggter User ausführen darf (aktuell `bump_api_usage`, `collapse_water_logs`, `admin_schema_inventory`). Grund ist die zweite Hälfte der Grant-Fallen unten: die `ALTER DEFAULT PRIVILEGES`-Regel vergibt `EXECUTE` weiterhin direkt an `authenticated`, und genau diese Regression (0207 → 0208 bei `bump_api_usage`) hatte bisher kein Signal. Neue service-role-only Funktion: Signatur dort eintragen.
- **`auto-close-sessions`** (Edge Function): schließt abgelaufene offene Sessions. Keine Sets → Session + Entries löschen (butt start); mit Sets → `ended` = letztes `updated_at` der Sets, `duration_minutes` berechnen, `in_progress_session_id` clearen; optional Pushover-Notification. Wird per Cron alle 15 Minuten aufgerufen (Supabase Dashboard → Edge Functions → Schedule). Timeout pro User in `session_timeout_minutes` (default 90 min).
- **Cron-Auth (Migration 0230):** `reminder`/`water-reminder`/`meal-reminder`/`medication-reminder`/`daily-log-reminder`/`auto-close-sessions` sind reine Cron-Trigger-Ziele ohne eigene Caller-Auflösung (anders als `pushover`, siehe dessen `resolveCaller`); sie verlangen seit Migration 0230 einen `Authorization: Bearer <CRON_SECRET>`-Header (Edge-Function-Secret `CRON_SECRET`, geprüft vor jeder eigentlichen Arbeit, fail-closed bei leerem Secret). Die fünf per pg_cron geschedulten Jobs lesen den Wert zur Laufzeit aus Supabase Vault (`vault.decrypted_secrets`); `auto-close-sessions` ist nur per Dashboard geschedult und braucht denselben Header dort manuell nachgetragen.

### Grant-Fallen bei neuen SECURITY-DEFINER-Funktionen

**Falle 1 (PUBLIC-Vererbung):** Postgres vergibt beim `CREATE FUNCTION` automatisch `EXECUTE` an die Pseudo-Rolle `PUBLIC`; davon erbt auch `anon`, **unabhängig** von einem gezielten `REVOKE ... FROM anon` (das war der Fehler in Migration 0125, korrigiert in 0128). Jede neue SECURITY-DEFINER-Funktion braucht explizit `REVOKE EXECUTE ON FUNCTION ... FROM PUBLIC` + `GRANT EXECUTE ON FUNCTION ... TO authenticated` (Ausnahme: rein intern aufgerufene Funktionen wie `find_user_by_email` bekommen kein Grant für `authenticated`).

**Falle 2 (Default Privileges, Migration 0132):** Zusätzlich zur PUBLIC-Vererbung existierte auf diesem Projekt eine `ALTER DEFAULT PRIVILEGES`-Regel (Rolle `postgres`, Schema `public`), die `anon` bei **jeder neu erstellten** Funktion automatisch einen direkten `EXECUTE`-Grant gab. Das ist ein expliziter Grant an `anon` selbst, kein geerbter über PUBLIC, und wird von `REVOKE ... FROM PUBLIC` **nicht** mitentfernt. Entdeckt an den beiden Migration-0131-Funktionen, die trotz korrektem `REVOKE FROM PUBLIC` weiterhin von `anon` ausführbar waren (verifiziert mit `has_function_privilege('anon', ...)`), während alle vorher existierenden Funktionen korrekt gesperrt waren. Migration 0132 hat die Default-Privileges-Regel für die Rolle `postgres` entfernt (Root Cause, schützt alle künftigen Funktionen) und zusätzlich explizit `REVOKE EXECUTE ... FROM anon` auf die beiden betroffenen Funktionen gesetzt. (Die gleiche Default-Privileges-Regel existiert auch für die Rolle `supabase_admin`, die vom Projekt aber nicht änderbar ist: „permission denied to change default privileges"; der tatsächlich genutzte Migrationspfad läuft nachweislich als `postgres` und ist gefixt.)

**Falle 3 (Default Privileges für `authenticated`, Migration 0208):** Analog zu Falle 2, aber für `authenticated` statt `anon`, und anders als dort besteht die Regel weiter: `ALTER DEFAULT PRIVILEGES` (Rolle `postgres`) vergibt `authenticated` bei jeder neu erstellten Funktion automatisch einen direkten `EXECUTE`-Grant, unabhängig von `REVOKE ... FROM PUBLIC`. Betrifft nur Funktionen, die **nicht einmal** `authenticated` ausführen dürfen, sondern ausschließlich der Service-Role vorbehalten sind (z.B. `bump_api_usage`, RPC-Referenz oben): der übliche Fall „REVOKE FROM PUBLIC + GRANT TO authenticated" bemerkt das Problem nie, weil `authenticated` dort ohnehin einen Grant bekommen soll. Entdeckt beim Verifizieren von Migration 0207 (`bump_api_usage` war trotz korrektem `REVOKE FROM PUBLIC` weiterhin von `authenticated` ausführbar). Migration 0208 hat nur den Einzelfall gefixt (`REVOKE EXECUTE ... FROM authenticated` auf `bump_api_usage`); die Default-Privileges-Regel für `authenticated` selbst wurde, anders als Falle 2 für `anon` in Migration 0132, **nicht** entfernt. Jede künftige service-role-only-Funktion muss das REVOKE also einzeln setzen.

**Kontrolle nach jeder neuen Funktion (DEFINER wie INVOKER):** `SELECT has_function_privilege('anon', 'public.<fn>(...)', 'execute');` muss `false` ergeben, außer für die zwei bewussten Ausnahmen `get_public_feature_map` (siehe Feature Map oben) und `get_founding_seats` (Seat-Zähler auf `welcome.html`, Migration 0244): beide bedienen login-freie Public-Seiten und geben ausschließlich das zurück, was die Seite ohnehin anzeigt, ohne Per-User-Daten. Der wöchentliche Live-Check (`tools/check-db-live.cjs`) kennt diese Ausnahme über `EXPECTED_ANON_EXEC` und prüft sie in beide Richtungen (fehlt der Grant dort, schlägt der Check ebenfalls fehl). Bei einer service-role-only Funktion (Falle 3) zusätzlich dieselbe Abfrage für `authenticated`.

**Nachtrag (Live-Audit Juli 2026, Migration 0141):** Das 0132-Audit hatte nur die SECURITY-DEFINER-Funktionen geprüft. Sieben ältere SECURITY-INVOKER-RPCs aus der Zeit vor 0132 (`get_exercise_best_e1rm`, `get_exercise_history`, `get_user_volume_stats`, `get_session_stats`, `sync_sets_batch`, `sync_daily_logs_batch`, `sync_meso_states_batch`) trugen live noch direkte anon-EXECUTE-Grants aus der alten Default-Privileges-Regel. Risiko gering (INVOKER + RLS: `auth.uid()` ist für anon NULL, Reads leer, Writes scheitern an `user_id`), aber die App ruft sie nie vor dem Login auf; Migration 0141 entzieht PUBLIC- und anon-Grants und behält `authenticated`.

### Realtime

`zane_coaching`, `zane_coaching_notes`, `zane_user_settings` und `zane_checkins` sind in der `supabase_realtime`-Publikation: ermöglicht Live-Coaching-Einladungen und -Nachrichten sowie (Migration 0177) gepushte Coach-Status-Badges ("Client trainiert gerade" / "Check-in fällig") statt reinem Polling. (Die Publikation enthält daneben `door_events` und `motion_events`: app-fremde Tabellen eines anderen Projekts im selben Supabase-Projekt, nirgends im Repo referenziert; ignorieren und nicht anfassen.) **Cross-Device Live-Sync laufender Sessions wurde entfernt** (der lokale Store ist die alleinige Quelle für eine laufende Session; ein Coach sieht die Live-Session eines Clients per Polling via `get_active_session_detail`, nicht über Realtime). Die `zane_user_settings`-Realtime-Zeile liefert nur das grobe `in_progress_session_id`/`status_mode`-Feld (dasselbe, was `get_coach_clients_status` schon immer gepollt hat), keine Live-Session-Inhalte, das bleibt unverändert Polling-only. RLS-Policies scopen beide neuen Tabellen bereits korrekt auf die eigenen aktiven Clients eines Coaches (`zane_user_settings`: "coach can read client settings"; `zane_checkins`: "checkins_coach_read"), keine neue Policy nötig. `zane_checkins` hört nur auf `INSERT`/`UPDATE`, nie `DELETE`: Realtime kann `DELETE` bei `postgres_changes` weder filtern noch per RLS scopen, ein ungefilterter `DELETE`-Listener würde also für jeden Coach bei jedem gelöschten Check-in irgendeines anderen Coaches feuern.

`subscribeToChanges(userId, onCoachingNote, onCoachingInvite, coachClients, onCoachStatusChange)` hängt die beiden neuen Coach-Status-Listener an denselben Kanal wie die Coaching-Tabellen (kein zweiter `_supabase.channel(...)`); `coachClients` (aktive Client-Ids, ohne `support_`-Pseudo-Einträge) steuert per `in.(...)`-Filter, welche Zeilen relevant sind, oberhalb von 100 Ids fällt der Filter weg und die RLS-Policies scopen allein (nur für `INSERT`/`UPDATE`). app.jsx's `isCoachActive`-Poll (`get_coach_clients_status`/`get_coach_checkin_status`) ist seitdem nur noch der 60s-Fallback für einen getrennten/rekonnektierenden Kanal, primär treiben die Realtime-Listener das Badge.

### Friends and social preview

The Friends feature is opt-in through `zane_user_settings.show_friends_tab`. Friends and Coaching share one navigation slot. The client loads this slice and subscribes to social Realtime changes only after the setting is enabled. Friends are not included in the personal workout backup because they reference other users and server-side conversations.

### `zane_social_profiles`

- `user_id` (uuid, primary key, auth user)
- `handle` (text, nullable, unique, 3-20 lowercase letters, numbers or underscores)
- `friend_code` (text, unique, stable generated lookup code)
- `steps_visible`, `workouts_visible`, `adherence_visible` (boolean, per-metric privacy opt-in)
- `created_at`, `updated_at` (timestamptz)

### `zane_social_friendships`

- `id` (uuid, primary key), `requester_id`, `addressee_id` (uuid auth users)
- `status` (text, `pending` or `accepted`)
- `created_at`, `updated_at` (timestamptz)
- The pair is unique in either direction; blocks and accepted status are checked before messaging.

### `zane_social_blocks`

- `blocker_id`, `blocked_id` (uuid auth users, composite primary key)
- `created_at` (timestamptz)
- Blocking removes the friendship and prevents new requests or direct messages.

### `zane_social_groups`

- `id` (uuid, primary key), `owner_id` (uuid auth user)
- `name` (text), `join_code` (text, unique)
- `created_at` (timestamptz)
- Groups are private and joined by code.

### `zane_social_group_members`

- `group_id`, `user_id` (uuid, composite primary key)
- `role` (text, `owner` or `member`)
- `joined_at` (timestamptz)
- Membership is visible only to members of the same group. The authenticated dashboard RPC adds each member's display identity and only the weekly metrics that member has explicitly shared.

### `zane_social_messages`

- `id` (uuid, primary key), `sender_id`, `recipient_id`, `group_id` (uuid)
- `body` (text, required, max 4000 characters)
- `created_at` (timestamptz)
- Exactly one of `recipient_id` or `group_id` is set. RLS limits rows to the sender, direct recipient, or group member.

### `zane_social_message_attachments`

- `id` (uuid, primary key), `message_id`, `uploaded_by` (uuid)
- `storage_path`, `file_name`, `mime_type` (text)
- `created_at` (timestamptz)
- Images are stored in the private `social-chat-attachments` bucket and signed only for message participants.

### `zane_social_message_reads`

- `message_id`, `user_id` (uuid, composite primary key)
- `read_at` (timestamptz)
- A row marks a message as read for that recipient.

### `zane_social_plan_shares`

- `id` (uuid, primary key), `sender_id`, `recipient_id` (uuid)
- `plan_name` (text), `snapshot` (jsonb immutable plan copy)
- `created_at`, `imported_at` (timestamptz, nullable)
- Sharing is restricted to accepted friends. Import creates a new local schedule; the original snapshot is unchanged.

### `zane_social_reports`

- `id` (uuid, primary key), `reporter_id`, `target_user_id`, `message_id`, `group_id` (uuid, nullable targets)
- `reason` (text: `spam`, `harassment`, `unsafe`, `other`), `details` (text)
- `status` (text: `open`, `reviewed`, `closed`), `created_at` (timestamptz)
- Reports are write-once for users and readable only by the reporter or the admin review path.

### Social RPCs

`social_lookup_profile`, `social_get_dashboard`, `social_update_profile`, `social_send_friend_request`, `social_respond_friend_request`, `social_remove_friend`, `social_block_user`, `social_create_group`, `social_join_group`, `social_leave_group`, `social_create_plan_share`, `social_mark_plan_imported`, `social_report` and the RLS helper `social_is_group_member` are authenticated-only. SECURITY DEFINER functions validate `auth.uid()`, use a fixed `search_path`, and do not expose health data except metrics explicitly enabled by the profile owner. `subscribeToFriends` listens for membership, relationship, message, attachment and plan-share changes and triggers a fresh dashboard load.

The social relationship, group, membership, message, attachment and plan-share tables are also added to `supabase_realtime` with full replica identity so enabled clients can refresh their social slice after changes.
